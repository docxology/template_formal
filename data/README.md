# Source-owned evidence data

| File | Role |
| --- | --- |
| `claim_ledger.yaml` | Quantitative manuscript claims and their generating source/artifact paths. |

The ledger is deliberately separate from disposable `output/` reports. It is
the project-owned bridge between the test/formal-method evidence and the
shared manuscript evidence registry.
