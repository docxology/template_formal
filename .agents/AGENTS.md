# Agent guidance for `template_formal`

Use the project-local skill bundle in `.agents/skills/template-formal/` for
routing. Keep protocol behavior in `src/`, keep scripts as deterministic
orchestrators, and run the project test command before changing generated
outputs. Public-project publication checks are driven by the repository roster;
do not add private paths or credentials to this tree.
