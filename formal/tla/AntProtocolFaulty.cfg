SPECIFICATION Spec

INVARIANTS
  TypeOK
  NoFalseEstablishment

\* Both peers' "closed" phases are deliberately terminal (no outgoing Next
\* disjunct once both established connections are torn down and no
\* messages remain in flight); that is a genuine dead end of this
\* single-shot handshake model, not a spec bug, so TLC's deadlock detector
\* is disabled exactly as in AntProtocol.cfg.
CHECK_DEADLOCK FALSE
