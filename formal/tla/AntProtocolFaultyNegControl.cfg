SPECIFICATION Spec

INVARIANTS
  TypeOK
  NoFalseEstablishment

\* This model is DELIBERATELY BROKEN (see AntProtocolFaultyNegControl.tla's
\* header) -- TLC is expected to report a NoFalseEstablishment violation
\* here. check_formal_specs.sh inverts the usual pass/fail logic for this
\* one config: a reported invariant violation is PASS, a clean "no error
\* found" result is FAIL (the negative control would itself be vacuous).
CHECK_DEADLOCK FALSE
