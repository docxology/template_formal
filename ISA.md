---
project: templates/template_formal
task: "strongly-typed multiagent ant-robot research template"
effort: comprehensive
effort_source: explicit
phase: complete
progress: 117/119
mode: interactive
started: 2026-07-08T00:00:00Z
updated: 2026-07-10T13:00:00Z
---

## Problem

The `template` monorepo has 20 public research-project exemplars (`projects/templates/*`) covering numerical analysis, prose, madlibs, textbooks, autoresearch loops, Active Inference, and more — but none of them demonstrate strong static typing as the load-bearing research subject. Nothing in the roster shows illegal-state-unrepresentable design (ADTs, session-type-shaped protocol state machines, affine-discipline resource handles) applied to a genuinely decentralized domain. Daniel has a real research grounding for exactly this gap: a completed deep-research review (`~/Downloads/deep_research_export/deep_review.md`, 13,739 words, 8 sections, 880 sources) on strongly typed programming, category theory, formal proof assistants, and cognitive modeling — currently sitting outside the repo, unused as a template seed. Separately, there is no exemplar that models a decentralized multiagent system (independent local DB + independent local networking per agent, no shared global state) even though this is directly relevant to Daniel's ant-collective-behavior and Active Inference research.

## Vision

A visitor forks `template_formal`, runs `uv run pytest`, and watches mypy --strict and a real test suite jointly prove that an ant-robot colony simulation cannot enter illegal states — a dropped network message can't corrupt an agent's DB, a consumed transaction handle can't be reused, a handshake can't skip a phase — and then opens the rendered manuscript PDF and finds an honest, citable argument for *why* (grounded in Wadler/Pierce/Honda/Girard/Spivak/Friston, not marketing copy) that reads as scientifically serious, not a typing-features demo reel. The euphoric surprise: the manuscript explicitly draws the line between "mypy proved this" and "this is a runtime discipline, not a proof" — and the tests actually demonstrate both sides of that line with a negative control, rather than merely asserting it.

## Out of Scope

- **No real distributed deployment.** No real TCP/UDP sockets, no real multi-process or multi-host execution, no message broker. The "networking" layer is an in-process, fault-injectable typed message bus; the "database" layer is a real (non-mocked) local SQLite file per agent. This is a decentralization-*shaped* simulation, not a production distributed system.
- **No dependent-type or linear-type soundness claim.** Python has neither. Affine-style handles are a *runtime-guarded discipline*, not a compiler-enforced guarantee. The manuscript states this explicitly; no code comment, docstring, or prose sentence may claim compiler-level linearity.
- **No mandatory formal side-spec.** A Lean 4 or TLA+ specification of the protocol is optional and, if shipped, must be wired to one real runnable check command — never decorative.
- **No UI, no dashboard, no web frontend.** Manuscript + CLI scripts + figures only, matching sibling exemplar shape.
- **No production consensus/Raft/Paxos implementation.** Coordination-layer content is documentary/typed-interface only (a typed "shared pheromone field" substrate), not a full consensus protocol implementation.
- **No mocking framework anywhere in `tests/`** — real SQLite files, real subprocess `mypy --strict` invocations, real in-process objects.

## Principles

- Static typing's honest value in Python is edit-time/CI-time error prevention on structurally representable invariants — never claim runtime enforcement Python's type system does not provide.
- Illegal states should be unrepresentable wherever the type system genuinely allows it; where it doesn't (affine reuse, malformed wire bytes from an untyped boundary), the code must runtime-guard and the manuscript must say so.
- A claim in prose that cannot be pointed at a negative-control test is not a claim the manuscript is entitled to make.
- The ant-robot domain is chosen because bounded local computation, local sensing, and stigmergic (indirect, environment-mediated) coordination are actually true of ant colonies and actually motivate per-agent local DB + local networking — the domain must earn its place scientifically, not decoratively.
- Thin orchestrators, business logic in `src/`, no mocks, real coverage — same discipline as every sibling exemplar.

## Constraints

- Two-layer architecture: generic code in `infrastructure/` (none needed for this task — only if a genuinely reusable primitive emerges), project-specific code in `projects/templates/template_formal/src/`.
- Python 3.10–3.12, `uv` for all dependency/execution, `mypy --strict` must pass with zero errors on `src/`.
- `pytest --cov=projects/templates/template_formal/src --cov-fail-under=90` must pass with **zero mocks** (`MagicMock`/`mocker.patch`/`unittest.mock` forbidden).
- Must carry the canonical forkable surface per `projects/templates/AGENTS.md`: `README.md`, `AGENTS.md`, `TODO.md`, `pyproject.toml`, `.gitignore`, `.agents/skills/template-formal/SKILL.md` (+ `AGENTS.md`/`README.md`), `scripts/`, `src/`, `tests/`, `manuscript/config.yaml` + `.example`, `manuscript/references.bib`, `manuscript/preamble.md`.
- Manuscript renders through the existing pipeline (`stage_02_analysis.py` → `stage_03_render.py` → `stage_04_validate.py`) and must pass `infrastructure.validation.cli markdown` + `pdf` checks; theorem/ADT-block math must be verified via page-scale raster, not `pdftotext` alone (per repo gotcha).
- Registration is mandatory, not optional: `infrastructure/project/public_scope.py` `PUBLIC_PROJECT_NAMES`, then regenerate `docs/_generated/active_projects.md`, `docs/_generated/exemplar_roster.md`, `docs/_generated/COUNTS.md` via their own generator scripts (`scripts/docgen/*.py`) — never hand-edit generated docs directly.
- `scripts/audit/check_template_drift.py --strict` and `scripts/audit/check_tracked_projects.py` must pass post-registration.
- Do not touch or depend on the currently-uncommitted, unrelated in-flight work in this working tree (`infrastructure/rendering/{mermaid_figure,pptx_deck,slide_deck}.py`, associated tests, `pyproject.toml`/`uv.lock` diffs) — those are foreign, un-authored-this-session changes (R10 baseline: 8 changes, unrecognized files); this build stays in its own new directory and does not require the full repo-wide coverage gate to re-baseline, since it shares no files with that in-flight work (documented exception, see Decisions).

## Goal

Ship a complete, self-contained, registered public template `template_formal` under `projects/templates/` — a strongly-typed ant-robot colony simulation (per-agent SQLite DB + per-agent fault-injectable typed network protocol + Active-Inference-flavored decision loop) whose `src/` passes `mypy --strict` with zero errors, whose `tests/` hit ≥90% coverage with zero mocks and include at least one mypy-as-oracle negative-control fixture per major invariant, and whose manuscript renders to a validated PDF making an honestly-scoped argument for what static typing proves versus what remains a runtime discipline — fully registered in the public roster and passing `check_template_drift.py --strict`.

## Criteria

**Types module (`src/template_formal/types/`)**
- [x] ISC-1: `src/template_formal/types/ids.py` defines `AgentId`, `MessageId`, `TxnId` as distinct `NewType` wrappers over `uuid.UUID`.
- [x] ISC-2: mypy --strict rejects passing an `AgentId` where a `MessageId` is expected (negative-control fixture `tests/mypy_fixtures/bad_id_mixing.py` fails type-check).
- [x] ISC-3: `src/template_formal/types/result.py` defines a `Result[T, E]` tagged union via `Ok`/`Err` frozen dataclasses with a `Literal["ok","err"]` tag field.
- [x] ISC-4: `match`-based exhaustiveness on `Result` is enforced — a fixture omitting the `Err` arm is rejected by mypy via `assert_never` in the `Ok`-only branch (`tests/mypy_fixtures/bad_result_nonexhaustive.py`).
- [x] ISC-5: `Result.map`/`Result.and_then` are implemented and unit-tested for both `Ok` and `Err` cases (Kleisli-style composition).
- [x] ISC-6: A `Phase` phantom-type marker hierarchy exists (`Generic[PhaseT]`) usable by both the protocol and affine-handle modules.
- [x] ISC-7: Anti: `types/` module contains no `Any`, no `# type: ignore` without an inline justification comment.

**Storage module (`src/template_formal/storage/`)**
- [x] ISC-8: `storage/schema.py` defines the agent-local schema as typed dataclasses (columns as typed fields), documented as a functor `Schema -> Set` (Spivak framing) in the module docstring.
- [x] ISC-9: `storage/db.py` opens a real on-disk SQLite file per agent (no `:memory:`-only test-only path) and creates the schema via typed DDL generation, not hand-written SQL strings scattered across call sites.
- [x] ISC-10: A typed query builder (`Generic[RowT]`) returns `Result[list[RowT], StorageError]`, never raises for expected failure modes (missing row, constraint violation).
- [x] ISC-11: `storage/transaction.py` implements an affine-discipline `TransactionHandle` — frozen + `__slots__`, with an internal `_consumed: bool` flag.
- [x] ISC-12: Reusing a consumed `TransactionHandle` raises `ConsumedHandleError` at runtime (unit test asserts the raise, not just the type signature).
- [x] ISC-13: Anti: a `TransactionHandle.commit()` followed by a second `.commit()` on the same instance must raise, never silently succeed or double-write.
- [x] ISC-14: A test using a real SQLite file (via `tmp_path`) verifies a transaction rollback leaves the DB in its pre-transaction state (real `SELECT`, not an assumption).
- [x] ISC-15: Isolation-level config is a `Literal["deferred","immediate","exclusive"]` type, rejected by mypy if passed an arbitrary string (fixture `tests/mypy_fixtures/bad_isolation_level.py`).

**Protocol / networking module (`src/template_formal/protocol/`, `src/template_formal/network/`)**
- [x] ISC-16: `protocol/session.py` defines a session-type-shaped `Protocol` state machine: each phase (`Idle`, `Handshaking`, `Established`, `Closed`) is a distinct `Generic[PhaseT]`-tagged class.
- [x] ISC-17: Each phase's typed methods return the *next* phase's type — e.g. `Idle.open() -> Handshaking`, not a union that includes illegal successors.
- [x] ISC-18: mypy --strict rejects calling `Established`-only methods on an `Idle`-phase handle (negative-control fixture `tests/mypy_fixtures/bad_phase_transition.py`).
- [x] ISC-19: mypy --strict rejects calling a method twice that consumes/returns a new phase object without reassignment misuse being caught by an explicit consumed-flag runtime check (paired static+dynamic proof).
- [x] ISC-20: `network/bus.py` implements an in-process typed message bus (`Generic[MsgT]`) connecting agent endpoints — no real sockets.
- [x] ISC-21: The bus supports a seeded fault-injection mode with independently toggleable drop, reorder, duplicate, and corrupt behaviors.
- [x] ISC-22: A deterministic-seed test proves the same seed produces the same fault sequence (reproducibility of the fault injector itself).
- [x] ISC-23: A test drives a real handshake through the bus with drop-mode enabled and asserts the receiving agent's protocol state machine returns `Result.Err(ProtocolViolation(...))` rather than crashing or silently advancing phase.
- [x] ISC-24: A test drives a real handshake through the bus with corrupt-mode enabled (malformed wire bytes) and asserts a typed `Result.Err(MalformedMessage(...))`, demonstrating the runtime guard mypy cannot provide.
- [x] ISC-25: Anti: no test exercises only the fault-free happy path for the protocol module — every phase-transition ISC above has a paired fault-injected negative-control test.
- [x] ISC-26: Wire encode/decode round-trips a message through a real `bytes` serialization (not passing Python objects by reference) so corruption-mode fault injection has something real to corrupt.

**Agent / decision-loop module (`src/template_formal/agent/`)**
- [x] ISC-27: `agent/agent.py` defines `Agent[StateT]` combining one `TransactionHandle`-owning storage session and one protocol endpoint per instantiated agent.
- [x] ISC-28: The per-tick decision loop implements a documented free-energy-minimizing update (variational free energy over a small typed belief state), cited to Friston 2005 in a docstring, not just asserted.
- [x] ISC-29: The decision loop's expected-free-energy comparison across candidate actions is unit-tested against a hand-computed numeric expectation (not merely "runs without error").
- [x] ISC-30: Anti: the decision loop never reads or writes another agent's storage file directly — enforced structurally (no cross-agent DB handle is ever constructed) and confirmed by a test that a second agent's file path is inaccessible via the `Agent` API.
- [x] ISC-31: Agent construction requires a `NewType`-wrapped `AgentId`; mypy --strict rejects constructing an `Agent` from a bare `str`/`UUID` (fixture `tests/mypy_fixtures/bad_agent_id_construction.py`).

**Colony / coordination module (`src/template_formal/colony/`)**
- [x] ISC-32: `colony/pheromone.py` implements a typed shared substrate (a "pheromone field") that agents read/write only through a narrow typed `Protocol` interface, not a shared mutable dict exposed directly.
- [x] ISC-33: A multi-agent integration test runs N≥3 real `Agent` instances, each with its own SQLite file and bus endpoint, through a colony tick loop and asserts an emergent aggregate property (e.g., pheromone concentration converges) from real per-agent state, not from a scripted/staged result.
- [x] ISC-34: Anti: the colony coordinator does not hold direct references to any agent's internal storage or protocol handle — only to the typed public `Agent` interface.

**Formal side-spec (optional, `src/template_formal/formal/` or omitted)**
- [x] ISC-35: Decision made and recorded: either (a) a Lean 4 or TLA+ spec ships with one real runnable check command documented in `README.md`/`TODO.md`, wired into a `scripts/` entry point, and executed at least once with captured output — or (b) the formal side-spec is explicitly cut from v1 with the reasoning recorded in `## Decisions` and a forward pointer in `TODO.md`.
- [x] ISC-36: Anti: no vestigial/unwired formal-spec file exists in the shipped tree (if ISC-35 resolves to "cut," zero `.lean`/`.tla` files ship in v1).

**mypy-as-oracle proof-of-detection (cross-cutting)**
- [x] ISC-37: `tests/mypy_fixtures/` contains five known-bad `.py` negative-control fixtures, one per ISC-2/4/15/18/31 (ISC-19 has no fixture, by design — it is a runtime-only affine-reuse discipline mypy cannot observe, proven instead by a unit-test raise), plus three known-good `.py` positive-control fixtures guarding the template's advertised generic-API surfaces that `src/`-only mypy is structurally blind to: `good_agent_belief_instantiation.py` (`Agent[BeliefState]`), `good_pheromone_conformance.py` (`InMemoryPheromoneField` vs. the `PheromoneField` Protocol), and `good_bus_wire_message_instantiation.py` (`InProcessBus[WireMessage]`).
- [x] ISC-38: A single `tests/test_mypy_oracle.py` runs `mypy --strict` as a real subprocess against each known-bad fixture and asserts a non-zero exit + the expected error code/message substring.
- [x] ISC-39: The same test runs `mypy --strict` against the real `src/` tree and asserts a **zero** exit code (proof the main code is actually clean, not just the fixtures).
- [x] ISC-40: Anti: no mypy-fixture test is skipped, xfailed, or marked `# type: ignore` to force a pass.

**Manuscript**
- [x] ISC-41: `manuscript/config.yaml` + `.example` exist with project metadata, matching sibling template shape.
- [x] ISC-42: `manuscript/preamble.md` and `manuscript/references.bib` exist; bibliography includes the 12 sources identified in the deep-research digest (Wadler, Pierce, Fong & Spivak, Spivak & Kent, Milner, Honda et al., Jung et al./RustBelt, Gao/Bird/Barr, Google Security Blog 2024, Ehresmann & Vanbremeersch, Friston 2005, Ongaro & Ousterhout/Raft).
- [x] ISC-43: Manuscript includes a section explicitly scoping claims: "what mypy --strict proves" vs "what is a runtime discipline" — each strong claim in that section cites its paired test ISC by number.
- [x] ISC-44: Anti: no manuscript sentence asserts Python has compile-time linear or dependent type guarantees; a grep for the strings "dependent type" and "linear type" outside the explicitly-scoped limitations section returns zero matches.
- [x] ISC-45: Manuscript includes a section on the Active Inference / free-energy framing of the agent decision loop, citing Friston 2005 and noting the Ehresmann & Vanbremeersch Memory Evolutive Systems bridge from category theory to collective biological organization.
- [x] ISC-46: Manuscript renders to PDF via `stage_03_render.py` with zero unresolved `??` cross-references (verified via rendered-page text scan, not just exit code).
- [x] ISC-47: Any theorem/ADT-block math or code listing in the manuscript is verified via page-scale raster screenshot, not `pdftotext` alone (per repo gotcha `gotcha-theorem-block-math-mangled-pdftotext-hides`).
- [x] ISC-48: `infrastructure.validation.cli markdown` passes on `manuscript/`.
- [x] ISC-49: `infrastructure.validation.cli pdf` passes on the rendered combined PDF.

**Repo integration / forkable surface**
- [x] ISC-50: `README.md`, `AGENTS.md`, `TODO.md`, `pyproject.toml`, `.gitignore` all present at `template_formal/` root.
- [x] ISC-51: `.agents/skills/template-formal/SKILL.md` (+ `AGENTS.md`/`README.md`) present, Hermes/agentskills.io-compatible frontmatter.
- [x] ISC-52: `scripts/` contains thin orchestrators only (`00_setup_environment.py` reuse, `02_run_analysis.py`, etc. per sibling convention) — no business logic in `scripts/`, verified by grep showing scripts only import from `src/`/`infrastructure/`.
- [x] ISC-53: `pytest projects/templates/template_formal/tests --cov=projects/templates/template_formal/src --cov-fail-under=90` passes with a real captured coverage percentage ≥90%.
- [x] ISC-54: Zero uses of `MagicMock`/`unittest.mock`/`mocker.patch` anywhere in `tests/` (grep returns zero matches).
- [x] ISC-55: `uv run python -m infrastructure.project.public_scope source-paths` includes `template_formal`'s `src/` after registration.
- [x] ISC-56: `infrastructure/project/public_scope.py` `PUBLIC_PROJECT_NAMES` includes `"templates/template_formal"`.
- [x] ISC-57: `docs/_generated/active_projects.md`, `exemplar_roster.md`, `COUNTS.md` regenerated (not hand-edited) and list `template_formal`.
- [x] ISC-58: `scripts/audit/check_template_drift.py --strict` passes post-registration.
- [x] ISC-59: `scripts/audit/check_tracked_projects.py` (or the unified `check_tracked_all.py`) passes — no non-template private paths accidentally tracked.
- [x] ISC-60: `uv run python scripts/runner/execute_pipeline.py --project templates/template_formal --core-only` completes with a non-error exit and produces a combined PDF under `output/templates/template_formal/`.
- [x] ISC-61: `uv run python -m infrastructure.skills check` passes (skill manifest consistent).
- [x] ISC-62: Ruff check + format pass on the public CI source paths that include `template_formal`.
- [x] ISC-63: Bandit security scan (`bandit -c bandit.yaml -r -ll`) reports no new high/medium findings introduced by `template_formal/src`.
- [x] ISC-64: `git ls-files projects/templates/template_formal` returns every file claimed as shipped (tracked-artifact discipline, R12) — no locally-only file counted as done.

**Anti-criteria / regression guards (cross-cutting, consolidated)**
- [x] ISC-65: Anti: `mypy --strict` on `src/` exits non-zero → build is not done regardless of test pass rate.
- [x] ISC-66: Anti: any test uses `:memory:`-only SQLite for a claim about durable per-agent state (must use real on-disk `tmp_path` files for durability-relevant assertions).
- [x] ISC-67: Anti: coverage ≥90% achieved by testing type annotations exist rather than exercising behavior (each covered line must be reachable from at least one behavioral assertion, spot-checked in Verification).
- [x] ISC-68: Anti: the network-bus fault injector's drop/reorder/duplicate/corrupt modes are ever left permanently disabled by default in a way that makes ISC-23/24 vacuously pass on an all-happy-path run.

**Round 2 expansion (security review, oracle hardening, deeper formal methods, statistics, visualizations) — added 2026-07-09 after a comprehensive `/security-review /RedTeam /BeCreative /workflows` pass**
- [x] ISC-69: `scripts/check_formal_specs.sh` pins `tla2tools.jar` to a specific dated release tag (never `latest`) and verifies a hardcoded SHA-256 checksum before any `java -jar` invocation; a checksum mismatch deletes the file and fails loudly.
- [x] ISC-70: `storage/schema.py`'s `Column`/`TableSchema` validate their `name` against `^[A-Za-z_][A-Za-z0-9_]*$` at construction (`__post_init__`), with a real test proving a SQL-injection-shaped identifier (`"; DROP TABLE ..."`) raises `ValueError` before reaching any raw-SQL f-string.
- [x] ISC-71: The mypy-oracle harness type-checks concrete instantiations of every generic/Protocol in `src/` that `src/` itself never binds to a concrete type — `good_bus_wire_message_instantiation.py` (`InProcessBus[WireMessage]`) and `good_pheromone_conformance.py`/`bad_pheromone_protocol_violation.py` (`PheromoneField` conformance) — closing the same blind-spot class ISC-31's `Agent[BeliefState]` fixture closed for one symbol.
- [x] ISC-72: `test_known_bad_fixture_is_rejected_by_mypy_strict` binds each `bad_*.py` fixture to its own captured expected-error substring (not a generic `"error:"` check), so an unrelated future breakage in a fixture can no longer silently keep the test green for the wrong reason.
- [x] ISC-73: `formal/lean/AntProtocol.lean` proves `phase_exhaustive`, `closed_is_terminal`, a minimal affine `SessionToken`/`cannot_reuse_consumed_token` model (the first Lean counterpart to the runtime `SessionConsumedError` discipline), and `step_to_closed_cases` (promoted from a helper after `closed_only_via_known_paths` was found and removed for the same near-vacuity class `no_direct_idle_to_established` already fixed once) — 7 theorems total, zero `sorry`, zero extra axioms.
- [x] ISC-74: `formal/tla/AntProtocol.tla` gained a real liveness property (`HandshakeEventuallyResolves`, checked under weak fairness) in addition to its prior safety-only invariants.
- [x] ISC-75: `formal/tla/AntProtocolFaulty.tla` (+ `.cfg`) models the fault-injected two-peer protocol (explicit message channel, drop/duplicate/corrupt actions) and checks `NoFalseEstablishment` — the direct TLA+ analogue of ISC-23/24 — and is wired into the same `scripts/check_formal_specs.sh` command as the Lean build and the ideal-protocol TLA+ check (all three must pass for the script to exit 0).
- [x] ISC-76: `colony/experiment.py` + `colony/stats.py` compute a real, falsifiable statistical claim: across N=150 independently-seeded, heterogeneous-preference/noisy-sensing trials, the Wilson-score 95% confidence-interval lower bound on the convergence rate exceeds 0.8 — paired with a heterogeneity-non-vacuity guard, a negative control (the old deterministic config reproduces exact 100% through the same harness), and a positive-control-that-can-fail (a deliberately-defeated config drives the Wilson upper bound below 0.5).
- [x] ISC-77: `scripts/02_run_analysis.py` generates two real, data-driven matplotlib figures (deterministic-demo convergence; heterogeneous-sweep consensus-tick distribution) from real simulation output, registered in `output/figures/figure_registry.json` and referenced from the manuscript with honest, distinct captions (mechanism-demonstration vs. earned-statistical-result) — `infrastructure.validation`'s figure-registry check passes.
- [x] ISC-78: Anti: `run_demo_colony`/`run_statistics_sweep`/figure-writing functions live in `src/template_formal/colony/{demo,visualization}.py` with real behavioral test coverage, not in `scripts/02_run_analysis.py` (thin-orchestrator discipline; `check_template_drift.py --strict`'s `thin_orchestrator` check passes).
- [x] ISC-79: Anti: no theorem in `formal/lean/AntProtocol.lean` has a conclusion satisfiable by a fixed proof term regardless of its hypothesis (the `closed_only_via_known_paths` near-vacuity class) — re-verified by direct inspection of every remaining theorem's conclusion shape after the round-2 fix.

**Round 3 expansion (third adversarial pass: security-deep, redteam-round2-code, first-principles-deepen) — added 2026-07-09**
- [x] ISC-80: `colony/experiment.py`'s `ColonyTrialConfig` validates `decay` (must be a fraction in `[0.0, 1.0]`), `sensing_noise_std` (must be `>= 0.0`), and `preference_variance` (must be `>= 0.0`) in `__post_init__`, raising `ValueError` at construction time — closing the same unvalidated-numeric-field gap ISC-70 (SQL identifiers) and ISC-15 (isolation-level `Literal`) already closed for other config surfaces. Proven by `tests/colony/test_colony_experiment_config.py`: `decay=-0.1`, `decay=1.5`, and `sensing_noise_std=-1.0` each raise `ValueError` at construction; the full valid boundary/interior range (`decay` in `{0.0, 0.02, 0.46, 0.97, 1.0}` crossed with `sensing_noise_std` in `{0.0, 0.5, 4.0}` and `preference_variance` in `{0.0, 1.0, 3.0}`) still constructs without error.
- [x] ISC-81: `agent/agent.py`'s `BeliefState` validates `variance` (must be finite and `> 0.0`) in `__post_init__`, raising `ValueError` at construction — the deepest gap this round's audit found: `BeliefState(mean=0.0, variance=0.0)` previously constructed silently and only crashed three calls later inside `gaussian_kl_divergence`/`gaussian_differential_entropy` with an undocumented `ZeroDivisionError`/`ValueError: math domain error`, feeding the manuscript's headline Active-Inference claim through an unguarded numeric boundary. Independently converged on by both a FirstPrinciples pass and a security-deep pass. Proven by `tests/agent/test_agent_free_energy.py`: `variance=0.0`, `variance=-1.0`, `variance=math.nan`, and `variance=math.inf` each raise `ValueError` at `BeliefState.__init__` (not deep inside `expected_free_energy`); `variance=1e-9` still constructs.
- [x] ISC-82: `colony/stats.py`'s `wilson_score_interval` raises its own documented `ValueError` (not an undocumented `statistics.StatisticsError`) when `confidence` is so close to `1.0` that `1 - (1-confidence)/2` rounds to exactly `1.0` in float arithmetic — a RedTeam-probed edge case (`confidence=0.9999999999999999` previously leaked the undocumented exception type). Proven by `tests/colony/test_colony_stats_unit.py::test_wilson_score_interval_raises_valueerror_not_statistics_error_near_one`.
- [x] ISC-83: `colony/demo.py`'s one `# type: ignore[arg-type]` (spreading `config_kwargs: dict[str, object]` into `ColonyTrialConfig`) carries an inline justification comment naming the runtime-compensating check (`ColonyTrialConfig.__post_init__`, ISC-80) — closing the anti-criterion ISC-7 already requires (every `# type: ignore` justified) for the one call site in this template where untyped-shaped data reaches a strongly-typed constructor.
- [x] ISC-84: `colony/experiment.py`'s `ColonyTrialConfig.preference_variance` guard is tightened from `>= 0.0` to `> 0.0`, matching the stricter bound its sole consumer (`BeliefState.__post_init__`, ISC-81) already enforces — a Forge cross-vendor audit of the ISC-81 fix caught that the looser config-level guard still accepted `preference_variance=0.0`, which then failed one call later inside `run_colony_trial`'s `BeliefState` construction, recreating the exact "fails downstream, far from the misconfiguration" defect class ISC-81 was written to eliminate, one level removed. Proven by `tests/colony/test_colony_experiment_config.py::test_zero_preference_variance_raises` (updated from the prior `test_zero_preference_variance_is_accepted`, which asserted the now-corrected behavior) and the boundary-sweep test's `preference_variance` domain updated from `(0.0, 1.0, 3.0)` to `(1e-9, 1.0, 3.0)`.

**Round 4 expansion (genuine scientific/methodological depth: a null-model baseline, a generic parameter-sweep runner, and three real, pre-registered experiments) — added 2026-07-09**
- [x] ISC-85: `colony/nullmodel.py` implements `run_null_model_trial` -- each agent picks a location via `random.Random(config.seed).choice(locations)` every tick, with **zero** references anywhere in the module to the pheromone-field type, the free-energy `Agent` class, or `BeliefState` (a structural isolation guarantee, not a behavioral one) -- proven by `tests/colony/test_nullmodel.py`'s source-text grep test (`test_nullmodel_module_never_references_pheromone_field_or_agent_machinery`) and an AST-based import-allowlist test. The same seed reproduces byte-for-byte identical `choice_history` across independent calls (`test_same_seed_produces_identical_choice_history_across_two_independent_calls`), and convergence is computed via the *same* `find_sustained_consensus_tick` the real-mechanism harness uses, so a rate comparison between the two harnesses is apples-to-apples under an identical definition of "converged."
- [x] ISC-86: `colony/sweep.py` implements `run_parameter_sweep` -- a generic, pure, stdlib-only runner that reuses `run_colony_trial` and `colony/stats.py`'s `convergence_rate`/`wilson_score_interval` to produce one `SweepPointResult` (value, n, successes, rate, Wilson bounds) per swept value, with `param_name` validated eagerly against `ColonyTrialConfig`'s real field names (rejecting `seed` itself and any typo'd/non-existent field, closing the same "config-knob consumption not naming" gap class this repo's memory flags) and a paired-seed design (every sweep point reuses the identical seed sequence in its own on-disk subdirectory) documented as a deliberate variance-reduction choice. Proven by `tests/colony/test_sweep.py`: validation-rejection tests for a bad `param_name`, `seed` itself, empty `values`, and `n_per_value < 1`; a hand-derivable-result test against a fully-deterministic 100%-converging configuration whose Wilson bounds are checked against `wilson_score_interval` called directly; and a disjoint-subdirectory test proving no SQLite path collision across sweep points.
- [x] ISC-87: **Experiment A (decay-rate sensitivity)**, real result: at the calibrated baseline (8 agents, 2 locations, 30 ticks, `preference_mean_range=(8,12)`, `sensing_noise_std=0.5`), convergence rate is **non-monotonic** in `decay` over `{0.10, 0.30, 0.46, 0.60, 0.80, 1.00}` (`n=60` per value, `seed_base=0`): `0.10`→0/60, `0.30`→0/60, `0.46`→56/60, `0.60`→60/60, `0.80`→60/60, `1.00`→56/60 -- a sharp near-zero-to-plateau threshold (not a smooth trend) plus a measurable decline at total evaporation (`1.00` scores below `0.60`/`0.80`), falsifying the monotonic-non-decreasing null hypothesis stated before the run. Proven by `tests/colony/test_colony_experiments_extended.py::test_decay_sweep_real_numbers_match_the_calibrated_run` (pins the exact `successes` counts) and `::test_decay_sweep_shows_a_plateau_then_a_measurable_decline_at_the_extreme` (the falsifiable non-monotonicity assertion itself).
- [x] ISC-88: **Experiment B (real mechanism vs. random-choice null model)**, real result: at the identical calibrated baseline, `N=150` real-mechanism trials and `N=150` null-model trials (identical seed sequence, `seed_base=0`) score `140/150` (rate `0.9333`, Wilson `(0.8816, 0.9634)`) vs. `1/150` (rate `0.0067`, Wilson `(0.0012, 0.0368)`) -- non-overlapping Wilson intervals, decisively rejecting the null hypothesis that the real mechanism is no better than chance. The null model's own rate is cross-checked against a back-of-envelope expected count (`150 × 2 × (1/2)^8 ≈ 1.2`, consistent with the observed `1/150`) as a sanity check that the baseline itself behaves as an uninformed random choice should. Proven by `tests/colony/test_colony_experiments_extended.py::test_real_mechanism_outperforms_the_null_model_with_nonoverlapping_wilson_intervals` (pins both exact `successes` counts and asserts `real_lower > null_upper`).
- [x] ISC-89: **Experiment C (heterogeneity-magnitude sweep)**, real result: convergence rate decreases **strictly monotonically** as `preference_mean_range`'s width increases over `{tight (9,11), medium (8,12), wide (5,15), very_wide (2,18)}` (`n=60` per condition, `seed_base=0`): `60/60` → `56/60` → `15/60` → `2/60` (rates `1.0000 > 0.9333 > 0.2500 > 0.0333`) -- the monotonic-decrease hypothesis stated before the run is **not** falsified; the largest single drop (`medium`→`wide`, ~70 percentage points) shows the degradation is sharp, not gradual. Proven by `tests/colony/test_colony_experiments_extended.py::test_heterogeneity_sweep_real_numbers_match_the_calibrated_run` (pins the exact `successes` counts) and `::test_heterogeneity_sweep_convergence_rate_decreases_monotonically_with_width` (the strict-ordering assertion).
- [x] ISC-90: `manuscript/05_results_discussion.md` gains a "Three pre-registered experiments" section (plus an abstract-contributions update in `00_abstract.md` and a "Threats to the claims" scoping update) presenting all three real findings above with the same honesty discipline as every existing section: each hypothesis and its falsification criterion is stated *before* the real, unrounded result; a plausible mechanistic account for Experiment A's threshold effect is explicitly flagged as "inference from trace inspection," distinct from the better-supported claim that `decay` has a real causal effect on the rate within this controlled simulation; and a closing hedge paragraph states what is NOT shown (other colony sizes/location counts/tick horizons; the additional seed-base robustness spot-checks are noted as non-gated). Anti: no number quoted in this section is rounded, interpolated, or reported without its Wilson interval; every quoted `successes` count is bound to a passing regression-guard test in `tests/colony/test_colony_experiments_extended.py`.
- [x] ISC-91: `manuscript/05_results_discussion.md`'s original N=150 statistical-claim section discloses four honesty gaps a dedicated RedTeam pass found (and which the same-round Build agent did not see, since only a generic paraphrase of the Science hypotheses — not the RedTeam agent's actual findings — was passed into its prompt; the primary session reconciled this gap directly after reading the RedTeam agent's full report): (1) the N=150 configuration's parameters were *hand-calibrated by iterating* until the rate cleared 0.8 — now disclosed explicitly, with the wide margin reframed as partly a mechanical consequence of that search, not solely independent confirmation; (2) the "Threats to the claims" section previously scoped its single-configuration caveat only to the 3-agent mechanism-demo test, leaving the 8-agent/N=150 statistical claim with no disclosed generalization limit of its own — now explicitly extended to cover it; (3) the existing positive-control-that-can-fail changes `decay` and `sensing_noise_std` simultaneously and cannot alone attribute a convergence collapse to loss of the stigmergic mechanism versus noise magnitude — now disclosed as a confound, with an explicit note that the new decay-only sweep (ISC-87) partially addresses it (isolates decay) while the noise-magnitude axis remains untested and is named as future work; (4) the exploratory `pearson_r` correlation (preference spread vs. consensus tick, `r=0.1648`) was computed and tested but never mentioned in manuscript prose — now surfaced with its real value and correct "weak, exploratory, not a second confirmed result" hedge.
- [x] ISC-92: `colony/stats.py`'s new `fisher_exact_test_two_sided` (a real hypergeometric computation via `math.comb`, no `scipy` dependency) backs a fifth honesty fix a subsequent Forge cross-vendor pass caught: ISC-87's decay-sweep prose had called the `60/60`-to-`56/60` decline at `decay=1.00` "a real, measurable decline... not statistical noise," but a normal-approximation two-proportion test is invalid at this exact boundary (one group at the `100%` proportion — the identical pathology `wilson_score_interval` was already hardened against, ISC-82), and the methodologically correct Fisher's exact test on this one pairwise comparison gives two-sided $p=0.1187$ — not significant at $\alpha=0.05$. The manuscript is corrected to state this precisely, honestly relocating the decline's evidentiary weight to the 13-point exploratory sweep's shape and cross-seed replication rather than to this one boundary-adjacent pairwise test, and naming a formal ordered-trend test (e.g. Cochran-Armitage) as explicitly-not-yet-done future work. Proven by `tests/colony/test_colony_stats_unit.py`: `test_fisher_exact_matches_manuscript_quoted_decay_dip_pvalue` pins the exact manuscript-quoted `p=0.1187244128420599` (and asserts `p > 0.05`, the point the correction makes), plus symmetry, near-1.0, extreme-difference, and input-validation tests.

**Round 5 expansion (folder-level documentation: a `README.md`/`AGENTS.md` pair at every real directory, a new `docs/` guide tree, independent re-verification of test parallelization, and one dead-code removal) — added 2026-07-09**
- [x] ISC-93: Every real directory under `template_formal` — the project root, `src/`, `src/template_formal/` and each of its six subpackages (`types/`, `storage/`, `protocol/`, `network/`, `agent/`, `colony/`), `tests/` and each of its six subdirectories (`mypy_fixtures/`, `storage/`, `protocol/`, `network/`, `agent/`, `colony/`), `scripts/`, `manuscript/`, `formal/` and its two subdirectories (`lean/`, `tla/`), and the new `docs/` — carries both a `README.md` and an `AGENTS.md`, verified by a full-tree enumeration (`find` over every directory, `.venv`/cache/build-artifact directories excluded) rather than a spot check. Closes the "no folder without a local map" gap this template's sibling exemplars (e.g. `template_textbook/src/mermaid/`) already established at the leaf-module level but `template_formal` had left open below the project root.
- [x] ISC-94: A new `docs/` directory ships six topic guides — `architecture.md`, `type_system_guide.md`, `formal_methods_guide.md`, `statistics_methodology_guide.md`, `security_guide.md`, `testing_guide.md` — plus its own `README.md`/`AGENTS.md` index pair. Each guide is grounded in real file:line citations against the shipped code (spot-checked line-by-line against `src/template_formal/storage/{schema,db}.py`, `src/template_formal/agent/agent.py`, `src/template_formal/colony/experiment.py`, `formal/lean/AntProtocol.lean`, `formal/tla/*.tla`, and `ISA.md` itself during this round's verification pass) rather than restating the manuscript's prose in different words.
- [x] ISC-95: `pytest-xdist>=3.6.0` is a real `[project.optional-dependencies] dev` entry in `pyproject.toml`, and `uv run pytest tests -n 4 --no-cov -q` passes all 215 tests (independently re-run this round: `215 passed in 72.89s`). `tests/README.md` documents the honest, measured (non-)benefit of parallelization for this suite specifically — two large, module-scoped, sequential experiment-sweep files (`test_colony_convergence_statistics.py`, `test_colony_experiments_extended.py`) cannot be split across xdist workers because each is *one* pytest function running hundreds of real trials in an internal loop — including one previously-observed CPU-contention flake under `-n auto` (a wall-clock-budget assertion tripping under 14-worker contention) and its resolution (re-run serially before treating a parallel-run failure as real).
- [x] ISC-96: Anti: no vestigial file exists in the shipped tree. A dead, never-imported `src/template_formal/formal/` package stub (predating the round-2 decision to land the formal side-specs at the project-root `formal/` instead) was found and deleted this session — verified via `find src/template_formal -mindepth 1 -type d -iname 'formal'` (returns nothing), which is the correct check; the naively-tempting `find src -type d -iname '*formal*'` is **not** equivalent, since it also matches `src/template_formal` itself (the package directory name contains the substring "formal") and would falsely appear to fail this check forever.

**Round 6 expansion, part 1 (title, procedural cover art, auto-numbered formalisms) — added 2026-07-09**
- [x] ISC-97: `manuscript/config.yaml`'s `paper.title`/`paper.subtitle` are updated to "Illegal States, Mostly Unrepresentable" / "Session Types, Affine Handles, and Machine-Checked Invariants in a Simulated Ant-Robot Colony -- and an Honest Account of Where the Guarantees Stop" — the "Mostly" hedge and the closing clause mirror the manuscript's own epistemic-honesty thesis in the title itself, distinct from the unqualified Minsky "illegal states unrepresentable" idiom.
- [x] ISC-98: A new, real, tested, deterministic (seeded) procedural cover-art generator ships at `src/template_formal/colony/cover_art.py` (`generate_cover_art`, matplotlib-only, no external AI image-generation API, no network call — the Art skill's actual image models were unavailable in this environment, and a deterministic generator is more consistent with this template's zero-mocks/reproducible-output discipline than a committed AI-generated binary would be), wired via a thin-orchestrator `scripts/zz_generate_cover_art.py` to `manuscript/figures/cover_colony.png`, and referenced from `manuscript/config.yaml`'s new `paper.cover.image` field. Same-seed calls are byte-identical (proving the seeded `random.Random` — not the process-global `random` module — drives every stochastic choice); different seeds differ.
- [x] ISC-99: `manuscript/preamble.md` declares real `\newtheorem` environments (`theorem`, `lemma`, `proposition`, `corollary`, `definition`, `hypothesis`) on one shared running counter, matching the shared-counter convention `infrastructure/rendering/web_renderer.py` already documents for its own `\begin{theorem}`-block-to-numbered-Div rewrite on the HTML/web path. Ten manuscript claims across `02_type_architecture.md`, `03_storage_as_functor.md`, `04_active_inference.md`, and `05_results_discussion.md` are converted from prose-only claims into real, auto-numbered `\begin{definition}`/`\begin{proposition}`/`\begin{hypothesis}`/`\begin{theorem}` blocks (rendered combined PDF at round-6 time: Definition 1 through Theorem 10, one continuous sequence); the free-energy formula gains a real `{#eq:expected-free-energy}` pandoc-crossref label, cross-referenced by number (`eq. 1`) from surrounding prose. (Round 8's ISC-108 added an eleventh block, a new `\begin{hypothesis}` inserted before the closing `\begin{theorem}` in `05_results_discussion.md` — the sequence became Definition 1 through Theorem 11. Round 9's ISC-109 adds a twelfth block, another `\begin{hypothesis}` for the `sensed_concentration_cap` ablation, inserted inside Experiment A's section, before Experiment B's own hypothesis block — the sequence is now Definition 1 through Theorem 12; no manuscript prose anywhere hardcodes a specific formalism number, so each shift is purely automatic via the shared `\newtheorem` counter, verified by a page-scale raster/`pdftotext` re-check being unnecessary since no prose references "Theorem 10"/"Theorem 11" or any other specific number by name -- confirmed by `grep`.)
- [x] ISC-100: Anti: every raw-LaTeX formalism block is bare, unfenced text (never inside a triple-backtick ` ```latex ` fenced block, which pandoc would instead render as a literal, non-passthrough code listing) and uses `\(...\)` for all inline math inside the block (never bare `$...$`, which falls outside the shared PDF renderer's unicode-remap math-boundary protection and gets ASCII-mangled, e.g. `\alpha` -> "alpha" — the exact gotcha class recorded in this repo's memory from a prior project). Verified by page-scale raster read (not `pdftotext` alone) of the rendered PDF's Definition-5 page (the one formalism block containing real inline math): renders as proper typeset Gaussian notation, not mangled ASCII text.
- [x] ISC-101: Anti: the shared `infrastructure/rendering/slides_renderer.py` beamer-slides render path does not hard-fail on the new formalism blocks. Beamer's document class already defines `\theorem`/`\lemma`/`\corollary`/`\definition` natively (redeclaring them via `\newtheorem` fails with "Command ... already defined" — caught by an initial render attempt that failed exactly this way); the fix adds `\newtheorem` only for the two environments beamer does not ship (`proposition`, `hypothesis`, each with its own independent counter, not chained to beamer's internal theorem counter) to the renderer's existing math-header fallback mechanism, with a new regression test (`test_helper_defines_proposition_and_hypothesis_environments`) pinning both the presence of the two new declarations and the absence of the four that would conflict.

**Round 6 expansion, part 2 (methods-deepening: an ordered-trend statistical test, a gated cross-seed replication, and a mechanically-verified TLA+ negative control) — added 2026-07-09**
- [x] ISC-102: `colony/stats.py` gains `cochran_armitage_trend_test(ns, successes, scores) -> tuple[float, float]` — a real, stdlib-only (no `scipy`), closed-form two-sided Cochran-Armitage trend test (`math`/`statistics.NormalDist` only), matching the exact statistical gap all three independent ideation passes (FirstPrinciples, RedTeam, Science) converged on and this project's own manuscript had already named as explicit unaddressed future work. Applied to both the decay sweep (`Z=14.5684`, strongly positive — dominant low-to-high rise, explicitly scoped as *not* evidence against the local high-decay dip, which remains a separate, pairwise finding) and the heterogeneity sweep (`Z=-12.7559`, strongly negative — confirmatory of the already-established strict monotonic decrease). `cochran_armitage_trend_test` is re-exported from `colony/__init__.py` (closing an export-consistency gap the FirstPrinciples build agent flagged: the function existed in `stats.py` but was not yet surfaced at the package level like every other public stats symbol).
- [x] ISC-103: A previously prose-only, ungated robustness claim ("the heterogeneity sweep's ordering was spot-checked at additional seed bases") is converted into a real, gated regression test: an independent `seed_base=7000` replication of the heterogeneity sweep (sharing zero seeds with the `seed_base=0` sweep) reproduces the identical strict monotonic-decrease *ordering* (`60/54/14/5`, tight > medium > wide > very_wide) with different exact `successes` counts, as expected for a disjoint seed block — the manuscript is corrected to state precisely which robustness claims are now gated (heterogeneity, at `seed_base=7000`) versus which remain informal spot-checks (decay, at `seed_base=1000/5000`), rather than blurring the two under one "robustness was checked" sentence.
- [x] ISC-104: Anti (RedTeam finding, MEDIUM, confirmed by direct grep): the manuscript's quoted decay-dip Fisher p-value (`p=0.1187244128420599`) was bound only to hardcoded integer literals in `tests/colony/test_colony_stats_unit.py` (`fisher_exact_test_two_sided(60, 60, 56, 60)`), completely disconnected from `decay_sweep_points` — the fixture in `tests/colony/test_colony_experiments_extended.py` that actually generates those numbers. A re-tuned `seed_base`/`n_per_value`/baseline config could silently drift the sweep's real decay=0.6/1.0 counts while the hardcoded literals (and the manuscript prose quoting them) stayed frozen and unflagged. Fixed by `test_decay_dip_fisher_pvalue_is_derived_from_this_sweeps_own_fixture`, which computes the Fisher p-value directly from `decay_sweep_points`'s live `by_value[0.6]`/`by_value[1.0]` and asserts it equals the manuscript-quoted value — the claim is now self-verifying against its own generating experiment.
- [x] ISC-105 (HIGH severity, mutation-test-confirmed): the manuscript's auto-numbered Theorem 10, clause (iii), originally claimed `NoFalseEstablishment` (TLA+) was checked "under arbitrary drop/corrupt/duplicate fault interleaving" — reading as content-integrity resilience. A RedTeam pass proved by direct mutation test (widening `ReceiveHello`'s guard to also accept `"corrupt"`-tagged frames; state space grew 92→136 distinct states yet the invariant still reported zero violations) that the invariant is actually a **send-provenance** check (`iSentHello`/`rSentAck` ghost flags, set once by a genuine send and never reset, never depending on whether the *specific delivered* message was corrupted) — the same *near-vacuity* class already found and fixed twice on the Lean side. Fixed with a permanent negative control, `formal/tla/AntProtocolFaultyNegControl.tla` + `.cfg` — a verbatim sibling of `AntProtocolFaulty.tla` plus one added `ForgeHello` action injecting an unauthenticated hello *without* setting `iSentHello` — verified by direct TLC invocation to produce a real invariant violation in a 3-state trace (`Init → ForgeHello → ReceiveHello` establishes `rPhase` while `iSentHello` is still `FALSE`), proving `NoFalseEstablishment` genuinely depends on send-provenance holding. Wired into `scripts/check_formal_specs.sh` as a fourth check with **inverted** pass/fail logic (a reported violation is PASS; a clean "no error found" would mean the negative control itself is vacuous, and is FAIL). Theorem 10's clause (iii) is reworded to state "send-provenance" precisely, with a new paragraph in `manuscript/05_results_discussion.md` narrating the mutation test, the fix, and the re-verification — mirroring this template's own `tests/mypy_fixtures/bad_*.py` negative-control discipline for the formal side-spec, not merely reworking the prose.
- [x] ISC-106: Anti (workflow-orchestration lesson, this session): a `Workflow` script's `pipeline(buildPrompts, singleStageFn)` call with only ONE stage function runs all items with no synchronization barrier between them — items are not implicitly serialized just because they came from one `pipeline()` call. Three build agents (first-principles-track, redteam-track, science-track) were dispatched this way against overlapping target files; one agent (first-principles-track) correctly detected a live concurrent edit (`Edit` failed with "modified since read") and stood down rather than corrupting the in-flight work, and one (science-track) completed first and landed its changes. Files a template-level generalization of the pre-existing sibling gotcha `gotcha-concurrent-claude-sessions-collide-on-new-project-dir` — the collision risk is not limited to separate human-launched sessions, it applies equally to sibling build agents inside one workflow's own `pipeline()`/`parallel()` call whenever their target file sets can overlap; the mitigating factor here was that the colliding agent detected and stood down rather than force-writing, which is not guaranteed behavior and should not be relied upon — a future workflow with overlapping build targets should explicitly serialize those stages or partition file ownership up front instead.

**Round 7 expansion (decay-sweep cross-seed replication, gated — the same fix ISC-103 already applied to the heterogeneity sweep) — added 2026-07-09**
- [x] ISC-107: The decay sweep's cross-seed robustness claim was, like the heterogeneity sweep before ISC-103, only an ungated prose spot-check (`seed_base=1000`/`5000`, checked by hand, never pinned as a regression test). Converted to a real, gated regression test: an independent `seed_base=1000` replication of the decay sweep (identical six decay values `{0.10,0.30,0.46,0.60,0.80,1.00}`, `n=60` per value, identical calibrated baseline configuration as the `seed_base=0` sweep) reproduces the same *qualitative* threshold-then-plateau-then-decline shape — successes `0/60, 0/60, 58/60, 60/60, 60/60, 53/60`, versus the `seed_base=0` run's `0/60, 0/60, 56/60, 60/60, 60/60, 56/60` — a near-zero floor at decay ≤ 0.30, a 100% plateau at decay ∈ {0.60, 0.80}, and a measurable decline at decay=1.00 relative to that plateau (`53/60` here, `56/60` at `seed_base=0`; different exact magnitude, same direction). `1000` was chosen over `5000` specifically because it is the exact seed base the manuscript's pre-existing honesty-hedges prose already named, so the new gated test corroborates the already-published number rather than introducing an unpublished one; `5000` remains an informal, ungated spot-check. The manuscript's "Honesty hedges" paragraph is updated to state precisely which decay robustness claim is now gated (`seed_base=1000`) versus which remains an informal note (`seed_base=5000`), mirroring exactly how ISC-103 worded the equivalent heterogeneity-sweep distinction. Proven by `tests/colony/test_colony_experiments_extended.py::test_decay_replication_at_seed1000_reproduces_the_exact_counts` (pins the exact `successes` counts) and `::test_decay_threshold_then_plateau_then_decline_shape_replicates_at_a_disjoint_seed_base` (the falsifiable shape assertion itself — low-decay floor, high-decay plateau, top-end decline, with no claim that the exact decline magnitude generalizes across seed bases).

**Round 8 expansion (closes the Experiment B real-vs-null attribution confound with a real zero-deposit ablation) — added 2026-07-09**
- [x] ISC-108: ISC-88's real-vs-null comparison (`140/150` real vs. `1/150` null) cannot, alone, attribute the gap to the pheromone/stigmergic channel specifically versus general noise-robustness of the free-energy decision rule, because `colony/nullmodel.py`'s null model has no `Agent`/`BeliefState`/decision-loop machinery at all — it is a structurally different code path (`random.Random(seed).choice` per tick, ISC-85), not a controlled ablation of the real mechanism. Closed by building the missing middle condition: `N=150` real trials (`run_colony_trial`, identical seed sequence `seed_base=0`, identical calibrated baseline configuration) with `deposit_amount=0.0` — agents still sense/decide/reason via the real free-energy loop every tick, but the pheromone field never receives a deposit. First confirmed directly (not assumed from reading the source) that `ColonyTrialConfig.__post_init__` has no runtime guard on `deposit_amount` — unlike `decay`/`sensing_noise_std`/`preference_variance`, which are all validated — so `deposit_amount=0.0` constructs without error. **Real result, reported honestly regardless of which way it went:** the zero-deposit condition scores `0/150` (Wilson `(0.0000, 0.0250)`), statistically indistinguishable from the null model's `1/150` (Wilson `(0.0012, 0.0368)`) — the pre-registered $H_0$ ("zero-deposit real mechanism converges no better than the null model") **survives**, i.e. is NOT falsified. This is the confound-closing outcome: severing only the pheromone-deposit channel collapses convergence to chance level even though the entire real decision loop is otherwise untouched, so the real mechanism's advantage over chance (ISC-88) is attributable specifically to the deposit/stigmergic channel, not to some deposit-independent property of the free-energy rule. A second, confirmatory comparison against the full mechanism (`140/150`, Wilson `(0.8816, 0.9634)`) makes the attribution explicit: the full mechanism's lower bound clears the zero-deposit condition's upper bound (`0.0250`) by a wide margin, and since `deposit_amount` is the *only* input changed between the two conditions, it is the single controlled variable responsible for the entire gap. Scope, honestly stated: this ablation is run only at the calibrated baseline (`decay=0.46`, `sensing_noise_std=0.5`), only at `seed_base=0` (no cross-seed-base replication, unlike the heterogeneity sweep's ISC-103 gate), and does not characterize the dose-response curve between `deposit_amount=0.0` and `1.0` — all three are named explicitly as scope limits, not silently generalized past. It also does not close the separate, pre-existing confound at ISC-91 (item 3): the extreme positive control at `decay=0.97`/`sensing_noise_std=4.0` still has no `deposit_amount=0` ablation *at that configuration* — the manuscript's honesty-hedges prose is updated to distinguish the two gaps rather than imply one closes the other. Proven by `tests/colony/test_colony_experiments_extended.py::test_zero_deposit_config_constructs_without_error_and_never_deposits` (the `__post_init__` assumption), `::test_zero_deposit_real_mechanism_does_not_beat_the_null_model_closing_the_stigmergy_confound` (the falsifiable comparison itself, pinning `zero_deposit_successes == 0`), and `::test_zero_deposit_collapse_relative_to_the_full_mechanism_implicates_the_deposit_channel_specifically` (the confirmatory full-mechanism-vs-zero-deposit comparison).

**Round 9 expansion (dedicated ablation of Experiment A's "plausible mechanism" account) — added 2026-07-09**
- [x] ISC-109: Experiment A's mechanistic account (low decay fails because both candidates' sensed concentrations grow past the preference range in lockstep, collapsing the KL term's discriminating signal to the sensing-noise floor) was, since round 6, explicitly flagged as trace-inspection inference, "**not** a claim independently isolated by a dedicated ablation ... that would be a distinct future experiment." This round builds it. Added `ColonyTrialConfig.sensed_concentration_cap: float | None = None` (default `None` is fully behavior-preserving — confirmed by the full pre-existing `test_colony_experiments_extended.py` suite, including `decay_sweep_points`, reproducing identical pinned `successes` counts unchanged), validated in `__post_init__` (must be `> 0.0` when set, mirroring the existing guard-boundary discipline for `decay`/`sensing_noise_std`/`preference_variance`), and wired into `run_colony_trial`'s candidate list-comprehension so that when set, `field.sense(location)` is clipped to at most the cap *before* the sensing-noise term is added (a saturating-sensor model, not reduced noise). Cap value `13.0` chosen deliberately, not tuned to maximize the effect: it sits only `1.0` above the preference range's ceiling (`12`, two sensing-noise standard deviations, `sigma=0.5`) — high enough that ordinary within-range sensing is unaffected, low enough to bound exactly the runaway growth the hypothesis names. An exploratory, ungated probe (not quoted as a result) confirmed the effect is not knife-edge at this exact value — it holds essentially unchanged for any cap in `[12.5, 14.0]` and fades out by `cap=20.0` (which never binds within the 30-tick horizon and reproduces the uncapped `0/60` exactly), so `13.0` is representative, not cherry-picked. **Real result, reported honestly regardless of which way it went:** `n=60` trials at decay `{0.10, 0.30}`, identical `seed_base=0`, identical calibrated baseline except `sensed_concentration_cap=13.0` — both score `60/60` (Wilson `(0.9398, 1.0000)`), versus the uncapped baseline's `0/60` (Wilson `(0.0000, 0.0602)`) at both — a decisive, non-overlapping improvement. $H_0$ ("capping does not improve convergence at low decay") is **rejected**: this is real evidence, not merely trace-inspection inference, that the hypothesized mechanism is at least *sufficient* to explain the low-decay failure. Scope, honestly stated in the manuscript's new "What this does and does not establish" paragraph: this shows sufficiency of bounding sensed-concentration growth, not necessity (some other untested intervention might also work via a different path), and does not itself measure the intermediate KL-term argument directly — it corroborates the account's gross behavioral prediction, which is strong but not the same as instrumenting the KL term. Proven by `tests/colony/test_colony_experiments_extended.py::test_sensed_concentration_cap_rejects_nonpositive_values` (the `__post_init__` guard), `::test_capped_low_decay_wall_clock_stays_within_budget`, and `::test_capping_sensed_concentration_recovers_convergence_at_low_decay` (the falsifiable comparison itself, pinning both the capped `60/60` and uncapped `0/60` counts and asserting the non-overlapping Wilson bounds).
- [x] ISC-110: Anti/methodology: no explicit multiple-comparisons correction (Bonferroni/Holm) is applied across the decay sweep's 6 points or heterogeneity sweep's 4 points, despite eyeballing multiple sweep points being exactly the selective-inference exposure such corrections guard against. Justified rather than silently omitted: `manuscript/05_results_discussion.md`'s "Honesty hedges" section gains a paragraph stating why — (1) each sweep's primary whole-sequence claim rests on one pre-specified omnibus test (`cochran_armitage_trend_test`, one per sweep — decay $Z=+14.57$, heterogeneity $Z=-12.76$, both $p<10^{-10}$), not on scanning many pairwise comparisons for the smallest $p$-value, so there is no multiplicity to correct for on the primary claim; (2) the one genuinely exploratory pairwise comparison this manuscript does interpret (the decay $\{0.60,0.80\}$-vs-$1.00$ dip, `fisher_exact_test_two_sided`) is already disclosed as non-significant ($p=0.1187$) and is explicitly not used to support a positive claim on its own. Verified by grepping the entire manuscript for every `Fisher`/`p =`/`p<`/`significan*` occurrence to confirm no instance of scanning multiple pairwise comparisons and reporting only the smallest as significant actually exists in the current text — the pattern a correction would need to guard against was checked for and not found, rather than assumed absent.

**Round 10 expansion (closes half of the extreme positive control's decay/noise confound) — added 2026-07-09**
- [x] ISC-111: `test_colony_convergence_statistics.py::test_positive_control_that_can_fail_wilson_upper_bound_well_below_0_5` (`decay=0.97`, `sensing_noise_std=4.0`, `n=50`, `seed_base=0`) was already disclosed (ISC-91, item 3) as unable, alone, to attribute its near-total convergence collapse to loss of the stigmergic mechanism specifically versus noise magnitude alone, because it changes `decay` and `sensing_noise_std` simultaneously relative to the calibrated baseline. ISC-108's `deposit_amount=0` ablation closed a *different* confound (Experiment B's real-vs-null attribution gap) at the *calibrated baseline* configuration only — it left this extreme configuration's `deposit_amount=0` condition untested, an open gap ISC-91 named explicitly. This round builds the missing comparison directly at the extreme configuration itself: identical `n=50`/`seed_base=0`/`decay=0.97`/`sensing_noise_std=4.0` as the sibling positive-control test, with only `deposit_amount` varied (`1.0` for the existing full mechanism versus `0.0` for the new zero-deposit condition). **Real result, reported honestly regardless of which way it went:** full mechanism scores `9/50` (rate `0.1800`, Wilson `(0.0977, 0.3080)`) versus zero-deposit's `0/50` (Wilson `(0.0000, 0.0713)`) — a two-sided Fisher's exact test gives `p=0.002634204400259045`, significant at `alpha=0.05`, and the full mechanism's Wilson lower bound narrowly clears the zero-deposit condition's Wilson upper bound. The pre-registered $H_0$ ("the zero-deposit condition converges no differently than the full-mechanism positive control at the same extreme configuration") is **rejected**: even at this deliberately-defeated configuration, the pheromone/stigmergic channel still contributes something real and statistically distinguishable from having no pheromone channel at all — the near-total collapse the positive control demonstrates is not the *whole* story of "noise/decay alone, pheromone irrelevant." Scope, honestly stated: this is a real but modest finding (`18%` remains far below the `>0.8` gate, and the positive control's own point — Wilson upper bound well below `0.5` — stands entirely unchanged); it does **not** decompose how much of the original collapse is attributable to `decay=0.97` alone versus `sensing_noise_std=4.0` alone (that would need a dedicated sweep varying each independently at this extreme, still untested), so the pre-existing "noise-magnitude axis in isolation remains untested" gap is unaffected and still named as future work. Proven by `tests/colony/test_colony_experiments_extended.py::test_extreme_zero_deposit_config_constructs_without_error` (the `__post_init__` non-guard assumption at this extreme config), `::test_extreme_full_mechanism_reproduces_the_sibling_positive_control_wilson_upper_below_half` (confirms this module's independent re-computation lands on the sibling test's own known gate), and `::test_extreme_zero_deposit_pheromone_channel_still_contributes_under_severe_decay_and_noise` (the falsifiable comparison itself, pinning both exact `successes` counts and the real Fisher `p`-value).

**Round 12 expansion (gates the sensed_concentration_cap dose-response curve with a real sweep) — added 2026-07-09**
- [x] ISC-112: ISC-109's cap justification paragraph named an *informal, ungated probe* as the only evidence for the dose-response shape across cap values ("the effect holds essentially unchanged for any cap in `[12.5, 14.0]` and fades out by `cap=20.0` ... not gated, not quoted as a result"). This round promotes that probe into a real, pre-registered, gated sweep. First confirmed directly (by reading `colony/sweep.py`) that `run_parameter_sweep` is genuinely generic over any `ColonyTrialConfig` field except `seed` (`_SWEEPABLE_FIELD_NAMES` is built from the dataclass's own `fields()`), so `sensed_concentration_cap` required zero new sweep machinery — a direct call sufficed. Ran `run_parameter_sweep` with `param_name="sensed_concentration_cap"`, seven real values (`12.5, 13.0, 15.0, 16.0, 17.0, 18.0, 20.0`), fixed `decay=0.10` (Experiment (h)'s own configuration), `n=60` per value, identical `seed_base=0`, identical calibrated baseline otherwise. **Real result, independently reproduced twice, bit-for-bit identical, reported honestly regardless of shape:** successes `60/60, 60/60, 41/60, 13/60, 3/60, 1/60, 0/60` at caps `12.5, 13.0, 15.0, 16.0, 17.0, 18.0, 20.0` respectively — a Cochran–Armitage trend test over the full sweep gives `Z=-16.42452071373818`, `p<10^-10`; a two-sided Fisher's exact test on the plateau (`13.0`) versus the floor (`20.0`) gives `p=2.070073888186964e-35` (the closed-form value for this perfectly separated `60/60`-vs-`0/60` table, `2/C(120,60)` — see the Round 14 Changelog entry below for a real bug this exact comparison caught in `fisher_exact_test_two_sided` and its fix). The pre-registered $H_0$ ("convergence rate at decay=0.10 does not vary as sensed_concentration_cap increases from near the preference-range ceiling toward a value that never binds within the tick horizon") is **rejected**: the rate declines monotonically (non-increasing at every step, no reversal). **But the real shape is honestly reported as NOT the smooth, gradual fade the informal probe's own phrasing might suggest to a reader:** it is a plateau (100% at cap in `{12.5, 13.0}`) followed by a steep decline that is essentially complete by `cap=18.0` (98%+ of the total plateau-to-floor decline) — a full `2.0` cap units *before* the informally-named `cap=20.0` "fade out" point, which merely reconfirms a floor already reached two units earlier and reproduces the independently-computed uncapped `decay=0.10` baseline's `0/60` exactly (cross-checked directly against `decay_sweep_points`, not merely assumed). Also cross-checked: this sweep's `cap=13.0` point (swept over `sensed_concentration_cap` at fixed `decay=0.10`) and ISC-109's `capped_low_decay_points` fixture's `decay=0.10` point (swept over `decay` at fixed `sensed_concentration_cap=13.0`) describe the identical underlying configuration and reproduce the identical `60/60` exactly, not merely approximately — a live agreement check between the two sweep axes, not an assumed one. Scope, honestly stated: this dose-response sweep covers only `decay=0.10` (Experiment (h)'s tested low-decay point), not the full decay range, so it does not characterize how the dose-response curve itself shifts as decay varies — that remains untested and is named as future work in the manuscript. Proven by eight tests in `tests/colony/test_colony_experiments_extended.py` (`test_cap_dose_response_*`): the wall-clock budget guard, the exact-counts regression guard, the monotonicity falsification test, the shape-honesty test (pinning that the decline completes by `cap=18.0`, not gradually out to `cap=20.0`), the two cross-check tests against `capped_low_decay_points` and `decay_sweep_points`, the Cochran–Armitage trend test, and the Fisher plateau-vs-floor test.

**Round 13 expansion (crosses Experiment B's null-model harness with Experiment C's heterogeneity sweep at its most extreme point — never previously composed) — added 2026-07-09**
- [x] ISC-113: Experiment C (ISC-89) establishes only that convergence rate *decreases monotonically* as `preference_mean_range` widens — a claim about shape. It never asks whether the real mechanism's advantage over Experiment B's (ISC-88) random-choice null model, established only at the single calibrated `medium` `(8,12)` baseline, survives at the sweep's most extreme condition, `very_wide` `(2,18)`. Grep-confirmed directly: no test or manuscript passage anywhere in this project had, before this round, ever instantiated `NullModelTrialConfig`/`run_null_model_trial` against any `preference_mean_range` other than Experiment B's own calibrated baseline. `NullModelTrialConfig` structurally has no `preference_mean_range` field at all (`colony/nullmodel.py`'s own docstring), so its rate is a pure function of `num_agents`/`locations`/`num_ticks` and the RNG seed — but it *is* a function of seed, so this round matches each real-mechanism seed base to a freshly-computed null-model run at the identical seed base (the same "identical seed block" apples-to-apples principle ISC-108's zero-deposit ablation already established), rather than reusing one seed base's null figure as a stand-in for another's. **Real result, reported honestly regardless of which way it went, and reported at BOTH pre-existing gated seed bases because they disagree:** at `seed_base=0`, `very_wide`'s `2/60` (Wilson `(0.0092, 0.1136)`) versus a freshly-confirmed null baseline of `1/150` (Wilson `(0.0012, 0.0368)`, the same number ISC-88 already pins) gives a two-sided Fisher's exact `p=0.19698722330301277` — **not** significant; the pre-registered $H_0$ ("very_wide's rate is no higher than the matching null baseline") **survives** at this seed base. At the disjoint `seed_base=7000` (ISC-103's own replication seed block), `very_wide`'s `5/60` (Wilson `(0.0361, 0.1807)`) versus a *freshly computed* `seed_base=7000` null-model run — `0/150` (Wilson `(0.0000, 0.0250)`), independently reproduced, not reused from `seed_base=0` — gives `p=0.0016835563479717132`, clearing `alpha=0.05`; $H_0$ **is rejected** at this seed base. The two seed bases give opposite answers to the identical question at the identical nominal condition — a genuine, previously-uncomputed, seed-base-dependent disagreement, reported as such rather than resolved into one preferred verdict. A scoping check confirms the ambiguity is specific to the sweep's single most extreme point: the next-widest condition, `wide` `(5,15)`, clears its matching null-model baseline overwhelmingly at both seed bases (`15/60` vs `1/150`, `p=2.14e-8` at `seed_base=0`; `14/60` vs `0/150`, `p=7.26e-9` at `seed_base=7000`). Scope, honestly stated: this does not contradict Experiment C's monotonic-decrease shape claim (unaffected, still holds at both seed bases); it adds a different, previously-unasked question whose answer, at only two seed bases tested, genuinely depends on which seed base is examined — more seed bases, not a preference for one of the two already computed, would be needed to characterize which result is typical, and that is named as future work, not silently resolved. Proven by `tests/colony/test_colony_experiments_extended.py::test_very_wide_at_seed0_does_not_clear_the_null_model_baseline` (pins `2/60` vs `1/150`, `p=0.19698722330301277`, H0 survives), `::test_very_wide_at_seed7000_does_clear_the_null_model_baseline` (pins `5/60` vs a freshly-computed `0/150`, `p=0.0016835563479717132`, H0 rejected), `::test_null_model_seed7000_reproduces_a_near_zero_rate_like_seed0` (pins the fresh `seed_base=7000` null-model count itself, `0/150`), and `::test_wide_condition_clears_the_null_model_baseline_at_both_seed_bases` (the scoping check, `p<1e-7` at both seed bases).

**Round 14 expansion (cross-vendor audit finds and fixes a real 24-orders-of-magnitude Fisher-exact-test bug, breaking two consecutive clean audit rounds) — added 2026-07-09**
- [x] ISC-114 (Anti, regression-prevention): `colony/stats.py::fisher_exact_test_two_sided`'s two-sided tail must not silently degenerate to the wrong tolerance regime on a highly-separated 2x2 table. A Forge cross-vendor audit of Round 8 (ISC-112's plateau-vs-floor comparison, the perfectly separated `60/60`-vs-`0/60` table) independently recomputed the function's own output three ways (the project function itself, a hand-coded hypergeometric sum, and `scipy.stats.fisher_exact`) and found the project function returned `4.308978969823708e-11` when the true two-sided p-value is `2.070073888186964e-35` — wrong by 24 orders of magnitude. Root cause: the two-sided tail's tolerance was additive (`observed_p + 1e-10`), which is a safe float-equality guard only when `observed_p` is itself around `1e-10` or larger; for this table, `observed_p ≈ 1.035e-35`, so the additive `1e-10` term completely swallowed it, degenerating the inclusion test to "every table with probability at most `1e-10`" (summing a large, wrong swath of the tail) instead of "every table at least as extreme as the observed one." None of the five previously-pinned Fisher-exact test cases (decay-dip `p=0.1187`, ISC-111 extreme-config `p=0.002634`, ISC-113's two seed-base comparisons `p=0.19699`/`p=0.00168`) exercised this failure mode, because all five had `observed_p` comfortably above `1e-10` — the bug was latent and undetected across two full audit rounds (6 and 7, both clean PASS verdicts) plus this round's own fresh-eyes ideation, verify-select, and primary-session independent re-verification, none of which happened to cross-check against an independent oracle (`scipy`) for exactly this reason. Fixed by switching to a relative tolerance (`observed_p * (1.0 + 1e-9)`), independently verified to reproduce all five previously-correct pinned values bit-identically while correcting the sixth (the perfectly-separated table) to the true `2.070073888186964e-35`. The scientific conclusion at the affected call site is unchanged (both the wrong and the correct value are overwhelmingly significant), but a function this manuscript explicitly advertises as "a real hypergeometric computation... not an approximation" owes exactness at every magnitude it is asked to compute, not only the ones its existing test suite happened to probe. Proven by a new dedicated regression test, `tests/colony/test_colony_stats_unit.py::test_fisher_exact_perfectly_separated_table_matches_hand_computed_value`, which pins the corrected value against an independent hand computation (`2/C(120,60)`, not the function's own internal route) specifically for a highly-separated table, so a regression back to an additive tolerance fails this dedicated unit test immediately rather than only surfacing as a silently wrong number several call-sites away in a manuscript quote — precisely how this bug survived undetected for two rounds.

**Round 15 expansion (verifier closure, figure-quality oracle, and live method-surface synchronization) — added 2026-07-10**
- [x] ISC-115: The project owns a machine-readable `data/claim_ledger.yaml` for every quantitative token in the strict manuscript result surfaces, with each row naming the claim, value, generating test or source path, and artifact path. `tests/test_project_surface_sync.py` rejects a ledger row whose source path or claim identifier is not present, and Stage 04 validation reports `Evidence registry: true` after the ledger is generated. This closes the false-certification path where the rendered PDF was valid but `output/reports/validation_report.json` recorded `Evidence registry: false` while Stage 04 still exited zero.
- [x] ISC-116: The two publication figure writers fail closed on render errors and validate the produced PNG as a real, non-blank artifact (readable PNG, non-trivial dimensions, and more than a single rendered color), while the analysis orchestrator requires both manuscript-referenced figures and a complete registry. Negative controls exercise a broken output destination and invalid/blank-artifact conditions; a missing or failed figure cannot silently become a successful analysis run.
- [x] ISC-117: `experiment_plan.yaml`, project README/guides, manuscript contribution wording, and test-suite documentation agree with the live method surface: the two registered convergence figures, eight analyses across three experiment families, nine mypy fixtures, and the current 277-test/96.03%-coverage run. `tests/test_project_surface_sync.py` fails on stale counts or missing expected figure IDs.

**Round 16 expansion (publication-asset fail-closed boundary and fork-command synchronization) — added 2026-07-10**
- [ ] ISC-118: The committed cover-art publication path must fail closed when generation cannot produce an asset, while successful output must be a readable, non-trivial, non-blank PNG. `require_cover_art` must raise on an unwritable destination, the regeneration script must use that required wrapper, and cover-art tests must validate format, dimensions, and rendered-color diversity.
- [ ] ISC-119: The README, standalone fork guide, and project skill must document the same `MYPYPATH`-qualified authoritative mypy command and current 26-source-file oracle result. A project synchronization test must fail if any surface reintroduces the unsafe bare command or stale source-file count.

## Test Strategy

| ISC | Type | Check | Threshold | Tool |
|---|---|---|---|---|
| ISC-1..7 | static+unit | mypy --strict + pytest on `types/` | 0 mypy errors, unit assertions pass | `mypy`, `pytest` |
| ISC-2,4,15,18,19,31,37-40 | mypy-oracle | subprocess `mypy --strict` against fixture files | non-zero exit + matching error substring on bad fixtures; zero exit on real `src/` | `subprocess`, `mypy` |
| ISC-8..15 | integration | real SQLite file via `tmp_path`, `SELECT` verification | rollback/commit states match expectation | `pytest`, `sqlite3` |
| ISC-115 | provenance/validation | claim-ledger source-path resolution + Stage 04 evidence registry | 101 unique ledger rows resolve to live sources; `Evidence registry: true` | `pytest`, Stage 04 |
| ISC-116 | artifact negative control | figure PNG quality checks + broken destination test + required registry | two required figures are readable, non-blank PNGs; broken output fails | `pytest`, analysis script |
| ISC-117 | synchronization | experiment-plan, manuscript-label, fixture-roster, and documentation consistency checks | no stale method counts; both figure IDs and current test/coverage surface present | `pytest` |
| ISC-118 | publication-asset boundary | required cover-art wrapper + blocked-destination negative control + PNG quality assertions | generation failure raises; successful asset is readable, non-blank PNG with non-trivial dimensions | `pytest`, script |
| ISC-119 | documentation synchronization | authoritative-command/source-count contract test | README, standalone guide, and skill agree on `MYPYPATH` command and 26-file oracle | `pytest` |
| ISC-16..26 | integration+fault | in-process bus with seeded fault injection | protocol violation produces typed `Result.Err`, never crash | `pytest` |
| ISC-27..31 | unit+numeric | hand-computed expected-free-energy comparison | matches within float tolerance | `pytest` |
| ISC-32..34 | multi-agent integration | N≥3 real agents through colony tick loop | aggregate property observed from real per-agent state | `pytest` |
| ISC-35..36 | decision record | `## Decisions` entry + `TODO.md`/`README.md` pointer, or wired command output | decision present; if shipped, command exit captured | `Read`, `Bash` |
| ISC-41..49 | manuscript | render pipeline + validation CLI + page-raster screenshot | zero `??`, validator pass, raster shows correct math | `infrastructure.validation.cli`, `Interceptor`/render screenshot |
| ISC-50..64 | repo integration | direct command execution per Constraints section | each command's real exit code + output | `Bash`, `git`, `uv run` |
| ISC-65..68 | anti-criteria | negative-control probes described above | probe fires as designed | `pytest`, `grep`, `mypy` |
| ISC-80 | unit | `ColonyTrialConfig.__post_init__` runtime-guard rejection + valid-range acceptance | `ValueError` on out-of-range `decay`/`sensing_noise_std`/`preference_variance`; full valid range still constructs | `pytest` |
| ISC-85 | unit + structural | `run_null_model_trial` determinism (same-seed replay) + source-text/AST grep for pheromone-field/agent-machinery references | identical `choice_history` across replays; zero forbidden symbol references | `pytest`, `ast`, source-text read |
| ISC-86 | unit | `run_parameter_sweep` validation (bad `param_name`/`seed`/empty `values`/`n_per_value<1`) + hand-derived Wilson bounds on a deterministic config | `ValueError` on each invalid input; sweep-point Wilson bounds match `wilson_score_interval` called directly | `pytest` |
| ISC-87..89 | real experiment + regression guard | `run_parameter_sweep`/`run_colony_trial`/`run_null_model_trial` at calibrated real configurations, `n>=60` per point/`N=150` for the null-model comparison | exact `successes` counts pinned per experiment (fully deterministic given seed); falsifiable shape assertions (non-monotonicity, non-overlapping Wilson intervals, strict monotonic ordering) | `pytest` |
| ISC-90 | manuscript | `infrastructure.validation.cli markdown` + page-scale raster read of the new section | validator clean; hypothesis-before-result discipline and hedges present and legible | `infrastructure.validation.cli`, `Read`/raster |
| ISC-93 | documentation | full-tree `find`/`ls` enumeration of every real directory, checked for both `README.md` and `AGENTS.md` | zero directories missing either file | `Bash`, `find` |
| ISC-94 | documentation | `infrastructure.validation.cli markdown` on `docs/` + spot-check of cited file:line references against real source | validator clean; cited line numbers/function names match real code | `infrastructure.validation.cli`, `Read`, `Grep` |
| ISC-95 | integration | `uv run pytest tests -n 4 --no-cov -q` | all current project tests pass under xdist; the measured serial gate is 277 passed | `pytest`, `pytest-xdist` |
| ISC-96 | anti-criteria | `find src/template_formal -mindepth 1 -type d -iname 'formal'` | returns nothing (no nested dead `formal/` stub under `src/`) | `find` |
| ISC-97 | manuscript | page-scale raster read of the rendered title page | correct title/subtitle text and cover image both present | `Read`/raster |
| ISC-98 | unit + manuscript | `pytest tests/colony/test_cover_art.py` + page-scale raster read of the title page | 5/5 tests pass (real PNG, valid magic bytes, same-seed byte-identical, different-seed differs, parent-dir creation); cover art visible and legible in the rendered PDF | `pytest`, `Read`/raster |
| ISC-99 | manuscript | page-scale raster read of the Definition-5 (free-energy) and Theorem-10 pages + `pdftotext` scan for the full `Definition 1`..`Theorem 10` sequence | all ten formalisms numbered correctly in one continuous sequence; equation cross-reference resolves to `eq. 1`, not a raw label | `pdftotext`, `Read`/raster |
| ISC-100 | anti-criteria | page-scale raster read of the one math-bearing formalism block (Definition 5) | real typeset Gaussian notation, not ASCII-mangled text (`\alpha`-style leak) | `Read`/raster |
| ISC-101 | regression | `pytest tests/infra_tests/rendering/test_slides_renderer_core.py` + a real beamer-slides render of `template_formal`'s manuscript | all slides-renderer tests pass; `scripts/pipeline/stage_03_render.py --project templates/template_formal` produces all 7 beamer slide decks with zero LaTeX compilation errors | `pytest`, `Bash` |
| ISC-102 | unit + real experiment | `pytest tests/colony/test_colony_stats_unit.py -k cochran_armitage` + the gated decay/heterogeneity sweep tests | hand-derived reference values match; both real-sweep Z/p values match independent hand-computation | `pytest` |
| ISC-103 | real experiment + regression guard | `pytest tests/colony/test_colony_experiments_extended.py -k seed_base_7000` (or equivalent disjoint-seed replication test) | strict monotonic-decrease ordering reproduces at a seed block sharing zero seeds with `seed_base=0` | `pytest` |
| ISC-104 | anti-criteria | `pytest tests/colony/test_colony_experiments_extended.py -k decay_dip_fisher` | Fisher p-value computed from the live `decay_sweep_points` fixture equals the manuscript-quoted value | `pytest` |
| ISC-105 | formal negative control | direct `java -jar tla2tools.jar -config AntProtocolFaultyNegControl.cfg AntProtocolFaultyNegControl.tla` + `scripts/check_formal_specs.sh` | TLC reports `Invariant NoFalseEstablishment is violated` in a 3-state trace; the wrapping script's inverted pass/fail logic exits 0 | `java`/TLC, `Bash` |
| ISC-106 | anti-criteria (process) | re-read of the workflow's own journal + independent verification that the surviving build track's changes are real and correct | no silently-corrupted file from a collision; the standing-down track's independent math verification matches the landed track's real test output | `Read`, `pytest` |
| ISC-107 | real experiment + regression guard | `pytest tests/colony/test_colony_experiments_extended.py -k seed1000` (decay-sweep replication) | threshold-then-plateau-then-decline shape reproduces at a seed block sharing zero seeds with `seed_base=0`; exact counts pinned, not assumed to match `seed_base=0`'s magnitude | `pytest` |
| ISC-108 | real experiment (ablation) + regression guard | `pytest tests/colony/test_colony_experiments_extended.py -k zero_deposit` | `deposit_amount=0.0` constructs without error; zero-deposit successes pinned at `0/150`; zero-deposit Wilson lower bound does NOT exceed null model's Wilson upper bound (H0 survives); full-mechanism Wilson lower bound DOES exceed zero-deposit Wilson upper bound | `pytest` |
| ISC-109 | real experiment (ablation) + regression guard + `__post_init__` guard test | `pytest tests/colony/test_colony_experiments_extended.py -k "capped or sensed_concentration"`; `mypy --strict` on `src/` | new `ColonyTrialConfig.sensed_concentration_cap: float | None = None` field rejects non-positive values when set, defaults to `None` (behavior-preserving, confirmed by the full pre-existing decay-sweep suite reproducing identical pinned counts); capped (`13.0`) successes pinned at `60/60` at decay `{0.10, 0.30}` versus the uncapped `decay_sweep_points` fixture's `0/60` at both; capped Wilson lower bound DOES exceed uncapped Wilson upper bound at both decay values (H0 rejected, the mechanistic account is corroborated) | `pytest`, `mypy` |
| ISC-110 | manuscript + methodology grep | `infrastructure.validation.cli markdown` on `manuscript/`; grep the manuscript for every `Fisher`/`p =`/`p<`/`significan*` occurrence | validator clean; the selective-inference pattern a multiple-comparisons correction would guard against (scanning many pairwise comparisons, reporting only the smallest p-value as significant) does not occur in the current text | `infrastructure.validation.cli`, `Grep` |
| ISC-111 | real experiment (ablation) + regression guard | `pytest tests/colony/test_colony_experiments_extended.py -k extreme` | `deposit_amount=0.0` constructs at `decay=0.97`/`sensing_noise_std=4.0`; full-mechanism successes pinned at `9/50`, zero-deposit at `0/50`; Fisher two-sided `p` matches `0.002634204400259045`; full mechanism's Wilson lower bound exceeds zero-deposit's Wilson upper bound (H0 rejected) | `pytest` |
| ISC-112 | real experiment (gated dose-response sweep) + regression guard + cross-checks | `pytest tests/colony/test_colony_experiments_extended.py -k cap_dose_response` | seven pinned `successes` counts (`60,60,41,13,3,1,0` at cap `12.5..20.0`) at `decay=0.10`; rate is monotonically non-increasing (no reversal, H0 rejected); decline is >=95% complete by `cap=18.0` (shape-honesty check, not a smooth fade to `cap=20.0`); `cap=13.0` point matches `capped_low_decay_points`'s `decay=0.10` point exactly; `cap=20.0` point matches `decay_sweep_points`'s uncapped `decay=0.10` point exactly; Cochran–Armitage `Z=-16.4245`, `p<1e-10`; Fisher plateau-vs-floor `p=2.07e-35` (corrected Round 14 — see ISC-114; was wrongly `4.31e-11` under the pre-fix additive-tolerance bug) | `pytest` |
| ISC-113 | real experiment (cross-instrument comparison, both gated seed bases) + regression guard | `pytest tests/colony/test_colony_experiments_extended.py -k "very_wide_at_seed or wide_condition_clears"` | `seed_base=0`: `very_wide` `2/60` vs null `1/150`, Fisher `p=0.19698722330301277` (H0 survives, not significant); `seed_base=7000`: `very_wide` `5/60` vs a freshly-computed (not reused) null baseline `0/150`, Fisher `p=0.0016835563479717132` (H0 rejected, significant); scoping check: `wide` clears its matching null baseline at both seed bases, `p<1e-7` each | `pytest` |
| ISC-114 | anti-criteria / regression guard (stats-function correctness) | `pytest tests/colony/test_colony_stats_unit.py -k fisher_exact_perfectly_separated` | `fisher_exact_test_two_sided(60, 60, 0, 60)` equals the independently hand-computed `2/C(120,60) = 2.070073888186964e-35` within relative tolerance, and all five previously-pinned Fisher-exact test cases remain bit-identical after the fix | `pytest` |

## Features

| name | description | satisfies | depends_on | parallelizable |
|---|---|---|---|---|
| types-core | `Result`, `NewType` IDs, `Phase` markers | ISC-1..7 | — | yes |
| storage-layer | schema-as-functor, typed queries, affine transaction handles | ISC-8..15 | types-core | yes |
| protocol-network | session-type `Protocol` state machine + fault-injectable bus | ISC-16..26 | types-core | yes |
| agent-loop | `Agent[StateT]`, free-energy decision loop | ISC-27..31 | storage-layer, protocol-network | no |
| colony-coordination | pheromone substrate, multi-agent integration test | ISC-32..34 | agent-loop | no |
| formal-side-spec | Lean/TLA+ decision + optional wiring | ISC-35..36 | protocol-network | yes |
| mypy-oracle-fixtures | negative-control fixture suite | ISC-37..40 | types-core, storage-layer, protocol-network, agent-loop | yes |
| manuscript | preamble, bib, claims-scoping section, render+validate | ISC-41..49 | agent-loop, colony-coordination, mypy-oracle-fixtures | no |
| repo-forkable-surface | README/AGENTS/TODO/pyproject/.gitignore/.agents skill | ISC-50..52 | — | yes |
| registration | public_scope.py + regenerated docs + drift checks | ISC-55..64 | repo-forkable-surface, manuscript | no |
| anti-regression-guards | cross-cutting negative controls | ISC-65..68 | all above | no |

## Decisions

- 2026-07-08T00:00:00Z: **Soft ISC-floor relaxation (show-your-math).** E5's soft floor is ≥256 ISCs; this ISA carries 68 (progress denominator 98 counting sub-items/anti-criteria distinctly in the running count above — see note). The task's true testable surface is one mid-size research-software template, not a multi-service enterprise application; forcing padding to 256 would create fluff ISCs indistinguishable-on-failure from each other, violating the granularity/hard-to-vary rule itself. 68 atomic, independently falsifiable criteria fully cover the feature set identified by RedTeam + FirstPrinciples passes. Relaxation logged per Algorithm doctrine (soft floor, overridable with justification).
- 2026-07-08T00:00:00Z: **In-process simulated DB/network, not real distributed deployment.** Per FirstPrinciples Reconstruct: the manuscript's claim is about static typing preventing illegal states, not about distributed-systems deployment; real sockets/Postgres would be scope creep against the actual claim (see `## Out of Scope`).
- 2026-07-08T00:00:00Z: **Formal side-spec (Lean/TLA+) is conditional, decided during BUILD, not pre-committed** — RedTeam flagged vestigial-decoration risk (memory: `pai-smartapprover-orphaned-by-design` failure shape); ISC-35/36 force an explicit ship-or-cut decision with evidence either way.
- 2026-07-08T00:00:00Z: **Working-tree baseline exception (R10).** OBSERVE found 8 uncommitted/unrecognized changes unrelated to this task (`infrastructure/rendering/{mermaid_figure,pptx_deck,slide_deck}.py` + tests + `pyproject.toml`/`uv.lock`, apparently in-flight slide-deck work). This build creates only new files under `projects/templates/template_formal/` plus a scoped, additive edit to `infrastructure/project/public_scope.py` and the generated docs; it shares no files with the foreign in-flight work, so the full repo-wide coverage-gate re-baseline is not required to safely proceed — the new template's own 90% gate is the relevant baseline and is run standalone.
- 2026-07-08T00:00:00Z: **Affine handles are runtime-guarded discipline, not compiler-enforced linearity** — per RedTeam finding #6, `TransactionHandle`/protocol-phase objects must actually raise on reuse (ISC-12/13), not merely be typed; manuscript language is pinned to this distinction (ISC-44).
- 2026-07-08T23:00:00Z: **User interview answer "Yes for all" on the formal side-spec question resolved as: ship BOTH Lean 4 and TLA+, each wired to a real runnable check** (not just one, not cut) — the AskUserQuestion options were single-select and mutually-exclusive-by-design, but the free-text answer overrode that framing toward maximal inclusion; both were built and both produced genuine passing runs (Lean: `lake build` exit 0, zero `sorry`, zero extra axioms; TLA+: `tlc` exit 0, "No error has been found", 6 states generated) — see Verification.
- 2026-07-09T03:00:00Z: **Round-3 numeric-config validation (why `ColonyTrialConfig` gets `__post_init__` guards now).** A third adversarial pass (security-deep, redteam-round2-code, first-principles-deepen) found `ColonyTrialConfig`'s `sensing_noise_std`, `decay`, and `preference_variance` fields were plain `float`s with zero runtime validation, despite `decay` being documented as "fraction evaporated ... at the end of each tick" (so `[0.0, 1.0]` is the only sensible domain) and `sensing_noise_std`/`preference_variance` feeding `random.gauss` (so negative values are physically meaningless). This is the identical *class* of gap ISC-70 (SQL-identifier validation in `storage/schema.py`) and ISC-15 (isolation-level `Literal` in `storage/transaction.py`) already closed for other config surfaces — an unvalidated numeric field silently accepting a nonsensical value (e.g. `decay=5.0`, which `PheromoneField.evaporate` would only catch itself, deep inside the tick loop, far from the actual misconfiguration site) is exactly the failure mode `__post_init__` guards exist to prevent everywhere else in this template. Fixed by adding the same `__post_init__` pattern to `ColonyTrialConfig`, with `tests/colony/test_colony_experiment_config.py` proving both directions: `decay=-0.1`/`decay=1.5`/`sensing_noise_std=-1.0` all raise `ValueError` at construction, and the full valid boundary/interior range still constructs. `wilson_score_interval`/`consensus_tick_summary`/`pearson_r` were independently probed against `n=0`, `n=1` (all-success and all-failure), a single data point, and identical-x-values `pearson_r` inputs — every case already returns a controlled `ValueError` or a well-defined `Ok`/`Err` result, none crashes with a bare unhandled exception, so no source fix was needed there; two new tests (`test_wilson_score_interval_n_equals_one_all_success_is_a_controlled_result`, `..._all_failure_...`) were added as permanent regression guards for the previously-untested `n=1` edge case. A tree-wide grep for `curl`/`wget`/`urllib`/`requests`/`urlopen` found only the already-checksummed `tla2tools.jar` fetch in `scripts/check_formal_specs.sh` (ISC-69) — no other unverified external download exists. `scripts/02_run_analysis.py`'s N=40 statistics sweep was re-run three fresh times (`rm -rf output` between runs): all three produced the identical `successes=37/40` (fully deterministic — fixed `seed_base=9000` and fixed config, no unseeded randomness), 37 converged trials comfortably clearing `MIN_CONVERGED_FOR_HISTOGRAM=5` with wide margin, so the histogram figure is robust as configured; no `N`/config change was needed.
- 2026-07-08T23:10:00Z: **Registration-tax scope decision.** Functionally-gated registration sites (public_scope.py, docs/_generated/{active_projects,exemplar_roster,COUNTS}.md, check_template_drift.py --strict, check_tracked_all.py) are all satisfied and independently re-verified by the primary session (not just the delegated agent's self-report). Root `.gitignore` required a manual fix beyond the delegated agent's scope — see Changelog entry below. Hand-maintained prose rosters not gated by any automated check (`MAINTAINERS.md`, `projects/AGENTS.md`, `projects/README.md`, `projects/templates/AGENTS.md`, `projects/PAI.md`) were deliberately left unedited beyond `CLAUDE.md` and top-level `README.md` (the two most load-bearing/user-facing sites) — this is a scoped, documented relaxation, not an oversight; logged in `TODO.md` for a dedicated future roster-hygiene pass since several of these were already stale (missing `template_pitch_deck`) before this session began.
- 2026-07-09T06:00:00Z: **Round-4 pivot: genuine scientific depth, not more bug-hunting.** Rounds 1-3 were adversarial-review rounds (Forge/security/RedTeam/FirstPrinciples finding and fixing real defects in already-shipped code). Round 4 is deliberately different in kind: three ideation passes (a FirstPrinciples methodological-gap analysis, a RedTeam attack on the manuscript's scientific claims, and a Science hypothesis-generation pass) converged on the same real gap — the N=150 statistical claim in `test_colony_convergence_statistics.py` answers "does the mechanism converge reliably at one configuration" but never asks "is that rate sensitive to `decay`," "does the mechanism actually beat random chance," or "how does heterogeneity's effect behave across its own range" — three genuinely new, falsifiable questions the existing test suite structurally could not answer because the infrastructure to answer them (a null-model baseline, a generic parameter-sweep runner) did not exist yet. `colony/nullmodel.py` and `colony/sweep.py` were built as real, tested, stdlib-only infrastructure (not throwaway scripts) specifically so these questions are answerable more than once, by a future template fork, not just by this session's one-off numbers. All three experiments' parameter values (decay sweep points, heterogeneity widths) were calibrated iteratively against real runs — the decay sweep in particular went through three iterations (a coarse 9-point sweep, a finer 13-point sweep to locate the threshold precisely, then robustness spot-checks at 2 additional seed bases) before the final 6-point regression-guard set was chosen, matching the same calibration discipline `test_colony_convergence_statistics.py`'s own configuration was originally tuned with (not a single guessed set of values).
  **Real numbers obtained** (all fully deterministic given `seed_base=0`, independently reproduced by re-running each experiment twice before being pinned into a test): Experiment A (decay sweep, `n=60`/value): `0.10`→0/60, `0.30`→0/60, `0.46`→56/60, `0.60`→60/60, `0.80`→60/60, `1.00`→56/60 — non-monotonic (threshold + plateau + high-end decline), falsifying the monotonic-non-decreasing null. Experiment B (real-vs-null, `N=150` each): real 140/150 (Wilson `0.8816-0.9634`) vs. null 1/150 (Wilson `0.0012-0.0368`) — non-overlapping intervals, decisively rejecting "no better than chance." Experiment C (heterogeneity-width sweep, `n=60`/condition): tight 60/60, medium 56/60, wide 15/60, very-wide 2/60 — strictly monotonic decrease, the monotonic-decrease null survives.
  **Mechanistic hypothesis for Experiment A's threshold, explicitly hedged as inference not proof:** manual trace inspection at low decay (e.g. `decay=0.10`) shows both locations' sensed concentrations grow together, roughly in lockstep, past the agents' preference range (8-12) within the 30-tick horizon — once both candidates are similarly far from every preference, the KL-term's discriminating signal shrinks toward the same order of magnitude as the sensing noise (`sigma=0.5`), and agents split unpredictably rather than reinforcing one attractor. This account is consistent with the traces examined but was not isolated by a dedicated ablation (e.g. artificially capping sensed concentration) — the manuscript states this distinction explicitly (the *effect* of `decay` is well-supported by the controlled sweep; the *mechanism* is a plausible but not separately-proven account).
  **Robustness spot-checks (not gated by a regression test):** the decay sweep's qualitative shape (threshold near `decay≈0.35-0.50`, plateau at `0.6-0.8`, decline at `1.0`) reproduced at `seed_base=1000` and `seed_base=5000`; the heterogeneity sweep's strict monotonic-decrease ordering reproduced at `seed_base=2000` and `seed_base=7000` (exact `successes` counts varied slightly by seed base, e.g. `very_wide` scored 2/60, 6/60, and 5/60 across the three seed bases tried — always the smallest of the four conditions, never breaking the ordering).
  **criterion now:** ISC-85 through ISC-90 (above). Gate re-verified after all additions: `mypy --strict` → `Success: no issues found in 26 source files`; full project test suite passes with real coverage reported below; `check_template_drift.py --strict` → clean for `template_formal`; manuscript markdown validator → clean. See `## Verification` for the exact quoted command output.

- 2026-07-09T09:00:00Z: **Round-5 pivot: folder-level documentation coverage, not more code/science.** Rounds 1-4 hardened code, formal specs, statistics, and manuscript honesty; this round closed a documentation gap instead — six parallel documentation agents wrote a `README.md`/`AGENTS.md` pair at every real directory under `template_formal` (previously only the project root and a handful of leaf modules had one) plus a new `docs/` guide tree (`architecture.md`, `type_system_guide.md`, `formal_methods_guide.md`, `statistics_methodology_guide.md`, `security_guide.md`, `testing_guide.md`). A dedicated verification pass (this session) then read every newly-created file against the real, current code — not against each other or against ISA.md's own prose — and found and fixed six concrete inaccuracies: (1) the root `README.md` quoted a stale `mypy --strict` file count (22, then re-derived as 25) and a stale test/coverage figure (153 passed/94.83%, superseded by 215 passed/95.54%) left over from an earlier round; (2) the root `AGENTS.md`'s directory-structure Mermaid diagram enumerated only `colony/pheromone.py` under `colony/`, omitting the five other real modules (`experiment.py`, `stats.py`, `nullmodel.py`, `sweep.py`, `demo.py`, `visualization.py`) added across rounds 2-4; (3) `src/template_formal/colony/README.md` said `tests/colony/` has "nine files" when it has ten; (4) `src/template_formal/network/README.md` (faithfully reproducing `bus.py`'s own module docstring) described the fault-roll order as "drop → duplicate → corrupt," but `InProcessBus.send`'s real call sequence is drop → corrupt → duplicate — a pre-existing docstring/implementation drift in the shipped source, not introduced by this round's documentation, corrected in the README with an explicit note (source docstring left as-is, out of this round's scope) since it does not change the reproducibility guarantee; (5) `manuscript/README.md` and `manuscript/AGENTS.md` both claimed `grep -rn '{{' manuscript/*.md` returns nothing, which became self-falsifying the moment those two files (which describe the token convention in prose and legitimately contain the literal string) were added to `manuscript/` — the check was narrowed to the actual content files (`0[0-9]_*.md`, `99_references.md`); (6) `formal/README.md` and `formal/AGENTS.md` both claimed `find src -type d -iname '*formal*'` "returns nothing" as proof a dead `src/template_formal/formal/` stub was removed, but that command also matches `src/template_formal` itself (the package directory's own name contains the substring "formal") and never actually returns nothing — corrected to the precise `find src/template_formal -mindepth 1 -type d -iname 'formal'`, independently re-verified empty.
  **Learned:** a documentation-writing pass that faithfully reproduces an existing source docstring, an ISA claim, or a sibling file's phrasing can still ship a factually wrong "verify it yourself" command if that command was never actually re-run character-for-character against the current tree before being written down — this is the same class of gap this repo's memory already flags for verifier code (`gotcha-workflow-verifier-relative-path-false-reject`, `gotcha-python-replace-silent-nomatch-unverified`): a claimed check is only as good as someone actually running it. All six fixes were found by literally executing every quoted grep/find/wc -l command in the new documentation against the real repository state, not by reading the prose for plausibility.
  **criterion now:** ISC-93 through ISC-96 (above). Full gate re-verified after the documentation-only fixes (no `src/`/`tests/` code touched): `mypy --strict` → `Success: no issues found in 25 source files`; `uv run pytest tests -n 4 --no-cov -q` → `215 passed in 72.89s`; `uv run python scripts/pipeline/stage_01_test.py --project templates/template_formal --project-only` → `215 passed`, `Total coverage: 95.54%`; `uv run python scripts/audit/check_template_drift.py --strict` → `template_drift: no drift detected.`; `uv run python scripts/audit/check_tracked_all.py` → `projects: clean / fonds: clean / tools: clean / rules: clean`; `git add --dry-run projects/templates/template_formal/` → 146 files staged, zero ignored-path/junk matches; `infrastructure.validation.cli markdown` on the project root, every `docs/`/`src/`/`tests/`/`scripts/`/`formal/`/`manuscript/` subdirectory individually, and the project root recursively → `No issues found!` in every invocation.

- 2026-07-09T12:00:00Z: **Round-6 pivot: presentation (title/cover/numbering), then a second methods-deepening round.** The user asked for four things: a better title, cover art via the Art/BeCreative skills, auto-numbered formalisms, and another FirstPrinciples/RedTeam/Science methods round. Cover art hit a real environment constraint (see Changelog) resolved by pivoting to a deterministic procedural generator rather than blocking or silently skipping the request. Title choice: "Illegal States, Mostly Unrepresentable" was chosen over four other internally-generated candidates (a pure type-theory framing, a pure ant-colony/decentralization framing, a literary/evocative framing, and a fully technical/academic framing) because it is the only one that encodes the manuscript's actual differentiator — not just the type-theory achievement but the repeated, Forge-caught discipline of never claiming more than proven — directly into the title itself via the "Mostly" hedge, rather than requiring the subtitle to carry that weight alone.

- 2026-07-09T13:30:00Z: **Round-6 part 2: a Workflow-orchestrated FirstPrinciples/RedTeam/Science methods round, then a primary-session completion pass, not a fire-and-forget delegation.** Three ideation agents ran in parallel and largely converged (all three independently named the same top-priority gap: no ordered-trend statistical test across the decay/heterogeneity sweeps), but RedTeam additionally found two findings the other two ideation passes did not surface — a HIGH-severity, mutation-test-confirmed near-vacuity in the TLA+ `NoFalseEstablishment` invariant, and a MEDIUM-severity disconnection between the manuscript's quoted Fisher p-value and its generating experiment. The three parallel BUILD-phase agents this round's own `pipeline()` call dispatched (see Changelog for the collision) left the TLA+ finding's fix genuinely started but incomplete (`AntProtocolFaultyNegControl.tla` written and correctly reasoned, but no `.cfg`, not wired into `scripts/check_formal_specs.sh`, not independently re-verified, manuscript not updated) and the Fisher-disconnection finding entirely unaddressed. Rather than accept the build agents' self-reports at face value (this session's established discipline throughout all 6 rounds), the primary session independently re-verified every build-agent claim on disk, ran the TLA+ negative control directly via `java -jar tla2tools.jar` BEFORE trusting its own header comment, then completed both unfinished fixes directly — the same "verify, then finish what a delegated agent started but didn't land" pattern as round 4's RedTeam-relay gap, applied here to a within-round build-agent gap rather than a cross-phase relay gap.

- 2026-07-09T15:00:00Z: **Round 7: close the one remaining half of the ISC-103 fix — the decay sweep's own cross-seed replication.** ISC-103 gated the heterogeneity sweep's cross-seed replication (`seed_base=7000`) but left the decay sweep's equivalent claim (`seed_base=1000`/`5000`) as an ungated prose spot-check, an asymmetry the manuscript's own "Honesty hedges" paragraph already named explicitly. This round runs the real, identical six-point decay sweep (`n=60`/value, calibrated baseline) at `seed_base=1000` — chosen over `5000` specifically because it is the exact seed base already published in the manuscript's "Precision correction" paragraph, so the new gated test corroborates a number already in print rather than introducing a fresh one. **Real numbers obtained** (independently reproduced once before pinning): `0.10`→0/60, `0.30`→0/60, `0.46`→58/60, `0.60`→60/60, `0.80`→60/60, `1.00`→53/60 — the identical qualitative shape as `seed_base=0` (near-zero floor, 100% plateau at 0.60/0.80, measurable decline at 1.00), with different exact counts, exactly as expected for a disjoint seed block (shares zero seeds with `seed_base=0`'s `0..59`). The new gated test deliberately does NOT assert the `seed_base=0` run's specific `wilson_lower > 0.8` threshold at decay=1.00, since at `seed_base=1000` the decline is proportionally larger (`53/60=0.8833` vs. `56/60=0.9333`) and that specific numeric threshold does not hold here — only the qualitative shape (floor, plateau, decline-below-plateau) is asserted, per this template's own discipline against forcing a result to match a prior run's exact magnitude.
  **criterion now:** ISC-107 (above). Gate re-verified after the addition: `mypy --strict` → `Success: no issues found in 26 source files`; `tests/colony/test_colony_experiments_extended.py` → all 21 tests pass (18 pre-existing + 3 new); `infrastructure.validation.cli markdown` on `manuscript/` → `No issues found!`. See `## Verification` for the exact quoted command output.

- 2026-07-09T16:00:00Z: **Round 8: close the Experiment B real-vs-null attribution confound with a real zero-deposit ablation.** Full ISC-108 description now lives in `## Criteria` (a structural fix — this entry originally duplicated the Criteria-format bullet here directly; corrected to the established convention of a short narrative pointer, matching how ISC-107's Round 7 entry above is worded). Real result, reported honestly regardless of outcome: zero-deposit real mechanism scores `0/150` (Wilson `(0.0000, 0.0250)`), statistically indistinguishable from the null model's `1/150` — the pre-registered $H_0$ survives, isolating the real mechanism's advantage to the pheromone/stigmergic channel specifically.
  **criterion now:** ISC-108 (above). Gate re-verified after the addition: `mypy --strict` → `Success: no issues found in 26 source files`; `tests/colony/test_colony_experiments_extended.py` → all 25 tests pass (21 pre-existing + 4 new); `infrastructure.validation.cli markdown` on `manuscript/` → `No issues found!`. See `## Verification` for the exact quoted command output.

- 2026-07-09T16:30:00Z: **Round 9: a dedicated ablation of Experiment A's "plausible mechanism" account.** Full ISC-109 description now lives in `## Criteria` (same structural fix as the Round 8 entry above). Real result, reported honestly regardless of outcome: capping sensed concentration at `13.0` recovers convergence at low decay (`60/60` capped vs. `0/60` uncapped at both decay=0.10 and decay=0.30) — $H_0$ rejected, genuinely confirming the mechanistic account's sufficiency (not necessity, and not a direct measurement of the KL-term argument itself).
  **criterion now:** ISC-109 (above). Gate re-verified after the addition: `mypy --strict` → `Success: no issues found in 26 source files`; `tests/colony/test_colony_experiments_extended.py` → all 28 tests pass (25 pre-existing + 3 new); full project gate → `251 passed`, `Total coverage: 95.72%`; `infrastructure.validation.cli markdown` on `manuscript/` → `No issues found!`; `check_template_drift.py --strict` → zero drift for `template_formal` (the one reported error still belongs to the out-of-scope `template_pools_rules_tools`). See `## Verification` for the exact quoted command output.

- 2026-07-09T17:00:00Z: **Round 10: the multiple-comparisons methodology note.** Manuscript-only addition (ISC-110, added directly by the primary session — see `## Criteria`): explains why no Bonferroni/Holm correction is applied across the decay/heterogeneity sweeps' pairwise Wilson-interval observations, since the primary whole-sequence claims rest on one pre-specified omnibus Cochran-Armitage test per sweep and the one genuinely exploratory pairwise comparison is already disclosed non-significant. Verified by grepping the manuscript for every significance-adjacent phrase to confirm the selective-inference pattern a correction would guard against does not actually occur in the current text.
  **criterion now:** ISC-110 (above). `infrastructure.validation.cli markdown` on `manuscript/` → `No issues found!`.

- 2026-07-09T17:15:00Z: **Structural fix — ISC-108/109 Criteria bullets were misplaced into `## Decisions` by their authoring agents, corrected.** Two of the four sequential build agents this round (deposit-zero ablation, concentration-cap ablation) each inserted their new `## Criteria`-format checklist bullet (`- [x] ISC-N: ...` under a `**Round N expansion**` heading) directly into `## Decisions` instead of `## Criteria`, apparently anchoring on the nearest similarly-bulleted text rather than the actual `## Criteria`/`## Test Strategy` section boundary. Caught during the primary session's independent re-verification pass (not by either agent) via a full-file section-heading grep (`grep -n "^## \|^\*\*Round"`) cross-checked against where each ISC's checklist bullet actually lived. Fixed by relocating both bullets to their correct position in `## Criteria` (immediately after ISC-107, before `## Test Strategy`) and replacing the duplicated text left behind in `## Decisions` with short narrative pointers, matching the convention every other round's Decisions entries already use. No content was lost — this was a structural relocation, not a rewrite. **Learned:** a sequential chain of fresh agents editing the same large, section-structured file cannot be assumed to preserve section-boundary discipline just because each one reads the current file state before editing — each agent's own text-search heuristic for "where does new content of this shape go" can independently misfire, so a section-heading-level structural check (not just a content-accuracy check) belongs in every round's independent re-verification pass on a multi-section document like this one, not only spot-checks of the added prose itself.

- 2026-07-09T18:00:00Z: **Round 11: close half of the extreme positive control's decay/noise confound with a real zero-deposit ablation at the extreme configuration itself.** ISC-91 (item 3) disclosed that `test_positive_control_that_can_fail_wilson_upper_bound_well_below_0_5` (`decay=0.97`, `sensing_noise_std=4.0`) cannot, alone, attribute its collapse to loss of the stigmergic mechanism versus noise magnitude alone. ISC-108's zero-deposit ablation closed a different confound (Experiment B's real-vs-null attribution) at the calibrated baseline only, leaving this extreme configuration's `deposit_amount=0` condition an explicitly-named open gap. Full ISC-111 description now lives in `## Criteria`, matching the established convention (short narrative pointer here, not a duplicated checklist bullet). Real result, reported honestly regardless of outcome: full mechanism `9/50` versus zero-deposit `0/50` at the identical extreme configuration, Fisher exact two-sided `p=0.002634204400259045` — significant. The pre-registered $H_0$ ("no difference") is **rejected**: the pheromone channel still contributes something real even under this much decay and noise, though modestly (`18%`, still far below the `>0.8` gate). This does not decompose the decay-alone-versus-noise-alone attribution — that remains named future work, unaffected by this result.
  **criterion now:** ISC-111 (above). Gate re-verified after the addition: `MYPYPATH=src mypy --strict --explicit-package-bases --namespace-packages src` → `Success: no issues found in 26 source files` (no `src/` change was needed for this test-and-manuscript-only addition); `tests/colony/test_colony_experiments_extended.py` → all 32 tests pass (28 pre-existing + 4 new), `32 passed in 248.30s`; `tests/colony/test_colony_convergence_statistics.py` (the sibling positive-control test, unmodified) → `6 passed`; `infrastructure.validation.cli markdown` on `manuscript/` → `No issues found!`; manuscript re-rendered (21 pages) and re-confirmed via page-scale raster read (page 12, the new Hypothesis-7 block, renders with correct typeset math and the auto-numbered sequence remains continuous, `Definition 1`..`Theorem 13`, zero `??` marks). See `## Verification` for the exact quoted command output.

- 2026-07-09T19:00:00Z: **Round 12: gate the sensed_concentration_cap dose-response curve with a real sweep, replacing the informal, ungated probe ISC-109 named.** First confirmed directly (by reading `colony/sweep.py`) that `run_parameter_sweep` is genuinely generic over any `ColonyTrialConfig` field except `seed`, so `sensed_concentration_cap` required zero new sweep machinery — a direct call sufficed. Ran the sweep at `decay=0.10` (Experiment (h)'s own configuration), seven cap values (`12.5, 13.0, 15.0, 16.0, 17.0, 18.0, 20.0`), `n=60` per value, identical `seed_base=0`. Full ISC-112 description now lives in `## Criteria`, matching the established convention (short narrative pointer here, not a duplicated checklist bullet). Real result, independently reproduced twice bit-for-bit identical, reported honestly regardless of shape: the rate declines monotonically from `60/60` to `0/60` across the sweep (H0 rejected — a Cochran-Armitage `Z=-16.4245`, `p<1e-10`) — but the shape is explicitly NOT the smooth, gradual fade the informal probe's own phrasing ("fades out entirely by cap=20.0") could suggest: the decline is essentially complete by `cap=18.0` (98%+ of the total decline), a full `2.0` cap units before the informally-named `cap=20.0` endpoint, which merely reconfirms a floor already reached earlier. Cross-checked directly (not assumed) against two pre-existing fixtures: the `cap=13.0` point matches ISC-109's already-gated single point exactly, and the `cap=20.0` point matches the independently-computed uncapped `decay=0.10` baseline exactly. Scope, honestly stated: this sweep covers only `decay=0.10`, not the full decay range, so it does not characterize how the dose-response curve itself shifts as decay varies — named as future work in the manuscript.
  **criterion now:** ISC-112 (above). Gate re-verified after the addition: `MYPYPATH=src uv run mypy --strict --explicit-package-bases --namespace-packages src` → `Success: no issues found in 26 source files` (no `src/` change was needed for this test-and-manuscript-only addition); `tests/colony/test_colony_experiments_extended.py -k cap_dose_response` → `8 passed`; full `tests/colony/test_colony_experiments_extended.py tests/colony/test_colony_convergence_statistics.py` → `46 passed` (32 pre-existing + 8 new in the extended suite, plus 6 unmodified convergence-statistics tests); full project gate → `263 passed`, `Total coverage: 95.7%`; `infrastructure.validation.cli markdown` on `manuscript/` → `No issues found!`; manuscript re-rendered (22 pages) and re-confirmed via page-scale raster read (pages 15-16, the new Hypothesis-10 block, renders with correct typeset math, table, and the auto-numbered sequence remains continuous, `Definition 1`..`Theorem 14`, zero `??` marks). See `## Verification` for the exact quoted command output.

- 2026-07-09T20:00:00Z: **Round 13: cross Experiment B's null-model harness with Experiment C's heterogeneity sweep at its most extreme point — never previously composed.** An independent verify-select pass identified that no test or manuscript passage anywhere had ever asked whether the real mechanism's advantage over the null-random-choice model (established only at Experiment B's calibrated `medium` baseline) survives at Experiment C's `very_wide` `(2,18)` heterogeneity condition — grep-confirmed directly (no prior `NullModelTrialConfig`/`run_null_model_trial` instantiation against any non-baseline `preference_mean_range`). Full ISC-113 description now lives in `## Criteria`, matching the established convention (short narrative pointer here, not a duplicated checklist bullet). One correction made to the candidate finding as originally proposed: its verification pass had used a single, seed_base=0 null-model figure (`1/150`) as the comparison baseline for BOTH seed bases, including the disjoint `seed_base=7000` heterogeneity replicate; this build instead computed a fresh, seed_base=7000-matched null-model baseline (following ISC-108's own "identical seed block" apples-to-apples principle), which came out to `0/150`, not the `1/150`/`2/150` figures used in the proposing pass's preliminary arithmetic — the qualitative conclusion (H0 rejected at `seed_base=7000`) is unchanged and the exact `p`-value is now even smaller (`p=0.00168` vs. the originally-estimated `p=0.0078`), but the manuscript and test file were written to the freshly-verified numbers, not the proposing pass's shortcut estimate. Real result, reported honestly regardless of which way it went, and reported at BOTH pre-existing gated seed bases because they disagree: at `seed_base=0`, `very_wide`'s `2/60` versus null `1/150` gives Fisher `p=0.19698722330301277` — H0 survives (not significant). At `seed_base=7000`, `very_wide`'s `5/60` versus a freshly-computed null `0/150` gives Fisher `p=0.0016835563479717132` — H0 is rejected (significant). The two seed bases disagree at the identical nominal condition; both are reported, neither is discarded. A scoping check confirms `wide` clears its matching null baseline at both seed bases (`p<1e-7` each) — the ambiguity is specific to the sweep's single most extreme point.
  **criterion now:** ISC-113 (above). Gate re-verified after the addition: `MYPYPATH=src uv run mypy --strict --explicit-package-bases --namespace-packages src` → `Success: no issues found in 26 source files` (no `src/` change was needed for this test-and-manuscript-only addition); `tests/colony/test_colony_experiments_extended.py tests/colony/test_colony_convergence_statistics.py` → `51 passed in 211.83s` (46 pre-existing + 5 new); full project gate → `268 passed`, `Total coverage: 95.72%`; `infrastructure.validation.cli markdown` on `manuscript/` → `No issues found!`; manuscript re-rendered (22 pages) and re-confirmed via page-scale raster read (page 18, the new Hypothesis-14 block, renders with correct typeset math, table, and the auto-numbered sequence remains continuous, `Definition 1`..`Hypothesis 14`..`Theorem 15`, zero `??` marks). See `## Verification` for the exact quoted command output.

- 2026-07-09T21:00:00Z: **Round 14: a cross-vendor audit breaks a two-round-long clean streak with a real, serious defect in shared statistics infrastructure.** Rounds 6 and 7's Forge audits both returned clean PASS (only minor doc/dead-code nits); round 8's own primary-session independent re-verification pass found nothing itself for the first time all session. The round-8 Forge audit did not rubber-stamp that streak — it independently recomputed every pinned statistic three ways (the project's own function, a hand-coded hypergeometric sum, and `scipy.stats.fisher_exact`) rather than trusting five-for-five internal self-consistency, and on the sixth, newly-exercised case (ISC-112's perfectly-separated plateau-vs-floor table) found the project's own `fisher_exact_test_two_sided` was wrong by 24 orders of magnitude. This is the single most severe defect found in any of this project's 8 rounds of adversarial hardening — not because the scientific conclusion changed (it didn't, at either value), but because it is exactly the class of error the manuscript's central honesty thesis exists to prevent: a function marketed as "a real hypergeometric computation... not an approximation" silently returning a wrong answer at an untested magnitude, undetected by two full audit rounds and this round's own three-stage ideation/verify-select/build pipeline, none of which happened to cross-check against an external oracle for this specific function. Fixed immediately (see Criteria ISC-114 and Changelog below for the full technical account). **Learned, generalizably:** internal self-consistency across N previously-pinned test cases is not evidence of correctness at an untested order of magnitude — a genuinely independent oracle (a different library, a closed-form hand computation, or both) is the only check that catches a tolerance-regime bug like this one, and this project's own stats-testing convention (documented at the top of `test_colony_stats_unit.py`: "every expected value ... computed independently of the function under test") should be read as applying to *magnitude coverage*, not just *method* independence — the five pre-existing Fisher-exact tests were all independently-computed in method, yet all five happened to share the property of `observed_p ≫ 1e-10`, so none of them could have caught this regardless of how independently each was derived.

**Follow-up: a second, narrowly-scoped Forge pass confirmed the fix and caught one last stale reference the primary session's own fix-and-verify pass had missed.** Independently re-derived all six affected cases via `scipy.stats.fisher_exact` (all six matched, including a previously-unnoticed seventh call site at `(140,150,1,150)` whose `observed_p≈3.2e-72` was also latently in the affected regime — never exactly re-pinned because its only assertion, `p<1e-6`, happened to hold under both the wrong and correct values, so no test needed correcting there, but it is now computed correctly regardless), confirmed the new regression test genuinely fails under the old additive-tolerance logic (both its assertions break), and enumerated all 11 real call sites of `fisher_exact_test_two_sided` project-wide to confirm no other silently-wrong pinned value exists. Found one real, if minor, loose end: the `## Test Strategy` table's ISC-112 row still quoted the old wrong `p=4.31e-11` as a live, unlabeled acceptance threshold — inconsistent with the corrected test, the Verification section, and this Changelog entry itself. Fixed immediately (corrected to `p=2.07e-35` with an explicit "corrected Round 14" note). **Learned:** a multi-location documentation fix (test + manuscript + multiple ISA.md sections) needs the same call-site-sweep discipline as a multi-location code fix — grepping for the wrong value across the whole file, not just the sections touched while writing the original fix narrative, is what catches a table row that a Changelog/Verification-focused fix pass can genuinely miss.

## Changelog

- **conjectured:** `uv run mypy --strict --explicit-package-bases --namespace-packages projects/templates/template_formal/src`, invoked bare from the repo root, would be the authoritative "src/ passes mypy --strict" oracle command (as literally specified in the delegated BUILD-phase agent prompts).
  **refuted by:** three independent BUILD-phase agents (protocol-network, agent-colony, registration-verify) each hit real, reproducible mypy errors (`Free type variable expected in Generic[...]`, `"SessionEndpoint" expects no type arguments`) with that exact bare invocation, and each independently root-caused it to the repo-root `pyproject.toml`'s `[tool.mypy]` section lacking `mypy_path`, which makes `--namespace-packages --explicit-package-bases` resolve the same file under two different module identities (once via `crawl_up` from repo root, once via the project's own intra-package absolute imports) — reproduced with a from-scratch minimal two-package repro unrelated to any `template_formal` code.
  **learned:** the authoritative oracle invocation for a project-local `src/` tree under this repo's mypy configuration is `MYPYPATH=<project>/src uv run mypy --strict --explicit-package-bases --namespace-packages <project>/src` (equivalently, run with `cwd=<project-root>`), which is exactly what `tests/test_mypy_oracle.py` already does as a subprocess — the ISA's own test harness was the correct oracle from the start; the bare repo-root form was a specification bug in the BUILD-phase delegation prompts, not a defect in `template_formal`'s code. Independently re-verified clean (`Success: no issues found in 20 source files`) by the primary session using the `MYPYPATH`-qualified form.
  **criterion now:** ISC-39/ISC-65 ("mypy --strict on src/ exits 0") is bound specifically to the `MYPYPATH`-qualified invocation documented in `tests/test_mypy_oracle.py` and this project's own `pyproject.toml` comment, not to a bare repo-root command.

- **conjectured:** registering a new public template only requires `public_scope.py` + the three generated docs + drift/tracked-projects checks (as scoped in this ISA's original `## Constraints`).
  **refuted by:** `git add --dry-run projects/templates/template_formal/` (run independently by the primary session after the BUILD-phase workflow reported "done") returned exit 1 — "The following paths are ignored by one of your .gitignore files" — because root `.gitignore`'s per-template allowlist is an explicit, fail-closed enumeration (`!projects/templates/<name>/` + `!projects/templates/<name>/**`) that the delegated registration agent never touched (out of its stated scope, and not covered by `check_template_drift.py`/`check_tracked_all.py`, which both passed clean even while the whole directory was untrackable).
  **learned:** `.gitignore`'s fail-closed per-template negation is a real, silent registration-tax site that survives every automated gate this repo currently runs — matches the concurrently-logged sibling gotcha (`gotcha-new-template-gitignore-and-isolated-venv-deps.md`, discovered mid-session while registering `template_pitch_deck`). A new template is not actually "trackable" until this is confirmed with `git add --dry-run`, independent of drift-check greenness.
  **criterion now:** added ISC-64.1 (implicit sub-check of ISC-64, tracked-artifact discipline): `git add --dry-run projects/templates/template_formal/` must exit 0 with zero ignored-path errors before "registered" is claimed. Fixed by adding `!projects/templates/template_formal/{,**}` and `!output/templates/template_formal/{,**}` to root `.gitignore`; re-verified `check_template_drift.py --strict` and `check_tracked_all.py` both still pass clean after the fix.

- **conjectured:** a root-venv `uv run pytest`/`uv run mypy` pass (as self-reported by the BUILD-phase workflow's registration agent) is sufficient evidence that "this project's gate passes."
  **refuted by:** the concurrently-logged sibling gotcha (found via ambient memory mid-session, from the `template_pitch_deck` registration effort) documents that the real pipeline (`execute_pipeline.py` / `scripts/pipeline/stage_01_test.py --project-only`) resolves a project-LOCAL isolated venv from the project's own `pyproject.toml` alone, which can lack transitive dependencies a root-venv run silently has access to.
  **learned:** re-ran `uv run python scripts/pipeline/stage_01_test.py --project templates/template_formal --project-only` independently (the real, isolated-venv-resolving invocation) and it passed clean (105 passed, 1 skipped, 95.3% coverage) — `template_formal` did not actually hit this trap (its only extra deps, `pyyaml`/`pillow`, were already added to its own `pyproject.toml` by the registration agent while fixing an unrelated `ModuleNotFoundError` during pipeline stage 02), but the self-reported root-venv pass alone would not have been sufficient evidence without this independent re-run.
  **criterion now:** ISC-53 is bound to the `stage_01_test.py --project-only` invocation, not a bare `uv run pytest`.

- **conjectured:** the `src/`-only mypy --strict gate (`Success: no issues found in 20 source files`), combined with per-module 90-100% coverage, was sufficient evidence that "strongly typed all the way down" holds for the shipped code, including its generic `Agent[StateT]` API.
  **refuted by:** Forge's cross-vendor audit (read-only, spawned per Rule 2a) instantiated `Agent[BeliefState]` — the template's own advertised reference pattern — and found it fails `mypy --strict` with two errors (`GaussianBelief` Protocol declared `mean`/`variance` as plain mutable attributes, which a `frozen=True` `BeliefState` dataclass can never satisfy). The `src/`-only oracle was structurally blind to this because `src/` itself never instantiates `Agent` with a concrete type argument — every real instantiation lived in `tests/`, which `tests/test_mypy_oracle.py` never type-checked. Forge additionally found: the "six known-bad fixtures" claim was off by one (5 bad + should have been counted separately from a positive control that didn't yet exist; ISC-19 correctly has no fixture, by design, but the manuscript conflated the two); the `bad_agent_id_construction.py` fixture carried a spurious second error caused by this same bug, weakening its bind to ISC-31 specifically; and the "emergent unanimous consensus" language for the 3-agent colony test overclaimed a result that is guaranteed by construction (identical agents, deterministic tie-break), not a discovered emergent regularity.
  **learned:** a `src/`-only mypy gate cannot certify a generic API's usability — the API surface a public template *advertises* must itself be type-checked, or the gate has a blind spot exactly where forkers will first hit trouble. Fixed by declaring `GaussianBelief.mean`/`.variance` as read-only `@property` members (satisfiable by a frozen dataclass) and adding `tests/mypy_fixtures/good_agent_belief_instantiation.py` as a permanent positive-control regression guard — re-verified: `Agent[BeliefState]` now type-checks cleanly, `src/` stays clean, and `bad_agent_id_construction.py`'s spurious second error is gone (now cites exactly one error, tightly bound to the `AgentId`/`UUID` mismatch). Manuscript corrected in four files (`00_abstract.md`, `01_introduction.md`, `02_type_architecture.md`, `04_active_inference.md`, `05_results_discussion.md`) to say "five known-bad + one known-good" fixtures and to reframe the colony result as a real, correctly-wired stigmergic positive-feedback mechanism rather than "emergence." Full gate re-run clean after the fix: mypy --strict 0 errors (20 files), `stage_01_test.py --project-only` → 106/106 passed (the previously-skipped positive-control parametrize case now runs and passes), 95.1% coverage, manuscript re-rendered and re-validated (page-scale raster re-confirmed, no `??`, honest language renders correctly).
  **criterion now:** added an implicit ISC-31.1 (regression guard): `tests/mypy_fixtures/good_agent_belief_instantiation.py` must exist and pass `mypy --strict` as a permanent positive control on the template's own generic API surface, not merely on `src/` in isolation.
- **conjectured:** committing `tla2tools.jar` (a compiled binary) directly into the public template tree was an acceptable way to make the TLA+ check runnable.
  **refuted by:** Forge's minor-note flag — a 2.2MB compiled binary has no place in a strongly-typed *source* template's tracked tree.
  **learned:** fetch-on-demand is strictly better here: `scripts/check_formal_specs.sh` now downloads `tla2tools.jar` from the official `tlaplus/tlaplus` GitHub release on first run (cached, gitignored) rather than shipping it as a tracked binary. Re-verified end-to-end: fresh fetch + real TLC run → "Model checking completed. No error has been found," exit 0.
  **criterion now:** ISC-35 is satisfied by a fetch-then-check script, not a committed binary; `formal/tla/tla2tools.jar` added to `.gitignore`.

- **conjectured (round 2, 2026-07-09):** the template was "done" after the round-1 Forge fix cycle; a broader `/security-review /RedTeam /BeCreative /workflows` audit pass (5 parallel specialists: security, mypy-oracle verifier, formal-methods comprehensiveness, statistics rigor, docs/visualization) would find only polish items, not real defects.
  **refuted by:** the audit found substantive, concrete gaps: (1) `tla2tools.jar` fetched from `latest` with zero integrity verification (HIGH severity, real supply-chain risk); (2) SQL table/column identifiers reaching raw-SQL f-strings with no construction-time validation (MEDIUM, no live exploit but a real inconsistency vs. every other typed boundary's runtime guard); (3) the mypy-oracle harness had the IDENTICAL blind-spot class as the round-1 `Agent[BeliefState]` finding, live-reproduced for `PheromoneField`/`InProcessBus[MsgT]` (broke `InMemoryPheromoneField`'s Protocol conformance, oracle stayed green); (4) the Lean/TLA+ specs modeled only the *ideal* protocol, with zero formal counterpart for fault injection (the Python implementation's headline feature) and zero liveness properties; (5) the colony "convergence" test was provably non-emergent (identical agents, deterministic tie-break) with no path to a genuinely earned statistical claim; (6) zero data-driven visualizations existed anywhere in the manuscript; (7) a *second* Forge pass, after all of the above was fixed, then caught `closed_only_via_known_paths` carrying the exact near-vacuity class the round-1 Lean expansion had already fixed once for a sibling theorem.
  **learned:** "no critical findings" from one cross-vendor pass does not mean the next broader pass (different lens, different specialists) won't find real gaps — security, oracle-completeness, formal comprehensiveness, statistical rigor, and honesty-of-conclusion-shape are each independent axes that need their own adversarial pass, not one pass assumed to cover all of them. The Lean vacuity recurrence specifically shows that fixing one instance of a defect class does not immunize the rest of the file against the same class — a rule (`# type: ignore` audits, mock-grep, etc.) generalizes automatically; a *proof-shape* defect does not, and needs a second look at every theorem of similar shape.
  **criterion now:** ISC-69 through ISC-79 (above) capture the concrete fixes. All independently re-verified by the primary session (not solely self-reported by delegated agents): `mypy --strict` → `Success: no issues found in 24 source files`; `stage_01_test.py --project-only` → `163 passed`, `94.9%` coverage; `check_formal_specs.sh` → Lean (7 theorems, zero `sorry`) + TLA+ ideal (liveness included) + TLA+ faulty (494 states, 92 distinct) all PASS; `check_template_drift.py --strict` → no drift; `check_tracked_all.py` → clean; manuscript re-rendered (15 pages) and re-confirmed via page-scale raster read. A follow-up Forge audit of the full round-2 expansion returned CONCERNS (not FAIL): one Lean near-vacuity finding (fixed, see above) and one informational SQL-guard defense-in-depth-consistency note (left as a documented, non-exploitable asymmetry — `validate_sql_identifier` covers every real input path; only a hypothetical `object.__setattr__` tampering scenario against `TableSchema.to_ddl`'s DDL generation is unguarded, and no code path constructs a `TableSchema` from untrusted input).

- **conjectured (round 3, 2026-07-09):** after two Forge-audited fix cycles, the codebase's numeric config surface was fully guarded (matching the SQL-identifier/isolation-level precedent) and the statistics/formal-methods layers were sound; a third `/FirstPrinciples /RedTeam /security-review /workflows` pass on the round-2 code specifically would find only cosmetic issues.
  **refuted by:** a FirstPrinciples pass and an independent security-deep pass *converged on the same real defect* without coordinating: `agent/agent.py`'s `BeliefState.variance` — the numeric field the manuscript's headline Active-Inference decision loop is built on — had zero validation, live-reproduced by both agents as `ZeroDivisionError`/`ValueError: math domain error` deep inside `gaussian_kl_divergence`/`gaussian_differential_entropy`, three calls removed from the actual defect (`BeliefState(mean=0.0, variance=0.0)` constructing silently). A build agent's round-3 fix pass had already closed the analogous gap at the `ColonyTrialConfig` level (ISC-80) but had not propagated the same guard to `BeliefState` itself, leaving the deeper, more load-bearing instance of the identical defect class open. The RedTeam pass additionally found: an unjustified `# type: ignore[arg-type]` in `colony/demo.py` (violating this template's own ISC-7 anti-criterion), and a real (currently-unreachable) `statistics.StatisticsError` leak in `wilson_score_interval` near `confidence≈1.0`. Everything else audited (calibration robustness across 7 independent seed bases, `stats.py` edge cases at n=0/n=1/degenerate inputs, the `MIN_CONVERGED_FOR_HISTOGRAM` guard, every manuscript-quoted number, supply-chain/JSON-deserialization/concurrency angles, and recurrence of the Lean vacuity class) came back clean.
  **learned:** a config-level validation fix (`ColonyTrialConfig.__post_init__`) does not automatically propagate to every other constructor of the same underlying value type (`BeliefState`) — closing a gap at one call site is not the same as closing the gap for the type itself. When two independent adversarial lenses (FirstPrinciples' "what's still decorative" question and security's "what could crash on bad input" question) converge on the identical file:line without prompting each other, that convergence is itself strong signal the finding is real, not lens-specific noise — this is the practical value of running genuinely different audit angles rather than one broader one.
  **criterion now:** ISC-81/82/83 (above) capture the three fixes, all directly implemented and verified by the primary session (not delegated): `BeliefState.__post_init__` added (mirrors `Column`/`TableSchema`/`ColonyTrialConfig`'s established pattern); `wilson_score_interval` now raises `ValueError` (not `statistics.StatisticsError`) near the `confidence≈1.0` float-rounding boundary; `demo.py`'s `# type: ignore` now carries an inline justification. Full gate re-verified after the fix: `mypy --strict` → `Success: no issues found in 24 source files`; `pytest projects/templates/template_formal/tests --no-cov` → `179 passed`; `stage_01_test.py --project-only` → `179 passed`, `95.0%` coverage; `check_template_drift.py --strict` → no drift; `check_tracked_all.py` → clean; `ruff check`/`ruff format --check` → clean.

- **conjectured:** the ISC-81 `BeliefState.variance` fix, once shipped and gate-clean, fully closed the "unvalidated-numeric-boundary" defect class for this round.
  **refuted by:** a third Forge cross-vendor audit (of the ISC-81/82/83 fix itself) found the fix was locally correct but left a residual: `ColonyTrialConfig.preference_variance` (`experiment.py`, ISC-80) still used its original `>= 0.0` guard, accepting `0.0` — but its sole consumer, `BeliefState.__post_init__` (ISC-81), now rejects `variance <= 0.0`. A config with `preference_variance=0.0` therefore still constructed silently and only failed one call later inside `run_colony_trial`'s `BeliefState` construction — the identical "guard accepts a value its downstream consumer rejects, so it fails far from the misconfiguration" pattern ISC-81 was written to eliminate, recreated one boundary removed. Live-reproduced by the audit exactly as described.
  **learned:** when two independent guards validate the same underlying value at different points in a call chain (a config field and the type it ultimately constructs), tightening one without checking the other reintroduces the same defect class at the remaining, looser boundary — "the fix passed its own tests" is not the same claim as "the fix closed the defect class everywhere the value flows." A boundary-consistency check (do all guards on the same value agree?) is a distinct thing to verify from correctness-of-one-guard-in-isolation.
  **criterion now:** ISC-84 (above). `ColonyTrialConfig.preference_variance`'s guard tightened from `>= 0.0` to `> 0.0`, matching `BeliefState` exactly. Re-verified: `mypy --strict` → `Success: no issues found in 24 source files`; `pytest projects/templates/template_formal/tests --no-cov` → `179 passed`; `stage_01_test.py --project-only` → `179 passed`, `95.0%` coverage; `check_template_drift.py --strict` → clean for `template_formal` (one unrelated WARNING for a sibling template, `template_pitch_deck`, out of scope); `check_tracked_all.py` → clean; `ruff check`/`ruff format --check` → clean; manuscript re-rendered (15 pages) and re-confirmed via page-scale raster read (pages 7, 9 spot-checked). No further residuals found on inspection of every other numeric-boundary pair in the codebase.

- **conjectured (round 4, 2026-07-09):** a Workflow fan-out with a FirstPrinciples pass, a RedTeam pass, and a Science hypothesis-generation pass, followed by one Build agent, would produce a coherent, fully-reconciled result as long as the Build agent's prompt faithfully summarized what each ideation agent would plausibly find.
  **refuted by:** the primary session's own orchestration choice was imprecise — the Build agent's prompt paraphrased the *kind* of hypotheses the Science pass would likely propose (H1/H2/H3 "something like...") but did not, and could not, contain the RedTeam agent's actual findings (agents in the same `parallel()` barrier do not see each other's output; only the primary session, reading all three results afterward, has that). The RedTeam pass found four real, independent honesty gaps (undisclosed hand-calibration/HARKing risk, a mis-scoped Threats-section caveat, a confounded positive control, an unmentioned exploratory correlation) that the Build agent consequently never addressed, even though the Build agent's own new experiments (the decay-only sweep) directly and coincidentally provided part of the fix for one of them (the confound).
  **learned:** in a multi-agent ideation-then-build pipeline, a build stage cannot act on findings it was never given — summarizing "the kind of thing an ideation pass would find" is not a substitute for passing its actual output forward. When ideation and build are split across a `parallel()`-then-single-`agent()` barrier (as here, since Build needed all three ideation results merged before it could act coherently), the orchestrating session — not a delegated agent — is responsible for carrying forward every ideation agent's real findings, not just the one used to shape the build's primary task list.
  **criterion now:** ISC-91 (above). All four RedTeam findings fixed directly by the primary session (not delegated) in `manuscript/05_results_discussion.md`: hand-calibration disclosed, Threats-section scope extended to the N=150 claim, the decay/noise confound disclosed with an explicit link to the (already-built) decay-only sweep as a partial mitigation, and the exploratory `pearson_r=0.1648` surfaced in prose. Re-verified: manuscript re-rendered (17 pages, up from 15) and re-confirmed via page-scale raster read (pages 11-14 spot-checked, new experiment tables and honesty-hedge prose render cleanly); `infrastructure.validation.cli markdown` → `No issues found!`; `check_template_drift.py --strict` / `check_tracked_all.py` / `git add --dry-run` all clean.

- **conjectured:** the round-4 manuscript honesty fixes (ISC-91), once gate-clean and Forge-audited, left this round's scientific content fully sound.
  **refuted by:** a fifth Forge cross-vendor audit (dedicated to this round's new science) returned PASS overall but one real CONCERN: ISC-87's own prose ("a real, measurable decline... not statistical noise") for the `decay=0.8→1.0` dip overreached, because the entire non-monotonicity finding rests on one pairwise comparison (`60/60` vs `56/60`) whose Wilson intervals overlap heavily, and the correct small-sample test for a proportion sitting exactly at the `100%` boundary (Fisher's exact, not a normal-approximation two-proportion test) gives two-sided $p=0.1187$ — not significant at $\alpha=0.05$.
  **learned:** "the raw counts happen to differ" is not the same claim as "the difference is statistically distinguishable from noise" — a pairwise comparison touching a `0%`/`100%` boundary needs the same boundary-aware test discipline this template already learned to apply to `wilson_score_interval` itself (ISC-82); the lesson generalizes across every proportion-comparison in this codebase, not just the one function it was first found in.
  **criterion now:** ISC-92 (above). `fisher_exact_test_two_sided` added to `colony/stats.py` (real hypergeometric computation, `math.comb`-only), the manuscript's phrasing corrected to quote the real, computed $p=0.1187$ and relocate the decline's evidentiary weight to the 13-point sweep shape and cross-seed replication rather than the one boundary-adjacent pairwise test, and a formal ordered-trend test named as explicit future work rather than silently implied to already exist. Re-verified: `mypy --strict` → `Success: no issues found in 26 source files`; `pytest projects/templates/template_formal/tests --no-cov` → `215 passed`; `stage_01_test.py --project-only` → `215 passed`, `95.5%` coverage; `check_template_drift.py --strict` → clean; `check_tracked_all.py` → clean; `ruff check`/`ruff format --check` → clean; manuscript re-rendered (18 pages) and re-confirmed via page-scale raster read (page 13 spot-checked, the corrected paragraph renders with the exact real $p$-value).

- **conjectured:** the Art skill (AI image generation) could produce the requested "cover image art" directly, since it is available in this session's skill listing.
  **refuted by:** invoking `Tools/Generate.ts` failed immediately with `Missing environment variable: GOOGLE_API_KEY`; a scan of every candidate `.env` location in this environment found only `ELEVENLABS_API_KEY`/`ELEVENLABS_VOICE_ID` configured — no Google/OpenAI/Flux image-generation credential exists in this environment at all.
  **learned:** rather than blocking on a missing credential or silently skipping the cover-art request, a deterministic procedural generator (matplotlib, seeded `random.Random`) is not just a fallback but arguably the *more correct* choice for this specific template — an AI-generated binary asset would be the one non-reproducible, non-tested artifact in an otherwise 100%-deterministic, 100%-tested, zero-mock, zero-network-call codebase, undermining the template's own stated principles rather than merely being lower-effort.
  **criterion now:** ISC-98 (above). `src/template_formal/colony/cover_art.py` ships `generate_cover_art`, tested by 5 real tests in `tests/colony/test_cover_art.py` (including a same-seed byte-identical determinism test and a different-seed byte-inequality test, proving the seed parameter is actually consumed, not merely accepted — the "hardcoded verdict"/unconsumed-parameter defect class this repo's own memory flags).

- **conjectured:** a raw-LaTeX `\begin{theorem}...\end{theorem}`-style block, fenced in a triple-backtick ` ```latex ` code block for visual clarity in the markdown source, would render identically to a bare unfenced block once passed through the same pandoc pipeline.
  **refuted by:** own prior-session memory (`gotcha-theorem-block-math-mangled-pdftotext-hides`) plus a direct read of `infrastructure/rendering/web_renderer.py`'s `_THEOREM_BLOCK_RE` (which scans the raw markdown *text* for bare `\begin{...}` sequences) established that a ` ```latex ` fence is pandoc's literal-code-listing syntax, not its `{=latex}` raw-passthrough syntax — using it would have silently rendered every formalism as a literal, unprocessed code listing. Avoided proactively (never actually built or rendered in the fenced form) by writing every formalism block as bare unfenced text from the start, with all inline math converted to `\(...\)` (never bare `$...$`, which the shared PDF renderer's unicode-remap does not protect inside raw blocks, per the same memory — this repo's `_MATH_BOUNDARY_RE` was independently confirmed to protect only `\(`/`\[`/named math environments, not `$...$`).
  **learned:** a documented gotcha from a *different* prior project generalizes directly to this one because both share the same `infrastructure/rendering` code — before implementing a feature this repo's memory already flags a failure mode for, re-derive the exact regex/mechanism in the CURRENT codebase rather than assuming the memory's fix transfers unchanged (in this case it did, but only after confirming `_MATH_BOUNDARY_RE`'s definition directly rather than trusting the memory's prose alone).
  **criterion now:** ISC-100 (above). Verified via page-scale raster read of the Definition-5 page (the manuscript's one formalism block with real inline math): renders as proper typeset $Q(o \mid \text{action}) = \mathcal{N}(\mu_q, \sigma_q^2)$-style notation, not ASCII-mangled text.

- **conjectured:** adding the same `\newtheorem` declarations used in `manuscript/preamble.md` (for the combined PDF) to `infrastructure/rendering/slides_renderer.py`'s existing math-header fallback mechanism would make the new formalism blocks render correctly in the per-section beamer slide decks too.
  **refuted by:** the first real render attempt failed beamer compilation with `LaTeX Error: Command \lemma already defined` / `\corollary already defined` / `\definition already defined` — beamer's own document class already provides `\theorem`/`\lemma`/`\corollary`/`\definition` as built-in styled blocks, and redeclaring them via `\newtheorem` collides with beamer's internal definitions. (`proposition`/`hypothesis`, which beamer does *not* provide, correctly failed the OTHER way first — "Environment proposition undefined" — confirming the diagnosis before the fix was written.)
  **learned:** a `\newtheorem` declaration that is safe and necessary for a plain LaTeX/amsthm document (the combined PDF) is not safe to blindly mirror into a beamer document, because beamer's document class pre-populates several theorem-like environment names itself; the correct fix declares only the environments beamer does not already provide, each with its own independent counter rather than attempting to chain onto beamer's internal (non-public-API) counter — consistent with this render path's existing degraded-but-non-fatal fallback design for citations/cross-references (slides already render `\citep`/`\cref` in simplified form rather than matching the PDF exactly).
  **criterion now:** ISC-101 (above). Fixed in `infrastructure/rendering/slides_renderer.py`'s `_maybe_write_math_header`; a new regression test (`test_helper_defines_proposition_and_hypothesis_environments`) pins both the two additions and the four omissions. Re-verified: `uv run pytest tests/infra_tests/rendering/test_slides_renderer_core.py` → `42 passed` (43 after the new test); a full real render (`scripts/pipeline/stage_03_render.py --project templates/template_formal`) produced all 7 beamer slide decks with zero LaTeX errors, confirmed by rastering slide 11 of the `02_type_architecture_slides.pdf` deck showing the Proposition block styled correctly.

- **conjectured:** dispatching three BUILD-phase agents via one `pipeline(buildPrompts, singleStageFn)` call, one per independent ideation source (FirstPrinciples/RedTeam/Science), would let each agent implement its own findings without interfering with the others.
  **refuted by:** `pipeline()` with a single stage imposes no synchronization barrier between items — nothing serialized the three agents against each other, and their target files overlapped (`colony/stats.py`, the two experiment test files, `manuscript/05_results_discussion.md`). The first-principles-track agent's own `Edit` call failed with "modified since read" mid-run, and it correctly diagnosed a live concurrent session stepping through the identical file sequence, then deliberately stood down and wrote zero files rather than force a conflicting write — the worse outcome it correctly avoided. The redteam-track agent, per its own artifact left behind (`formal/tla/AntProtocolFaultyNegControl.tla`, correctly reasoned but not wired to a `.cfg`/`check_formal_specs.sh`/the manuscript), was very likely interrupted by the same race rather than choosing to stop early.
  **learned:** a `Workflow` script's `pipeline()` call does not implicitly serialize the *items* passed to it merely because they share one call site — only explicit multi-stage sequencing serializes stages *within* an item, and even that runs different items' stages independently ("no barrier between stages" per the tool's own documentation). When multiple build agents' target file sets can genuinely overlap, either restructure as sequential `agent()` calls, partition file ownership explicitly in each prompt, or accept the collision risk and rely on (never assume) an agent's own collision-detection discipline. This generalizes the pre-existing sibling gotcha about separate human-launched Claude sessions colliding on a directory (`gotcha-concurrent-claude-sessions-collide-on-new-project-dir`) to sibling agents inside one workflow's own fan-out.
  **criterion now:** ISC-106 (above). The primary session independently re-verified the surviving (science-track) build's changes were real and correct (mypy --strict clean, ruff clean after formatting, all quoted test counts reproduced) before building further on top of them, and separately completed the redteam-track agent's unfinished-but-correct TLA+ negative control (ISC-105) rather than discarding real, verifiably-correct work just because its authoring agent's run was interrupted mid-task.

- **conjectured:** `test_fisher_exact_matches_manuscript_quoted_decay_dip_pvalue`'s hardcoded literals (`fisher_exact_test_two_sided(60, 60, 56, 60)`) were an acceptable way to pin the manuscript's quoted Fisher p-value, since the arithmetic itself is correct and independently re-checkable.
  **refuted by:** a RedTeam pass found (MEDIUM, confirmed by direct grep of `tests/colony/test_colony_experiments_extended.py`) that nothing connects those literals to `decay_sweep_points` — the fixture that actually generates the real decay=0.6/1.0 counts. A future re-tuning of `seed_base`/`n_per_value`/the baseline config could silently drift the sweep's real numbers while the hardcoded literals (and the manuscript prose quoting them) stayed frozen, unflagged by any test.
  **learned:** "the number is arithmetically correct" is a different, weaker claim than "the number is derived from its own generating experiment" — a regression guard that pins a manuscript-quoted statistic must compute that statistic from the live fixture that produces the underlying data, not from a second, independently hand-typed copy of what that fixture happened to produce once.
  **criterion now:** ISC-104 (above). `test_decay_dip_fisher_pvalue_is_derived_from_this_sweeps_own_fixture` added to `tests/colony/test_colony_experiments_extended.py`, computing the Fisher p-value directly from `decay_sweep_points`'s `by_value[0.6]`/`by_value[1.0]` and asserting equality with the manuscript-quoted value. Re-verified: `pytest tests/colony/test_colony_experiments_extended.py -k decay_dip_fisher` → `1 passed`.

- **conjectured:** the naive mutation test the RedTeam ideation pass proposed (widening `ReceiveHello`'s guard to accept `"corrupt"`-tagged frames) would, once wired as a permanent negative control, demonstrate `NoFalseEstablishment` genuinely depends on content-integrity.
  **refuted by:** direct re-execution of exactly that mutation (independently reproduced by the primary session via `java -jar tla2tools.jar` against a scratch-mutated copy) confirmed RedTeam's own finding: the invariant still reports zero violations even with the widened guard, because `Corrupt` preserves a message's `sender` field and only ever acts on a frame a genuine `SendHello` already placed on the channel — a corrupted-but-genuinely-originated frame still satisfies send-provenance. The already-existing `AntProtocolFaultyNegControl.tla` artifact (left by the interrupted redteam-track build agent) had itself already reasoned through and rejected this exact naive mutation in its own header comment, in favor of the correct one (`ForgeHello`, a spoofed hello with `iSentHello` never set).
  **learned:** the "obviously right" mutation for a negative control is not automatically the mutation that actually falsifies the invariant under test — the ghost-variable semantics have to be traced precisely (what does `iSentHello` actually track: content-integrity, or send-provenance?) before choosing which property to break. A negative control that fails to reject the wrong mutation would itself be a false confirmation of the fix, not a real one.
  **criterion now:** ISC-105 (above). `AntProtocolFaultyNegControl.cfg` created, TLC run directly and independently by the primary session (not merely trusted from the file's own header comment): `Error: Invariant NoFalseEstablishment is violated` in a 3-state trace (`Init → ForgeHello → ReceiveHello`), reproduced identically on a second run after wiring into `scripts/check_formal_specs.sh`. Manuscript's Theorem 10 clause (iii) reworded to "send-provenance," with a new paragraph narrating the mutation test, the correct fix, and the re-verification. `.gitignore` updated for the TLC trace-exploration artifacts (`*_TTrace_*.tla`/`.bin`) this negative control generates on every run by design.

- **conjectured:** `fisher_exact_test_two_sided`'s two-sided tail inclusion test, `hypergeom_pmf(k) <= observed_p + 1e-10`, is a safe, magnitude-independent float-equality guard for any valid input table — the additive `1e-10` term was written (round 4) as a generic "don't let float rounding exclude the observed table itself" safety margin, and five independently-derived test cases (decay-dip, the two ISC-111/ISC-113 extreme-configuration comparisons, and ISC-113's two seed-base comparisons) all passed against independently hand-computed or cross-checked expected values, reinforcing the belief that the function was fully correct.
  **refuted by:** a Forge cross-vendor audit of round 8, auditing ISC-112's plateau-vs-floor comparison (a perfectly-separated `60/60`-vs-`0/60` table — the first pinned test case in this function's history with `observed_p` far below `1e-10`, specifically `≈1.035e-35`), independently recomputed the true two-sided p-value three ways (the project's own function, a hand-coded hypergeometric sum, and `scipy.stats.fisher_exact`) and found the project function's `4.308978969823708e-11` disagreed with the true `2.070073888186964e-35` by 24 orders of magnitude. The additive tolerance, safe when `observed_p` is itself order `1e-10` or larger, completely swallows `observed_p` when it is dramatically smaller — degenerating "sum every table at least as extreme as observed" into "sum every table with probability at most `1e-10`," a categorically different and much larger sum.
  **learned:** a float-tolerance term meant to guard against rounding error must scale with the quantity it is protecting (relative tolerance), never be a fixed absolute value, whenever that quantity's magnitude can vary across many orders of magnitude between call sites — an absolute tolerance that is appropriately tiny for typical inputs can silently become enormous, relative to an atypical input, without ever producing an error, a warning, or a test failure, because the function still returns *a* number in `[0, 1]` that looks superficially plausible. This is also a testing-methodology lesson distinct from this project's own stated "independently computed expected value" discipline: independence of *derivation method* across test cases does not guarantee coverage of *magnitude regime* — five independently-derived test cases all sharing `observed_p ≫ 1e-10` provided zero evidence about correctness at `observed_p ≪ 1e-10`, and no number of additional same-regime test cases would have caught this; only a test case in the previously-untested regime (or an external-oracle cross-check) could.
  **criterion now:** ISC-114 (above). Fixed by switching to a relative tolerance, `observed_p * (1.0 + 1e-9)`. Re-verified: all five previously-correct pinned Fisher-exact values (`0.1187244128420599`, `0.002634204400259045`, `0.19698722330301277`, `0.0016835563479717132`, plus the general symmetric/identical-proportions/extreme-difference unit tests) reproduce bit-identically after the fix; the sixth, previously-wrong value corrected to `2.070073888186964e-35`, independently confirmed against `scipy.stats.fisher_exact` and the closed-form `2/C(120,60)`. New dedicated regression test added (`test_fisher_exact_perfectly_separated_table_matches_hand_computed_value`) pinning the corrected value against the independent closed-form computation specifically for this previously-unexercised magnitude regime. `tests/colony/test_colony_experiments_extended.py:1531`'s dose-response Fisher assertion re-pinned to the corrected value. Manuscript `05_results_discussion.md` and `ISA.md`'s own prior Round-12 quotes corrected, with an explicit narration of the bug and fix rather than a silent correction.

## Verification

- ISC-1..7 (types): `uv run pytest tests/test_types_ids.py tests/test_types_result.py -v` — quoted: `11 passed` (primary session, direct run).
- ISC-2/4/15/18/19/31 (mypy-oracle negative controls): `tests/test_mypy_oracle.py::test_known_bad_fixture_is_rejected_by_mypy_strict[...]` — quoted from independent re-run via `scripts/pipeline/stage_01_test.py --project-only`: all 6 parametrized fixture cases pass among `105 passed, 1 skipped`.
- ISC-8..15 (storage): quoted from `stage_01_test.py --project-only` run: `tests/storage/*` all pass; coverage report line `storage/db.py ... 87.64%`, `storage/transaction.py ... 89.19%` (both count toward the 95.3% aggregate, well above the 90% floor).
- ISC-16..26 (protocol/network): quoted: `protocol/session.py` and `network/bus.py` both **100.00%** coverage per the BUILD-phase agent's own coverage report, independently consistent with the primary session's aggregate 95.3%/95.33%/95.86% runs (minor variance is coverage-tool branch-counting noise across runs, not a regression — all three runs clear the 90% floor with the identical 105-passed test count).
- ISC-27..34 (agent/colony): quoted numeric hand-derivation confirmed present in `tests/agent/test_agent_free_energy.py` per BUILD-phase agent report; three-agent colony integration result independently visually confirmed in the rendered manuscript §6.3 (page-scale raster read, not text-extraction) describing 5 concrete emergent properties checked against real per-agent state.
- ISC-35/36 (formal side-spec): quoted, BUILD-phase agent report: `lake build` → `Build completed successfully (3 jobs)`, exit 0, zero `sorry`; `java -jar tla2tools.jar ... AntProtocol.tla` → `Model checking completed. No error has been found. 6 states generated, 5 distinct states found`, exit 0. Both wired into `scripts/check_formal_specs.sh`, documented in `TODO.md`.
- ISC-37..40 (mypy-oracle harness integrity): quoted: `test_at_least_one_bad_fixture_exists PASSED`; `test_real_src_tree_passes_mypy_strict_clean PASSED` — both confirmed in the primary session's independent `stage_01_test.py --project-only` run.
- ISC-41..49 (manuscript): quoted, primary-session direct page-scale raster read (`Read` tool, PDF pages 1–12 of `output/templates/template_formal/template_formal_combined.pdf`, 12 pages total per `pdfinfo`): title page, TOC, abstract, §2–§6 all render with correct math (KL-divergence/entropy formula in §5.1 renders as real LaTeX, not mangled), §3.5 "What mypy --strict proves vs. what is a runtime discipline" section present and ISC-numbered exactly as required, §6.5 "Threats to the claims, honestly stated" present, References page (12) lists all 12 real citations with no `??` marks and no fabricated DOIs. `grep -c "dependent type\|linear type" manuscript/*.md` outside the explicit non-claim paragraph in §3.5 returns zero (ISC-44).
- ISC-50..64 (repo integration): quoted, primary-session independent re-runs: `check_template_drift.py --strict` → `template_drift: no drift detected.`; `check_tracked_all.py` → `projects: clean / fonds: clean / tools: clean / rules: clean`; `MYPYPATH=.../src uv run mypy --strict ... .../src` → `Success: no issues found in 20 source files`; `git add --dry-run projects/templates/template_formal/` → 80 files, zero ignored-path errors, zero `.DS_Store`/cache matches (after the `.gitignore` fix documented in Changelog).
- ISC-53 (authoritative coverage gate): quoted, primary-session run of `uv run python scripts/pipeline/stage_01_test.py --project templates/template_formal --project-only`: as of round 2 (post-expansion), `163 passed ... Required test coverage of 90% reached. Total coverage: 94.9%` — the real isolated-venv pipeline invocation, not a root-venv proxy. (Historical: 105 passed/95.3% at round-1 close, 106 passed/95.1% after the round-1 Forge fix — both superseded by round-2's larger test surface.)
- ISC-65..68 (anti-criteria): confirmed via the above — `grep -rn "MagicMock\|mocker.patch\|unittest.mock" tests/` returns zero matches (primary session spot-check consistent with all sub-agent reports); every fault-injection ISC (23/24) has a paired test distinct from its happy-path counterpart per `tests/network/test_handshake_over_bus.py`.

Coverage (round 1): 68/68 ISC groups passed, 68/68 artifact-backed (0 `[DEFERRED-VERIFY]`).

**Cross-vendor audit round 1 (Forge, read-only, Rule 2a):** initial verdict FAIL (1 critical, 3 concerns) — see Changelog. All four findings addressed and re-verified by the primary session: (1) `GaussianBelief` Protocol fixed to read-only `@property`, `Agent[BeliefState]` now `mypy --strict`-clean (quoted: `Success: no issues found in 1 source file` on the new good-fixture); (2) fixture count corrected to "five known-bad + one known-good" in all 5 manuscript files; (3) `bad_agent_id_construction.py` now cites exactly one error (quoted: `Found 1 error in 1 file`); (4) "emergent"/"convergence" language corrected across the manuscript to honestly describe a deterministic, correctly-wired stigmergic mechanism rather than emergence. Final re-run: `MYPYPATH=src mypy --strict src` → `Success: no issues found in 20 source files`; `stage_01_test.py --project-only` → `106 passed`, `95.1%` coverage; manuscript re-rendered (12 pages), re-validated, and re-confirmed via page-scale raster read (pages 3, 8, 9 spot-checked post-fix). Drift/tracked-projects/git-add-dry-run all re-confirmed clean after the fix cycle.

**Round 2 (security review + RedTeam + BeCreative + workflow expansion, 2026-07-09):** ISC-69..79 added and verified (see Changelog). Two audit cycles: (a) 5 parallel specialist agents (security, mypy-oracle verifier, formal-methods, statistics, docs/viz) found real gaps, all fixed; (b) a second Forge cross-vendor audit of the full expansion returned CONCERNS — one Lean near-vacuity finding (`closed_only_via_known_paths`, fixed by removal + promoting `step_to_closed_cases`) and one informational SQL-guard consistency note (documented, non-exploitable). Final state, all independently re-verified by the primary session: `mypy --strict` → `Success: no issues found in 24 source files`; `stage_01_test.py --project-only` → `163 passed`, `94.9%` coverage; `scripts/check_formal_specs.sh` → Lean (7 theorems, zero `sorry`) + TLA+ ideal (with liveness) + TLA+ faulty (494 states/92 distinct) all PASS, exit 0; `check_template_drift.py --strict` → no drift; `check_tracked_all.py` → clean; `git add --dry-run projects/templates/template_formal/` → zero ignored-path/junk errors; manuscript re-rendered (15 pages, up from 12 — two new real data-driven figures + two new Mermaid diagrams), re-validated (figure registry PASS), and re-confirmed via page-scale raster read (pages 4-6, 10-13 spot-checked). Nothing committed to git — left for the user to review.

Coverage (round 2, cumulative): 79/79 ISC groups passed, 79/79 artifact-backed (0 `[DEFERRED-VERIFY]`).

**Round 3 (third adversarial pass: security-deep, redteam-round2-code, first-principles-deepen, 2026-07-09):** ISC-80 added and verified (see Decisions). `tests/colony/test_colony_convergence_statistics.py` re-run three independent times in a row: all three produced identical output — `convergence: successes=140/150 rate=0.9333 wilson_95%=(0.8816, 0.9634)` (well above the 0.8 lower-bound gate; deterministic given fixed seeds, so zero flakiness across runs, `6 passed` each time). `stats.py`'s `wilson_score_interval`/`consensus_tick_summary`/`pearson_r` directly probed with `n=0`, `n=1` (all-success and all-failure), a single data point, and identical-x-values inputs — every case already returns a controlled `ValueError` or `Ok`/`Err`, zero unhandled tracebacks; two new regression tests added for the previously-untested `n=1` case. Tree-wide grep for `curl|wget|urllib|requests\.|urlopen` found only the already-checksummed `tla2tools.jar` fetch (ISC-69) — no other unverified download exists. `scripts/02_run_analysis.py` re-run three fresh times (`rm -rf output` between runs): identical `successes=37/40` every time, comfortably clearing `MIN_CONVERGED_FOR_HISTOGRAM=5`. Final gate re-run, all independently executed by the primary session: `MYPYPATH=.../src uv run mypy --strict --explicit-package-bases --namespace-packages .../src` → `Success: no issues found in 24 source files`; `uv run python scripts/pipeline/stage_01_test.py --project templates/template_formal --project-only` → `174 passed`, `Total coverage: 94.98%` (94.98% >= 90% floor); `uv run python scripts/audit/check_template_drift.py --strict` → `template_drift: no drift detected.`

Coverage (round 3, cumulative): 80/80 ISC groups passed, 80/80 artifact-backed (0 `[DEFERRED-VERIFY]`).

- ISC-85 (nullmodel determinism + structural isolation): `tests/colony/test_nullmodel.py` — `11 passed`; source-text grep test and AST import-allowlist test both confirmed passing (primary session direct run).
- ISC-86 (sweep validation + hand-derived Wilson bounds): `tests/colony/test_sweep.py` — `9 passed`; `test_sweepable_field_names_matches_colonytrialconfig_fields_minus_seed` confirms `_SWEEPABLE_FIELD_NAMES` tracks `ColonyTrialConfig`'s real fields (not a hand-maintained, driftable list).
- ISC-87..89 (three real experiments): `tests/colony/test_colony_experiments_extended.py` — `10 passed` in `49.13s` (well under the 180s budget); real quoted output, primary-session direct run:
  ```
  decay sweep:
    decay=0.10 successes=0/60 rate=0.0000 wilson=(0.0000,0.0602)
    decay=0.30 successes=0/60 rate=0.0000 wilson=(0.0000,0.0602)
    decay=0.46 successes=56/60 rate=0.9333 wilson=(0.8407,0.9738)
    decay=0.60 successes=60/60 rate=1.0000 wilson=(0.9398,1.0000)
    decay=0.80 successes=60/60 rate=1.0000 wilson=(0.9398,1.0000)
    decay=1.00 successes=56/60 rate=0.9333 wilson=(0.8407,0.9738)

  real-vs-null: real successes=140/150 rate=0.9333 wilson=(0.8816,0.9634)
  real-vs-null: null successes=1/150 rate=0.0067 wilson=(0.0012,0.0368)

  heterogeneity sweep:
    tight range=(9.0, 11.0) width=2.0 successes=60/60 rate=1.0000 wilson=(0.9398,1.0000)
    medium range=(8.0, 12.0) width=4.0 successes=56/60 rate=0.9333 wilson=(0.8407,0.9738)
    wide range=(5.0, 15.0) width=10.0 successes=15/60 rate=0.2500 wilson=(0.1578,0.3723)
    very_wide range=(2.0, 18.0) width=16.0 successes=2/60 rate=0.0333 wilson=(0.0092,0.1136)
  ```
- ISC-90 (manuscript): `uv run python -m infrastructure.validation.cli markdown projects/templates/template_formal/manuscript/` → `No issues found!`; new "Three pre-registered experiments" section, abstract-contributions update, and "Threats to the claims" scoping update all present in `05_results_discussion.md`/`00_abstract.md`, each hypothesis stated before its result per grep-spot-check of section ordering.

**Round 4 (genuine scientific/methodological depth — a null-model baseline, a generic parameter-sweep runner, and three real pre-registered experiments, 2026-07-09):** ISC-85..90 added and verified (see Decisions). New modules `colony/nullmodel.py` (100.00% coverage) and `colony/sweep.py` (100.00% coverage). Full gate re-run, all independently executed by the primary session:
- `cd /Users/4d/Documents/GitHub/template && MYPYPATH=.../template_formal/src uv run mypy --strict --explicit-package-bases --namespace-packages projects/templates/template_formal/src` → `Success: no issues found in 26 source files`.
- `uv run python scripts/pipeline/stage_01_test.py --project templates/template_formal --project-only` → `209 passed`, `Total coverage: 95.45%` (95.45% >= 90% floor); per-module coverage for the two new modules both `100.00%`.
- `uv run python scripts/audit/check_template_drift.py --strict` → `template_drift: no drift detected.`
- `grep -rn "MagicMock\|mocker.patch\|unittest.mock" projects/templates/template_formal/tests/` → zero matches.
- `uv run python -m infrastructure.validation.cli markdown projects/templates/template_formal/manuscript/` → `No issues found!`.
- `uvx ruff check` / `uvx ruff format --check` on all five new/modified files (`colony/nullmodel.py`, `colony/sweep.py`, `colony/__init__.py`, `tests/colony/test_nullmodel.py`, `tests/colony/test_sweep.py`, `tests/colony/test_colony_experiments_extended.py`) → clean after one `ruff format` auto-wrap pass (two files had lines that needed re-wrapping to the project's line-length; no logic changed).

Manuscript PDF intentionally **not** re-rendered by this round (per explicit instruction — the primary session renders after this build completes); markdown-source validation only.

Coverage (round 4, cumulative): 90/90 ISC groups passed, 90/90 artifact-backed (0 `[DEFERRED-VERIFY]`).

**Round 5 (folder-level documentation coverage + independent verification pass, 2026-07-09):** ISC-93..96 added and verified (see Decisions). Parallel documentation agents wrote a `README.md`/`AGENTS.md` pair at every real source, test, script, manuscript, and formal-methods directory, plus a new `docs/` tree with topic guides and its own index pair — confirmed complete via a full-tree `find` enumeration (`ISC-93`), zero directories missing either file. A dedicated verification pass then read every new file against the real, current code (not against ISA.md's own prose) and fixed six inaccuracies (see Decisions for the full list: stale test-count/coverage/file-count numbers in the root `README.md`/`AGENTS.md`, a wrong file count in `colony/README.md`, a wrong fault-roll order in `network/README.md`, a self-falsifying `{{`-grep claim in `manuscript/README.md`/`AGENTS.md`, and an imprecise `find` command in `formal/README.md`/`AGENTS.md`). All fixes were documentation-only — zero `src/`/`tests/` code changed this round. Final state, all independently re-verified by the primary session:
- `MYPYPATH=.../template_formal/src uv run mypy --strict --explicit-package-bases --namespace-packages projects/templates/template_formal/src` → `Success: no issues found in 25 source files`.
- `uv run pytest tests -n 4 --no-cov -q` → `215 passed in 72.89s` (`ISC-95`, pytest-xdist parallelization independently confirmed working).
- `uv run python scripts/pipeline/stage_01_test.py --project templates/template_formal --project-only` → `215 passed`, `Total coverage: 95.54%`.
- `uv run python scripts/audit/check_template_drift.py --strict` → `template_drift: no drift detected.`
- `uv run python scripts/audit/check_tracked_all.py` → `projects: clean / fonds: clean / tools: clean / rules: clean`.
- `git add --dry-run projects/templates/template_formal/` → 146 files, zero ignored-path/junk matches.
- `uv run python -m infrastructure.validation.cli markdown` on the project root (recursive) and on every one of its 21 subdirectories individually (`src/`, all 6 `src/template_formal/*` subpackages, `tests/`, all 6 `tests/*` subdirectories, `scripts/`, `manuscript/`, `formal/`, `formal/lean/`, `formal/tla/`, `docs/`) → `No issues found!` in every invocation.
- `find src/template_formal -mindepth 1 -type d -iname 'formal'` → empty (`ISC-96`; the dead stub stays gone).

Coverage (round 5, cumulative): 96/96 ISC groups passed, 96/96 artifact-backed (0 `[DEFERRED-VERIFY]`).

**Round 6 (title + procedural cover art + auto-numbered formalisms, then a FirstPrinciples/RedTeam/Science methods-deepening round, 2026-07-09):** ISC-97..106 added and verified (see Decisions/Changelog). Part 1 (presentation): new title/subtitle, a deterministic seeded `cover_art.py` generator (5/5 tests), `\newtheorem`-based auto-numbering across 10 formalism blocks (Definition 1 through Theorem 10, one shared sequence) plus a labeled+cross-referenced equation, and a shared-infra fix to `infrastructure/rendering/slides_renderer.py` so beamer slides don't hard-fail on the new environments. Part 2 (methods): a Workflow-orchestrated ideation round (FirstPrinciples/RedTeam/Science in parallel) converged on the same top statistical gap (a real, stdlib-only Cochran-Armitage ordered-trend test, now shipped and applied to both sweeps) and RedTeam additionally found two independent defects the other passes missed — a HIGH-severity, mutation-test-confirmed near-vacuity in the TLA+ `NoFalseEstablishment` invariant (fixed with a permanent negative control, `AntProtocolFaultyNegControl.tla`/`.cfg`, wired into `scripts/check_formal_specs.sh` with inverted pass/fail logic) and a MEDIUM-severity disconnection between a manuscript-quoted Fisher p-value and its generating experiment (fixed with a fixture-derived regression test). Three build-phase agents dispatched via one `pipeline()` call raced on overlapping files; one correctly detected the collision and stood down, one landed real changes, and one left genuinely correct but unfinished work that the primary session completed directly after independently re-verifying it (see Changelog for the full account). Final state, all independently re-verified by the primary session:
- `MYPYPATH=.../template_formal/src uv run mypy --strict --explicit-package-bases --namespace-packages projects/templates/template_formal/src` → `Success: no issues found in 26 source files`.
- `uv run python scripts/pipeline/stage_01_test.py --project templates/template_formal --project-only` → `240 passed`, `Total coverage: 95.7%` (>= 90% floor).
- `uvx ruff check` / `uvx ruff format --check` on all touched Python files → clean after one `ruff format` auto-wrap pass on three files (no logic changed).
- `uv run python -m infrastructure.validation.cli markdown projects/templates/template_formal/manuscript/` → `No issues found!`.
- `uv run python -m infrastructure.validation.cli pdf projects/templates/template_formal/output/pdf/template_formal_combined.pdf` → `Total issues found: 0`.
- Full manuscript re-render (`scripts/pipeline/stage_03_render.py --project templates/template_formal`) → combined PDF (20 pages) + combined HTML + all 7 beamer slide decks, zero errors; page-scale raster read (not `pdftotext` alone) confirmed the title page (title/subtitle/cover art), the Definition-5 free-energy page (real typeset math, not ASCII-mangled), Theorem-10 and its surrounding new prose (both pages), and beamer slide 11 of `02_type_architecture_slides.pdf` (Proposition block styled correctly) — all render exactly as intended.
- `java -jar tla2tools.jar -config AntProtocolFaultyNegControl.cfg AntProtocolFaultyNegControl.tla` (run directly, independent of `check_formal_specs.sh`) → `Error: Invariant NoFalseEstablishment is violated` in a 3-state trace, then `scripts/check_formal_specs.sh` end to end → `check_formal_specs.sh: both formal side-specs passed`, exit 0 (all four checks: Lean build, TLC ideal model, TLC faulty model, TLC negative control).
- `uv run python scripts/audit/check_template_drift.py --strict` → zero errors/warnings for `template_formal` (two remaining findings belong to `template_pools_rules_tools`, out of this session's scope).
- `uv run python scripts/audit/check_tracked_all.py` → `projects: clean / fonds: clean / tools: clean / rules: clean`.
- `git add --dry-run projects/templates/template_formal/` → zero ignored-path/junk matches.
- `grep -c "templates/template_formal" infrastructure/project/public_scope.py` → `1` (registration intact; re-checked given this session's prior convergent-automation risk).

**Cross-vendor audit, round 6 (Forge, read-only):** verdict **PASS** — independently re-derived the Cochran-Armitage constants from scratch (cross-checked against R's `prop.trend.test`), independently re-ran TLC against both `AntProtocolFaulty.tla` and `AntProtocolFaultyNegControl.tla` and diffed them to confirm the negative control's violation traces to `ForgeHello` specifically (not an unrelated bug), independently reproduced the cited mutation-test state counts (92→136), and independently compiled minimal beamer documents to confirm `slides_renderer.py`'s fix doesn't collide with any other template. Three minor, non-blocking nits were found (no correctness/safety defect): (1) a now-dead `if not snippet_parts: return None` branch in `slides_renderer.py` plus a stale docstring claim, predating round 6 but cemented by it — fixed by removing the dead branch and correcting the docstring; (2) `cover_art.py`'s module docstring underclaimed what its own test actually asserts (byte-identical within-process, not merely "composition") — fixed by tightening the docstring to match the test; (3) the Cochran-Armitage 2-group happy path (mathematically valid, independently verified by Forge) had no dedicated regression-pinned test, only the `<2`-groups raise — fixed by adding a hand-computed 2-group test. All three re-verified: `mypy --strict` → `Success: no issues found in 26 source files` (project) + `Success: no issues found in 1 source file` (`slides_renderer.py`); `uv run pytest tests/infra_tests/rendering/test_slides_renderer_core.py --no-cov` → `43 passed`; `stage_01_test.py --project-only` → `241 passed`, `95.7%` coverage.

**Round 7 (decay-sweep cross-seed replication, gated, 2026-07-09):** ISC-107 added and verified (see Decisions). Real, quoted output, primary-session direct runs:
- `uv run pytest projects/templates/template_formal/tests/colony/test_colony_experiments_extended.py -v -k "decay_replication or decay_threshold_then_plateau" --no-cov` →
  ```
  test_decay_replication_wall_clock_stays_within_budget PASSED
  test_decay_replication_at_seed1000_reproduces_the_exact_counts PASSED
  test_decay_threshold_then_plateau_then_decline_shape_replicates_at_a_disjoint_seed_base PASSED
  3 passed, 18 deselected in 27.63s
  ```
  Real numbers printed by the new test at `seed_base=1000`: `decay=0.10 successes=0/60`, `decay=0.30 successes=0/60`, `decay=0.46 successes=58/60`, `decay=0.60 successes=60/60`, `decay=0.80 successes=60/60`, `decay=1.00 successes=53/60` — versus `seed_base=0`'s `0/60, 0/60, 56/60, 60/60, 60/60, 56/60` (ISC-87 above): different exact counts, identical qualitative shape (near-zero floor at decay ≤ 0.30, 100% plateau at decay ∈ {0.60, 0.80}, measurable decline at decay=1.00 relative to that plateau).
- `uv run pytest projects/templates/template_formal/tests/colony/test_colony_experiments_extended.py --no-cov -q` → `21 passed in 156.85s` (18 pre-existing + 3 new; no regression in the pre-existing 18).
- `cd /Users/4d/Documents/GitHub/template/projects/templates/template_formal && MYPYPATH=src uv run mypy --strict --explicit-package-bases --namespace-packages src` → `Success: no issues found in 26 source files`.
- `uv run python scripts/pipeline/stage_01_test.py --project templates/template_formal --project-only` → `244 passed`, `Total coverage: 95.70%` (>= 90% floor; unchanged from round 6's 95.7%, as expected — no new `src/` code was added, only new tests).
- `uv run python -m infrastructure.validation.cli markdown projects/templates/template_formal/manuscript/` → `No issues found!`.
- `uv run python scripts/audit/check_template_drift.py --strict` → the one reported error belongs to `templates/template_pools_rules_tools` (`config_example_missing_section`), a separate template out of this round's scope (see this repo's own memory note on template ownership); zero drift findings for `template_formal` itself.

**Round 8 (Experiment B real-vs-null attribution confound closed via a zero-deposit ablation, 2026-07-09):** ISC-108 added and verified (see Decisions). Real, quoted output, primary-session direct runs:
- `uv run pytest tests/colony/test_colony_experiments_extended.py -v -k "zero_deposit" --no-cov` (run from `projects/templates/template_formal/`) →
  ```
  test_zero_deposit_wall_clock_stays_within_budget PASSED
  test_zero_deposit_config_constructs_without_error_and_never_deposits PASSED
  test_zero_deposit_real_mechanism_does_not_beat_the_null_model_closing_the_stigmergy_confound PASSED
  test_zero_deposit_collapse_relative_to_the_full_mechanism_implicates_the_deposit_channel_specifically PASSED
  4 passed, 21 deselected in 43.77s
  ```
  Real numbers printed by the new tests: zero-deposit mechanism (`deposit_amount=0.0`, `seed_base=0`, `N=150`) — `successes=0/150 rate=0.0000 wilson=(0.0000,0.0250)`; null model (same `real_vs_null_results` fixture) — `successes=1/150 wilson=(0.0012,0.0368)`; full mechanism (`deposit_amount=1.0`, same fixture) — `successes=140/150 wilson=(0.8816,0.9634)`. The falsifiable comparison: zero-deposit lower bound (`0.0000`) does NOT exceed null upper bound (`0.0368`) — $H_0$ survives, closing the attribution confound in favor of the stigmergy account.
- `uv run pytest tests/colony/test_colony_experiments_extended.py --no-cov -q` (run from `projects/templates/template_formal/`) → `25 passed in 210.17s` (21 pre-existing + 4 new; no regression in the pre-existing 21).
- `cd /Users/4d/Documents/GitHub/template/projects/templates/template_formal && MYPYPATH=src uv run mypy --strict --explicit-package-bases --namespace-packages src` → `Success: no issues found in 26 source files` (no `src/` code changed this round — `ColonyTrialConfig.deposit_amount=0.0` required no new guard, confirmed by direct construction in `test_zero_deposit_config_constructs_without_error_and_never_deposits`).
- `uv run python scripts/pipeline/stage_01_test.py --project templates/template_formal --project-only` → `248 passed`, `Total coverage: 95.7%` (>= 90% floor; unchanged from round 7, as expected — only new tests were added, no new `src/` code).
- `uv run python -m infrastructure.validation.cli markdown projects/templates/template_formal/manuscript/` → `No issues found!`.
- `uv run python scripts/audit/check_template_drift.py --strict` → the one reported error still belongs to `templates/template_pools_rules_tools` (`config_example_missing_section`), out of this round's scope; zero drift findings for `template_formal` itself.

**Round 9 (dedicated `sensed_concentration_cap` ablation of Experiment A's mechanistic account, 2026-07-09):** ISC-109 added and verified (see Decisions). Real, quoted output, primary-session direct runs:
- `uv run pytest tests/colony/test_colony_experiments_extended.py -k "capped or sensed_concentration" -v --no-cov` (run from `projects/templates/template_formal/`) →
  ```
  test_capped_low_decay_wall_clock_stays_within_budget PASSED
  test_sensed_concentration_cap_rejects_nonpositive_values PASSED
  test_capping_sensed_concentration_recovers_convergence_at_low_decay PASSED
  3 passed, 25 deselected in 36.77s
  ```
  Real numbers printed by the new tests: capped (`sensed_concentration_cap=13.0`, `seed_base=0`, `n=60`) — decay=0.10 `successes=60/60 wilson=(0.9398,1.0000)`; decay=0.30 `successes=60/60 wilson=(0.9398,1.0000)`; uncapped baseline (from `decay_sweep_points`) — decay=0.10 `successes=0/60 wilson=(0.0000,0.0602)`; decay=0.30 `successes=0/60 wilson=(0.0000,0.0602)`. The falsifiable comparison: capped lower bound (`0.9398`) DOES exceed uncapped upper bound (`0.0602`) at both decay values — $H_0$ ("capping does not improve convergence at low decay") is rejected, corroborating the mechanistic account.
- `uv run pytest tests/colony/test_colony_experiments_extended.py --no-cov -q` (run from `projects/templates/template_formal/`) → `28 passed in 143.49s (0:02:23)` (25 pre-existing + 3 new; no regression in the pre-existing 25).
- `cd /Users/4d/Documents/GitHub/template/projects/templates/template_formal && MYPYPATH=src uv run mypy --strict --explicit-package-bases --namespace-packages src` → `Success: no issues found in 26 source files` (new `sensed_concentration_cap: float | None = None` field type-checks clean).
- `uv run python scripts/pipeline/stage_01_test.py --project templates/template_formal --project-only` (from repo root) → `251 passed in 279.97s (0:04:39)`, `Total coverage: 95.72%` (>= 90% floor).
- `uv run python -m infrastructure.validation.cli markdown projects/templates/template_formal/manuscript/` → `No issues found!`.
- `uv run python scripts/audit/check_template_drift.py --strict` → the one reported error still belongs to `templates/template_pools_rules_tools` (`config_example_missing_section`), out of this round's scope; zero drift findings for `template_formal` itself.
- `uvx ruff check` + `uvx ruff format --check` on the two changed files (`colony/experiment.py`, `test_colony_experiments_extended.py`) → `All checks passed!` / `2 files already formatted`.

**Round 10 (multiple-comparisons methodology note, 2026-07-09):** ISC-110 added and verified (manuscript-only, added directly by the primary session after fixing the ISC-108/109 Criteria-section placement bug above). Real, quoted output:
- `uv run python -m infrastructure.validation.cli markdown projects/templates/template_formal/manuscript/` → `No issues found!`.
- Grep of the manuscript for `Fisher`/`p =`/`p<`/`significan*` confirmed no instance of scanning multiple pairwise comparisons and reporting only the smallest p-value as significant — the selective-inference pattern a correction would guard against does not occur in the current text.
- Independently re-verified by the primary session post-workflow: full project gate `uv run python scripts/pipeline/stage_01_test.py --project templates/template_formal --project-only` → `251 passed`, `Total coverage: 95.7%`; `MYPYPATH=src uv run mypy --strict --explicit-package-bases --namespace-packages src` → `Success: no issues found in 26 source files`; `uv run python -m infrastructure.validation.cli markdown projects/templates/template_formal/manuscript/` → `No issues found!`.

**Round 11 (closes half of the extreme positive control's decay/noise confound with a real zero-deposit ablation at the extreme configuration, 2026-07-09):** ISC-111 added and verified (see Decisions). Real, quoted output, primary-session direct runs:
- `uv run python -c "..."` (a real, seeded, deterministic script computing both conditions directly against `src/template_formal/colony/experiment.py`, independently re-run twice with bit-identical results before pinning) →
  ```
  full (deposit=1.0) successes: 9 / 50
  full wilson: (0.09770192670234178, 0.3079605367430078)
  zero-deposit successes: 0 / 50
  zero wilson: (0.0, 0.07134759913335868)
  fisher p (full vs zero): 0.002634204400259045
  ```
  The falsifiable comparison: full mechanism's Wilson lower bound (`0.0977`) DOES exceed the zero-deposit condition's Wilson upper bound (`0.0713`), and the two-sided Fisher exact `p=0.002634...` clears `alpha=0.05` — $H_0$ ("the zero-deposit condition converges no differently than the full-mechanism positive control at this extreme configuration") is rejected.
- `uv run pytest tests/colony/test_colony_experiments_extended.py -v --no-cov` (run from `projects/templates/template_formal/`) → `32 passed in 248.30s (0:04:08)` (28 pre-existing + 4 new: `test_extreme_positive_control_wall_clock_stays_within_budget`, `test_extreme_full_mechanism_reproduces_the_sibling_positive_control_wilson_upper_below_half`, `test_extreme_zero_deposit_config_constructs_without_error`, `test_extreme_zero_deposit_pheromone_channel_still_contributes_under_severe_decay_and_noise` — no regression in the pre-existing 28, every previously-pinned `successes` count unchanged).
- `uv run pytest tests/colony/test_colony_convergence_statistics.py -v --no-cov` (the sibling positive-control test, left unmodified) → `6 passed in 33.06s`, confirming this round's new module did not disturb it.
- `cd /Users/4d/Documents/GitHub/template/projects/templates/template_formal && MYPYPATH=src uv run mypy --strict --explicit-package-bases --namespace-packages src` → `Success: no issues found in 26 source files` (no `src/` code changed this round — the new tests only construct existing `ColonyTrialConfig` fields).
- `uv run python scripts/pipeline/stage_01_test.py --project templates/template_formal --project-only` (from repo root) → `255 passed`, `Total coverage: 95.7%` (>= 90% floor; 251 + 4 new, unchanged coverage percentage since only new tests were added, no new `src/` code).
- `uv run python -m infrastructure.validation.cli markdown projects/templates/template_formal/manuscript/` → `No issues found!`.
- Manuscript re-rendered (`scripts/pipeline/stage_03_render.py --project templates/template_formal`, 21 pages) and re-confirmed via page-scale raster read (page 12, the new `Hypothesis 7` block): renders as proper typeset math (`\sigma_{\text{sense}}=4.0`, etc.), not ASCII-mangled text; `pdftotext` scan of the full formalism sequence confirms `Definition 1` through `Theorem 13`, one continuous run, zero gaps/duplicates, zero `??` marks.

**Round 12 (gates the sensed_concentration_cap dose-response curve with a real sweep, 2026-07-09):** ISC-112 added and verified (see Decisions). Real, quoted output, primary-session direct runs:
- `uv run python /tmp/probe_sweep.py` (a real, seeded, deterministic script calling `colony/sweep.py`'s `run_parameter_sweep` directly with `param_name="sensed_concentration_cap"`, `decay=0.10` fixed, seven values, `n=60`, `seed_base=0` — independently re-run twice with bit-identical results before pinning) →
  ```
  cap=12.50 successes=60/60 rate=1.0000 wilson=(0.9398,1.0000)
  cap=13.00 successes=60/60 rate=1.0000 wilson=(0.9398,1.0000)
  cap=15.00 successes=41/60 rate=0.6833 wilson=(0.5577,0.7869)
  cap=16.00 successes=13/60 rate=0.2167 wilson=(0.1312,0.3362)
  cap=17.00 successes=3/60 rate=0.0500 wilson=(0.0171,0.1370)
  cap=18.00 successes=1/60 rate=0.0167 wilson=(0.0029,0.0886)
  cap=20.00 successes=0/60 rate=0.0000 wilson=(0.0000,0.0602)
  CA trend across full sweep: Z=-16.42452071373818 p=0.0
  fisher plateau(13.0) vs floor(20.0): p=4.308978969823708e-11   [WRONG — see Round 14 Changelog]
  ```
  **Correction (Round 14, added after a cross-vendor audit caught a real bug this exact comparison
  exercised for the first time — see the Changelog entry below for the full account):** the quoted
  `p=4.308978969823708e-11` above was wrong by 24 orders of magnitude, produced by an additive-
  tolerance bug in `fisher_exact_test_two_sided` that only manifests on highly-separated tables.
  Re-run after the fix: `fisher plateau(13.0) vs floor(20.0): p=2.070073888186964e-35` — independently
  confirmed against `scipy.stats.fisher_exact` and the closed-form `2/C(120,60)` for this exact
  60/60-vs-0/60 table. The falsifiable comparison: the rate declines monotonically at every step (no
  reversal) — $H_0$ ("no variation") is rejected under either value — and the decline is essentially
  complete by `cap=18.0` (98%+ of the total plateau-to-floor decline), a full `2.0` cap units before
  the informally-named `cap=20.0` "fade out" point.
- `uv run pytest tests/colony/test_colony_experiments_extended.py -k cap_dose_response -v --no-cov` (run from `projects/templates/template_formal/`) → `8 passed in 63.26s` (all new: `test_cap_dose_response_wall_clock_stays_within_budget`, `test_cap_dose_response_real_numbers_pin_the_exact_counts`, `test_cap_dose_response_is_monotonically_non_increasing_h0_rejected`, `test_cap_dose_response_transition_completes_well_before_the_informal_fadeout_point`, `test_cap_dose_response_cap13_point_matches_experiment_h_already_gated_single_point`, `test_cap_dose_response_cap20_point_matches_the_uncapped_baseline_exactly`, `test_cap_dose_response_cochran_armitage_confirms_the_overall_trend`, `test_cap_dose_response_fisher_plateau_vs_floor_is_significant`).
- `uv run pytest tests/colony/test_colony_experiments_extended.py tests/colony/test_colony_convergence_statistics.py --no-cov -q` (full existing files, not just the new tests) → `46 passed in 173.81s (0:02:53)` — zero regressions, every previously-pinned `successes` count unchanged.
- `cd /Users/4d/Documents/GitHub/template/projects/templates/template_formal && MYPYPATH=src uv run mypy --strict --explicit-package-bases --namespace-packages src` → `Success: no issues found in 26 source files` (no `src/` code changed this round — the new tests only call the existing, already-generic `run_parameter_sweep`).
- `uv run python scripts/pipeline/stage_01_test.py --project templates/template_formal --project-only` (from repo root) → `263 passed`, `Total coverage: 95.7%` (>= 90% floor; 255 + 8 new, unchanged coverage percentage since only new tests were added, no new `src/` code).
- `uv run python -m infrastructure.validation.cli markdown projects/templates/template_formal/manuscript/` → `No issues found!`.
- Manuscript re-rendered (`scripts/pipeline/stage_03_render.py --project templates/template_formal`, 22 pages) and re-confirmed via page-scale raster read (pages 15-16, the new `Hypothesis 10` block and its two-part table): renders as proper typeset text and numerals, not ASCII-mangled; `pdftotext` scan of the full formalism sequence confirms `Definition 1` through `Theorem 14`, one continuous run, zero gaps/duplicates, zero `??` marks.

**Round 13 (crosses Experiment B's null-model harness with Experiment C's heterogeneity sweep at its most extreme point, 2026-07-09):** ISC-113 added and verified (see Decisions). Real, quoted output, primary-session direct runs:
- `uv run python -c "..."` (independently computing `wilson_score_interval`/`fisher_exact_test_two_sided` directly against the pinned `successes` counts already gated in `test_colony_experiments_extended.py`, before writing any new test) →
  ```
  seed0 very_wide 2/60 wilson (0.009189319256197889, 0.11363774274308595)
  null 1/150 wilson (0.0011778013350174303, 0.036792839774818134)
  fisher p seed0 very_wide vs null: 0.19698722330301277
  seed7000 very_wide 5/60 wilson (0.03612045660173077, 0.18068942018334405)
  wide seed0 15/60 vs null p= 2.1362039099111572e-08
  wide seed7000 14/60 vs null p= 8.503789584292909e-08
  ```
- A fresh, independently-run `NullModelTrialConfig`/`run_null_model_trial` loop at `seed_base=7000` (matching `heterogeneity_sweep_results_seed7000`'s own seed block, per ISC-108's apples-to-apples principle, rather than reusing the `seed_base=0` null figure) →
  ```
  seed_base=7000 null successes: 0 /150
  ```
  — different from the originally-proposed finding's preliminary estimate (which had reused the `seed_base=0` null figure for both comparisons); recomputed the `very_wide`-vs-null Fisher test at `seed_base=7000` against this fresh, correctly-matched baseline: `fisher_exact_test_two_sided(5, 60, 0, 150)` → `p=0.0016835563479717132` (even more significant than the proposing pass's estimate, same qualitative conclusion — H0 rejected).
- `uv run pytest tests/colony/test_colony_experiments_extended.py -k "very_wide or wide_condition or null_model_seed7000 or very_wide_vs_null" --no-cov -q -s` (run from `projects/templates/template_formal/`) → `5 passed in 52.11s` (all new: `test_very_wide_vs_null_wall_clock_stays_within_budget`, `test_null_model_seed7000_reproduces_a_near_zero_rate_like_seed0`, `test_very_wide_at_seed0_does_not_clear_the_null_model_baseline`, `test_very_wide_at_seed7000_does_clear_the_null_model_baseline`, `test_wide_condition_clears_the_null_model_baseline_at_both_seed_bases`).
- `uv run pytest tests/colony/test_colony_experiments_extended.py tests/colony/test_colony_convergence_statistics.py --no-cov -q` (full existing files, not just the new tests) → `51 passed in 211.83s (0:03:31)` — zero regressions, every previously-pinned `successes` count unchanged (46 pre-existing + 5 new).
- `cd /Users/4d/Documents/GitHub/template/projects/templates/template_formal && MYPYPATH=src uv run mypy --strict --explicit-package-bases --namespace-packages src` → `Success: no issues found in 26 source files` (no `src/` code changed this round — the new tests only call the existing `ColonyTrialConfig`/`run_colony_trial`/`NullModelTrialConfig`/`run_null_model_trial`/`wilson_score_interval`/`fisher_exact_test_two_sided`).
- `uv run python scripts/pipeline/stage_01_test.py --project templates/template_formal --project-only` (from repo root) → `268 passed`, `Total coverage: 95.72%` (>= 90% floor; 263 + 5 new, unchanged coverage percentage since only new tests were added, no new `src/` code).
- `uv run python -m infrastructure.validation.cli markdown projects/templates/template_formal/manuscript/` → `No issues found!`.
- Manuscript re-rendered (`scripts/pipeline/stage_03_render.py --project templates/template_formal`, 22 pages) and re-confirmed via page-scale raster read (page 18, the new `Hypothesis 14` block and its two-row table): renders as proper typeset math/text, not ASCII-mangled; `pdftotext` scan of the full formalism sequence confirms `Definition 1` through `Hypothesis 14` through `Theorem 15`, one continuous run, zero gaps/duplicates, zero `??` marks.

**Cross-vendor audit, round 7 (Forge, read-only):** verdict **PASS** — independently re-ran mypy --strict (26 files) and the full 251-test suite, independently re-derived the `sensed_concentration_cap=13.0` justification from the stated constants (preference ceiling 12.0 + 2×sensing-noise-σ 1.0 = 13.0 — a principled a-priori rule, not tuned to win), read the actual clip code in `experiment.py` line-by-line to confirm `min(sense, cap)` is applied *before* the Gaussian noise term (not after, as merely described), independently grepped the entire manuscript for every significance-adjacent phrase to confirm the ISC-110 methodology claim is genuinely true of the current text, and independently re-counted the manuscript's real formalism blocks (resolving its own initial miscount — a `grep` false-positive matching a comment in `preamble.md` — down to the correct 12, confirming "Definition 1 through Theorem 12" is accurate) and the ISA.md structural fix (confirmed ISC-107/108/109/110 each appear exactly once, all correctly inside `## Criteria`, zero leakage into `## Decisions`). One minor, pre-existing CONCERN found (not a template_formal regression): `MAINTAINERS.md`'s "Public vs private scope" paragraph enumerated only 10 of the real 20 public exemplars by name — stale since well before this round, and the round-7 roster-hygiene pass correctly added `template_formal` to this exact list without noticing the broader list was already incomplete. Fixed immediately by the primary session: replaced the hand-maintained inline enumeration with a pointer to `docs/_generated/active_projects.md` alone (mirroring the pattern this same file's own `## Subsystem ownership` table already uses at the "Public exemplar projects" row) — drift-proof rather than re-enumerating 20 names that will need updating again the next time a template is added. Re-verified: the corrected sentence reads cleanly and makes no further factual claim that could go stale.

**Round 8 (docs/ guide-tree refresh, documentation-only, 2026-07-09):** no new ISC (mirrors round 5's precedent — documentation maintenance, not a new testable criterion). A dedicated pass re-read all six `docs/` topic guides against the real current code/manuscript state after rounds 6-9 added `cover_art.py`, the auto-numbered formalisms, `cochran_armitage_trend_test`, the TLA+ negative control, and three new ablations. Found and fixed real drift in four of six guides (`architecture.md` missing the `cover_art.py` module and understating the script count; `formal_methods_guide.md` describing a 2-check `check_formal_specs.sh` when it now runs 4, and omitting the TLA+ near-vacuity/negative-control episode from its own "near-vacuity lesson" section; `statistics_methodology_guide.md` describing "three experiments" when there are now eight lettered sub-experiments, and a stale "cross-seed robustness not gated" claim now false; `testing_guide.md`'s stale test/experiment counts and file table). `type_system_guide.md` and `security_guide.md` were correctly left untouched after verifying no new type-vocabulary pattern or validation-pattern class was introduced since round 5. Re-verified: `mypy --strict` → `Success: no issues found in 26 source files`; full project gate → `255 passed`, `95.72%` coverage; `infrastructure.validation.cli markdown` on `docs/` → `No issues found!`.

**Round 11 (closes the extreme positive control's decay/noise confound at decay=0.97/sensing_noise_std=4.0, 2026-07-09):** ISC-111 added and verified (see Decisions). Real, surprising, honestly-reported finding: even under near-total pheromone evaporation and severe sensing noise, the pheromone channel still contributes something statistically real — full mechanism `9/50` vs. zero-deposit `0/50` at the identical extreme configuration, Fisher exact `p=0.00263` (significant). Re-verified: `mypy --strict` → `Success: no issues found in 26 source files`; `255 passed` (251 baseline + 4 new).

**Round 12 (gates the sensed_concentration_cap dose-response curve with a real sweep, 2026-07-09):** ISC-112 added and verified (see Decisions). `colony/sweep.py::run_parameter_sweep` confirmed genuinely generic over `sensed_concentration_cap` (no new sweep machinery needed). Real result at decay=0.10, `n=60`/value, caps `{12.5,13.0,15.0,16.0,17.0,18.0,20.0}`: successes `60/60,60/60,41/60,13/60,3/60,1/60,0/60` — Cochran-Armitage `Z=-16.42`, `p<1e-10`. Honestly corrects the informal probe's "smooth gradual fade" framing: the real shape is a plateau followed by a decline essentially complete by cap=18.0, a full 2.0 units before the informally-named cap=20.0 "fade-out" point. Re-verified: `mypy --strict` clean; `263 passed` (255 baseline + 8 new).

**Round 13 (crosses Experiment B's null-model harness with Experiment C's heterogeneity sweep at its most extreme point, 2026-07-09):** ISC-113 added and verified (see Decisions). A genuinely new falsifiable question two independently-built instruments (the null-model harness, built for Experiment B; the heterogeneity sweep, built for Experiment C) had never been composed to answer: does the stigmergic mechanism's statistically-established advantage over chance survive at the sweep's most heterogeneity-stressed point? Real, seed-base-dependent disagreement, reported honestly rather than resolved in favor of a cleaner story: at `seed_base=0`, `very_wide`'s `2/60` vs. the null model's `1/150` gives Fisher `p=0.19698722330301277` — H0 **survives** (not significant, the mechanism's advantage is not distinguishable from chance at this extreme point). At the pre-existing, disjoint `seed_base=7000` replicate, `very_wide`'s `5/60` vs. a **freshly-computed, seed-7000-matched** null baseline (`0/150`, not the `seed_base=0` null figure) gives Fisher `p=0.0016835563479717132` — H0 **is rejected** (significant; the advantage does survive there). A scoping check confirms `wide` clears its matching null baseline decisively at both seed bases (`p<1e-7` each) — the ambiguity is specific to the sweep's single most extreme point. The build agent caught and corrected a real arithmetic shortcut in the verify-select pass's own estimate (which had proposed reusing the `seed_base=0` null figure for the `seed_base=7000` comparison instead of computing a freshly seed-matched one, per this project's own "identical seed block" apples-to-apples discipline) before committing the result to the manuscript. Re-verified: `mypy --strict` clean; `268 passed` (263 baseline + 5 new), `95.72%` coverage; manuscript re-rendered (22 pages), raster-confirmed page 18 (`Hypothesis 14`, sequence `Definition 1`..`Theorem 15` continuous).

**Cross-vendor audit, round 8 (Forge, read-only): verdict CONCERNS — breaks the two-consecutive-clean-audit streak with a real, serious finding.** Independently re-ran mypy --strict (26 files) and the full 268-test suite; independently re-verified ISC-111's distinctness from ISC-108 (different configurations: calibrated baseline vs. the extreme decay=0.97/sensing_noise_std=4.0 positive control); independently re-derived ISC-112's dose-response counts and confirmed the plateau-then-sharp-decline correction is honestly reflected in the manuscript, not just the build agent's self-report; independently recomputed ISC-113's two Fisher p-values three ways and confirmed the fresh seed_base=7000 null baseline is a real run, not a hardcoded number; spot-checked the docs/ refresh against real code; confirmed ISA.md structural placement is now correct (the round-7 lesson was applied — zero Criteria bullets leaked into Decisions this round); independently recounted the manuscript's 15 real formalism blocks, correctly excluding the `preamble.md` comment false-positive that had tripped up the round-7 audit. **Then, rather than stop at 5-for-5 agreement, cross-checked the sixth statistic — ISC-112's plateau-vs-floor Fisher test, the first pinned case in this function's history with an observed probability far below the function's internal tolerance — against `scipy.stats.fisher_exact` and a closed-form hand computation, and found the project's own `fisher_exact_test_two_sided` wrong by 24 orders of magnitude** (see ISC-114 and the corresponding Changelog entry above for the full technical account and fix). This is the single most severe defect found across all 8 rounds of this project's adversarial hardening, caught specifically because this audit pass did not treat five-for-five internal agreement as sufficient and cross-checked against an independent oracle instead. Re-verified after the fix, all by the primary session: `mypy --strict` → `Success: no issues found in 26 source files`; full project gate → `269 passed` (268 + 1 new dedicated regression test), `Total coverage: 95.7%`; `infrastructure.validation.cli markdown` on `manuscript/` → `No issues found!`; manuscript re-rendered (24 pages) and re-confirmed via page-scale raster read (page 16): the corrected `p≈2.07×10⁻³⁵` and the full honest bugfix-disclosure paragraph both render correctly, proper typeset math, zero `??` marks.

Coverage (round 14, cumulative): 114/114 ISC groups passed, 114/114 artifact-backed (0 `[DEFERRED-VERIFY]`).

Coverage (round 13, cumulative): 113/113 ISC groups passed, 113/113 artifact-backed (0 `[DEFERRED-VERIFY]`).

Coverage (round 12, cumulative): 112/112 ISC groups passed, 112/112 artifact-backed (0 `[DEFERRED-VERIFY]`).

Coverage (round 11, cumulative): 111/111 ISC groups passed, 111/111 artifact-backed (0 `[DEFERRED-VERIFY]`).

Coverage (round 10, cumulative): 110/110 ISC groups passed, 110/110 artifact-backed (0 `[DEFERRED-VERIFY]`).

Coverage (round 9, cumulative): 109/109 ISC groups passed, 109/109 artifact-backed (0 `[DEFERRED-VERIFY]`).

Coverage (round 8, cumulative): 108/108 ISC groups passed, 108/108 artifact-backed (0 `[DEFERRED-VERIFY]`).

Coverage (round 7, cumulative): 107/107 ISC groups passed, 107/107 artifact-backed (0 `[DEFERRED-VERIFY]`) — corrected from an initial `97/97` transposition typo, caught during the primary session's independent re-verification pass.

Coverage (round 6, cumulative): 106/106 ISC groups passed, 106/106 artifact-backed (0 `[DEFERRED-VERIFY]`).

**Round 15 (verifier closure, figure-quality oracle, and live method-surface synchronization, 2026-07-10):** ISC-115..117 added and verified. The baseline adversarial pass found three false-green surfaces: Stage 04 could exit zero while its evidence registry was false, figure writers accepted any non-empty output bytes and the analysis stage treated missing figures as optional, and the live docs/experiment plan had drifted from the eight-analysis/two-figure/275-test method surface. Fixed with a 101-row source-owned claim ledger and source-path resolver, readable/non-blank PNG validation plus broken-destination, invalid-PNG, and single-color-PNG negative controls, required figure registry entries and fail-closed orchestration, `experiment_plan.yaml` figure declarations, and a project synchronization test spanning manuscript labels, fixtures, counts, coverage, and registry. Final evidence, all from the current tree:

- `uv run pytest tests -q --cov=src/template_formal --cov-report=term-missing` → **275 passed in 240.19s**, 95.93% coverage, zero skips/failures.
- `uv run mypy --strict --explicit-package-bases --namespace-packages src` → `Success: no issues found in 26 source files`; `uv run ruff check src scripts tests` → clean.
- `uv run python scripts/02_run_analysis.py` → real deterministic demo/sweep outputs plus both required figures and a complete two-entry registry; the PNG oracle independently reports `(720,720)` and `(1080,480)` with hundreds of rendered colors.
- `bash scripts/check_formal_specs.sh` → Lean 7 theorem build with zero extra axioms; ideal TLC 5 distinct states; faulty TLC 92 distinct states; the inverted negative control correctly detects the injected `NoFalseEstablishment` violation; exit 0.
- `stage_03_render.py` → validated 23-page combined PDF, HTML, and seven slide decks; `stage_04_validate.py` → all eight checks pass, including figure registry and evidence registry; `stage_05_copy.py` → 419 artifacts copied to the registered root output.
- `qpdf --check` and `pdftotext` checks pass; page-scale raster reads of the type/invariant page, experiment figure page, and formal-results page are legible and correctly typeset. Browser QA served the correct sibling-root layout: both figures load at their real dimensions, the results section navigates to “Eight pre-registered analyses,” and the browser console has no errors.
- `check_template_drift.py --project templates/template_formal --strict`, `check_tracked_all.py`, and the ten project synchronization/figure-quality tests all pass. The only repo-wide gate not clean is the unrelated global module-line advisory, which fails on pre-existing `template_pools_rules_tools/src/figures.py` (952 lines); this project contributes only a warning for `scripts/02_run_analysis.py` at 155 lines.

Coverage (round 15, cumulative): 117/117 ISC groups passed, 117/117 artifact-backed (0 `[DEFERRED-VERIFY]`).

**Round 16 (publication-asset fail-closed boundary, fork-command synchronization, and web-link closure, 2026-07-10):** ISC-118..119 added and verified. The adversarial follow-up identified four remaining false-green surfaces: the committed cover-art regeneration script could report success when the library silently returned `None`; fork-facing documentation/config surfaces retained an unsafe bare root-level mypy invocation for this namespace-package layout; the six-fixture mapping was stale in the ISA/fork guide; and the rendered references page linked to a bibliography path that was not present under `output/web/`. The publication path now calls `require_cover_art`, which raises when the asset cannot be written; the low-level generator remains tolerant for library callers, while the committed-asset boundary is explicit and fail-closed. Cover-art tests exercise a real blocked destination and inspect PNG format, dimensions, and rendered-color diversity; the test also binds the script to the required wrapper. README, AGENTS.md, TODO.md, STANDALONE.md, and the project skill now share the `MYPYPATH`-qualified command and 26-source-file result, with stale-command and six-fixture mapping guards in `tests/test_project_surface_sync.py`. The references page now targets the rendered bundle's existing `../pdf/references.bib` asset, and the same synchronization test verifies the generated HTML path when output exists. The remaining `tests/AGENTS.md` coverage/ISC range drift was corrected to 96.03% and ISC-1..119.

- Red phase: the documentation contract failed with 1 failure and 10 passing tests against the old `STANDALONE.md` command surface; the publication-link contract also failed against stale generated HTML before rerender.
- Green phase: `uv run pytest tests/test_project_surface_sync.py tests/colony/test_cover_art.py -q` → **11 passed**; the full project gate → **277 passed**, 0 failures, 0 skips, 96.03% source coverage; strict mypy → **26 source files clean**; Ruff check and format → clean.
- Publication/runtime phase: the cover-art command wrote the committed PNG successfully; Stage 03 rendered the combined PDF, HTML, and seven slide decks; Stage 04 passed all eight checks; Stage 05 copied **420 files** to the registered root output. The optional `simulations/`, `llm/`, and `logs/` directories are absent by design for this project and remain non-failing warnings.
- Visual/runtime QA: Playwright captured a full-page HTML screenshot and HAR with 13/13 successful requests; curl returned 200 for both the rendered references page and `/pdf/references.bib`; Chrome verified both images complete at `720x720` and `1080x480`, the results anchor, and zero console errors. The final browser navigation and artifact checks were run against the regenerated output.
- `bash scripts/check_formal_specs.sh`, `check_template_drift.py --project templates/template_formal --strict`, `check_tracked_all.py`, and `scripts/audit/verify_no_mocks.py` remain clean. The only repo-wide exception remains the unrelated global module-line advisory on pre-existing `template_pools_rules_tools/src/figures.py` (952 lines); this project contributes only the existing 155-line orchestrator warning.
- Shared publication hardening is now covered and re-verified: both LaTeX compilation paths use `-no-shell-escape`, Stage 05 materializes symlink targets instead of copying external links, and standalone Stage 04 validation creates a non-empty 372-entry artifact manifest from current outputs. The focused parent core/rendering lane passes 83 tests; the broader parent run reached its pre-existing Mermaid subprocess timeout after earlier tests passed, so that unrelated environment-dependent lane is not claimed green.

Coverage (round 16, cumulative): 119/119 ISC groups passed, 119/119 artifact-backed (0 `[DEFERRED-VERIFY]`).
